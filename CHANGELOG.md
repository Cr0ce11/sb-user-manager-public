# sb-user-manager 迭代记录

版本规则：

- 小修小补、交互优化、兼容性修复：递增小版本号，例如 `4.2.1` → `4.2.2`。
- 新增重要功能、结构或数据模型发生重大变化：递增大版本号，例如 `4.2.x` → `4.3.0`。
- 每次迭代必须保留上一版本作为回退点，在本文件顶部追加增量记录，并通过本地门禁与 GitHub CI 后再发布。

## Unreleased

- 修复首次交互启动维护 `sbm` 快捷入口时调用未定义 `pause` 的问题：快捷入口被占用或自动创建失败时现在使用真实菜单暂停函数，显示提示后继续进入管理界面，不再出现 `command not found` 或提前退出。移除会掩盖缺陷的测试桩，并新增裸命令目标静态门禁及反向样例，对应 [公开 Issue #36](https://github.com/DTB201/sb-user-manager-public/issues/36)。
- 修复无人值守到期检查对损坏 `expires_at` 静默放行的问题：有效期无法解析为时间戳时会明确告警并跳过该用户，但继续处理其余用户；“服务与配置检查”同步将其列为 `[需要处理]` 且不自动猜测日期。新增坏记录与正常到期记录并存、后续用户仍停用及一致性报告回归测试，对应 [公开 Issue #38](https://github.com/DTB201/sb-user-manager-public/issues/38)。
- 为用户/分流操作与部署、现有环境接管、完整卸载、检查更新建立统一单写者锁，并让操作事务与环境事务双向拒绝对方仍活动的恢复记录；锁冲突会在任何环境写入前明确停止，成功、失败、回滚和信号中断路径均释放锁。全新环境会安全创建锁目录，完整卸载保留锁文件以避免 inode 替换绕过互斥，对应 [公开 Issue #40](https://github.com/DTB201/sb-user-manager-public/issues/40)。

## 4.25.5 - 2026-08-13

- “续期用户”扩展为“调整用户有效期”：正整数继续按月延长，负整数按原到期时间提前；提前前显示新旧时间并要求确认，且不允许把到期时间调整到当前时间或更早。提前有效期不自动启用已停用用户、不清零流量、不改写 sing-box 配置；既有正数续期行为保持不变。新增正负月数、确认取消、过期拒绝、状态与流量保持的回归测试，对应 [公开 Issue #31](https://github.com/DTB201/sb-user-manager-public/issues/31)。
- 负数月使用无歧义的“N 个月前”表达式，避免 GNU `date` 把 `-N` 误当成数字时区；正负方向按输入符号判断，避免超大月数在 Bash 整数运算中溢出后走错分支。
- 本版本由 [公开 Issue #33](https://github.com/DTB201/sb-user-manager-public/issues/33) 跟踪发布，仅采用本地门禁与公开仓库 GitHub CI 验收，不登录或修改服务器；不可变 v4.25.4 Release 保留为直接代码回退点。

## 4.25.4 - 2026-08-13

- 休眠 v5 退役进入第一阶段：普通启动不再识别或分发入口控制器、落地机和首次角色选择，恢复为唯一的 standalone 启动链；两个历史落地 helper 文件名保留失败关闭墓碑，避免旧符号链接误入普通管理菜单。
- standalone 启动的关键步骤全部显式失败即止，不依赖 Bash `set -e`；环境恢复日志的普通文件、所有者和权限检查下沉到恢复函数，到期定时任务仅在 10 项既有单机部署足迹完整时执行。新增测试覆盖关键步骤逐项失败、恢复日志符号链接/错误所有者/过宽权限，以及 fresh、external 和逐项不完整部署的只读拒绝。本阶段对应 [公开 Issue #17](https://github.com/DTB201/sb-user-manager-public/issues/17)，不改变菜单结构、状态 schema、迁移格式、用户数据或运行服务；其余已不可达 v5 低层模块后续另行删除。
- 休眠 v5 退役完成第二阶段：删除 16 个已不可达的控制器/落地源码模块、20 个专属测试、v5 POC 和 ADR 0005–0027；CI 改为只验证 standalone、旧私有版接管、jq 1.6 和固定 Debian 12 兼容性，同时暂时保留既有必需检查名称，避免破坏分支保护。生成脚本只留下两个历史 helper 文件名的失败关闭墓碑；不主动清理服务器上的历史文件，不改变 v4 多协议、状态 schema 7、迁移备份、事务、菜单或服务行为。
- 固定 Debian 12 的独立版门禁由历史名称 `debian-landing-e2e` 更正为 `debian-standalone-e2e`，Release 依赖和治理文档同步使用新名称；切换过程先并行验证新旧检查，再更新分支保护并删除旧别名，不改变运行代码或服务器。
- 本版本由 [公开 Issue #28](https://github.com/DTB201/sb-user-manager-public/issues/28) 跟踪发布，仅采用本地门禁与公开仓库 GitHub CI 验收，不登录或修改服务器；不可变 v4.25.3 Release 保留为直接代码回退点。

## 4.25.3 - 2026-08-13

- 修复“续期用户”把输入月数误解析为数字时区、导致 2/3/6/12 等输入仍通常只增加 1 个月的问题；续期现在使用无歧义的月份表达式，并在有效期或日期计算异常时于事务开始前停止。新增多月确切到期时间、已过期用户和真实 GNU date 回归测试。本项对应 [公开 Issue #20](https://github.com/DTB201/sb-user-manager-public/issues/20)，不改变菜单、状态 schema、迁移格式、自动启用、流量清零或事务顺序。
- 项目永久收敛到 `DTB201/sb-user-manager-public`：公开仓库成为代码、Issue、Pull Request、完整 CI、匿名更新与不可变 Release 的唯一正式来源，私有双版本和原 v5 方向不再恢复；仍运行旧私有版的服务器继续保留单向无损接管到公开版的兼容路径。公开 Issues 与私密漏洞报告已启用，Mihomo 候选需求已迁移，休眠 v5 清理由独立 Issue 跟踪。本项对应 [公开 Issue #15](https://github.com/DTB201/sb-user-manager-public/issues/15)，不修改运行代码、版本、状态 schema、服务器或用户数据。
- 此前已按当时决定对齐 v4.25.2 的公开迁移与双仓库边界；该边界现由上方单一公开仓库决定取代。本项只记录治理演进，不改变运行代码或服务器；原工作由公开 [PR #14](https://github.com/DTB201/sb-user-manager-public/pull/14) 完成。
- 本版本由 [公开 Issue #22](https://github.com/DTB201/sb-user-manager-public/issues/22) 跟踪发布，继续仅采用本地门禁与公开仓库 GitHub CI 验收，不登录或修改服务器；不可变 v4.25.2 Release 保留为直接代码回退点。

## 4.25.2 - 2026-08-11

- 安装或更新现在会在建立备份、停止服务或写入系统前，实际验证 `127.0.0.1` 可绑定；本机回环地址异常时会给出明确提示并安全取消，避免停止仍可工作的连接服务。
- 新增 loopback 正常路径与失败即止测试；不自动修改网络配置，不改变菜单、状态 schema、事务顺序、协议配置或用户数据。本版本对应公开仓库 [PR #12](https://github.com/DTB201/sb-user-manager-public/pull/12)。

## 4.25.1 - 2026-08-11

- 修复不计量用户在“管理用户协议”中添加共享入口时，合法的 `metered=false` 被 `jq -e` 误判为失败并导致脚本静默退出的问题。
- 非布尔计费字段仍会在事务开始前明确拒绝；新增不计量用户成功添加入口与损坏字段拒绝测试。本版本对应 [Issue #263](https://github.com/DTB201/sb-user-manager/issues/263)。

## 4.25.0 - 2026-08-11

- 同一用户现在可受限共存一个原生 SS2022、一个既有的 `SS2022 + ShadowTLS` 旧版入口和一个 AnyTLS；三类入口继续共享同一份流量、有效期与启停状态，不允许任意重复入口，也不重新开放新增 ShadowTLS。
- 协议管理、编辑、移除、导出、服务诊断、迁移冲突检查和用户级分流改为按精确入口类型处理；双 SS2022 共存时使用互不冲突的内部标签与导出名称，移除或改密只影响所选入口。
- 状态 schema 升级到 7；schema 6 的原生或旧版用户只提升版本号，已有端口、密码、入口顺序和主入口镜像保持不变，迁移备份同步支持最多三类入口。
- 新增三入口状态、标签、导出、分流、精确编辑/删除、重复拒绝、菜单与 schema 6 升级测试；本版本对应 [Issue #262](https://github.com/DTB201/sb-user-manager/issues/262)，继续仅采用本地门禁与 GitHub CI 验收，不登录或修改服务器。

## 4.24.1 - 2026-08-11

- “管理用户协议”提升为用户管理一级入口，不再隐藏在“添加用户”的协议选择中；添加用户子菜单恢复为只负责新建单协议或双协议用户。
- 新入口继续复用原有协议增删事务：移除一个协议时保留共享账户、累计用量、月配额、有效期、启停状态和剩余协议，不改变状态 schema、迁移格式或服务器配置行为。
- 新增菜单路由与返回路径测试；本版本对应 [Issue #261](https://github.com/DTB201/sb-user-manager/issues/261)，继续仅采用本地门禁与 GitHub CI 验收，不登录或修改服务器。

## 4.24.0 - 2026-08-11

- 新增原生 SS2022 用户协议：新建单协议、双协议及为已有用户补充 SS2022 时，不再生成或要求 ShadowTLS 密码与 SNI；一个 Shadowsocks 入站在同一端口承载 TCP 和 UDP。
- 原生 SS2022 的 Surge 与 Shadowrocket 导出不含 ShadowTLS 参数；用户列表、诊断和编辑流程会明确区分原生 SS2022 与“SS2022 + ShadowTLS（旧版）”。
- 状态 schema 升级到 6；升级时所有既有 SS2022 用户明确标记为旧版 ShadowTLS 并保持原配置、密码、端口、分流和启停状态，不自动改写为原生协议。
- 既有 `SS2022 + ShadowTLS` 用户及同类分流出口继续兼容，迁移备份可在新旧状态之间安全归一化；新增测试覆盖原生配置、导出、共享配额、旧状态升级和旧版兼容。
- 本版本对应 [Issue #260](https://github.com/DTB201/sb-user-manager/issues/260)，继续仅采用本地门禁与 GitHub CI 验收，不登录或修改服务器。

## 4.23.5 - 2026-08-11

- 新增由目标脚本主动执行的管理渠道接管命令：旧私有版无需预先支持接管，即可无损切到同版本或更高版本公开版；私有仓库补齐后也可用同一机制从公开版接回同版本或更高版本私有版。
- 接管严格禁止版本或状态 schema 倒退，只接受安全的正式项目脚本、正式安装入口和版本记录；目标、安装入口、恢复目录或版本记录为符号链接、权限不安全、结构损坏或在准备期间发生变化时均失败关闭。
- 接管前持久保存旧管理脚本和版本记录，使用独立锁、持久恢复日志和同目录原子替换；写入或最终核验失败立即恢复，中途断电则在下次脚本启动时先恢复。
- 接管不修改用户、流量、有效期、证书、分流、sing-box、Nfuse 或到期定时器，也不调用服务启停命令；自动测试覆盖低版本前向接管、同版本双向重复接管、同渠道升级、拒绝路径、最终核验失败回滚和断电恢复。
- 本版本对应 [Issue #258](https://github.com/DTB201/sb-user-manager/issues/258)，继续仅采用本地门禁与 GitHub CI 验收，不登录或修改服务器。

## 4.23.4 - 2026-08-11

- 新增可手动触发的发布保护预检，只验证 `IMMUTABLE_RELEASES_READ_TOKEN` 非空且能读取仓库不可变 Release 设置，不创建标签、草稿、附件或 Release；密钥创建、轮换后必须先通过该预检再占用新版本号。
- 公开源码审计继续只允许这一种精确的 Secret 引用形式，发布工作流仍将专用只读密钥与 GitHub 自动签发的 Release 写入令牌分开使用。
- `v4.23.3` 标签的三组完整 CI 全部通过，但发布门禁发现 Actions Secret 实际为空并在创建 Release 前安全停止；本版本不移动或复用该标签，也不为其补建 Release。
- 本版本只调整发布预检、版本记录和测试，不改变菜单、运行期业务行为、状态 schema、迁移格式、服务器配置或用户数据；继续仅采用本地门禁与 GitHub CI 验收，不登录服务器。

## 4.23.3 - 2026-08-11

- 不可变 Release 保护门禁改用仓库专用的只读 Actions Secret `IMMUTABLE_RELEASES_READ_TOKEN` 查询仓库设置；密钥缺失、接口失败、响应无效或未启用时仍会在创建 Release 前安全停止。
- 专用密钥只用于读取不可变 Release 设置；草稿创建、附件上传、摘要核对和正式发布继续使用 GitHub 自动签发的短期工作流令牌，避免扩大长期密钥的使用范围。
- `v4.23.2` 标签工作流因默认令牌没有读取仓库管理设置的权限而在创建 Release 前安全停止；本版本不移动或复用该标签，也不为其补建 Release，改用新版本号重新通过完整门禁。
- 本版本只调整发布授权、测试和文档，不改变菜单、运行期业务行为、状态 schema、迁移格式、服务器配置或用户数据；继续仅采用本地门禁与 GitHub CI 验收，不登录服务器。

## 4.23.2 - 2026-08-11

- 正式发布前新增不可变 Release 保护门禁：只有 GitHub API 明确返回仓库已启用不可变 Release 时才允许创建版本；接口失败、响应无效、字段缺失或未启用都会安全停止。
- 发布工作流先完成版本、生成物和完整 CI 校验，再创建草稿、上传并核对附件摘要，最后一次性发布；已存在的标签或 Release 继续拒绝覆盖和复用。
- `v4.23.1` 创建于新门禁合并之前并显示为非不可变历史版本，本版本不会移动其标签、替换附件或复用版本号；`v4.23.2` 原计划作为首个通过新门禁的公开版本，但标签工作流在创建 Release 前安全停止，后续由更高版本重新验收。
- 本版本只调整发布安全与测试，不改变菜单、运行期业务行为、状态 schema、迁移格式、服务器配置或用户数据；继续仅采用本地门禁与 GitHub CI 验收，不登录服务器。

## 4.23.1 - 2026-08-11

- 项目以经过审计的干净快照建立受保护公开仓库 `DTB201/sb-user-manager-public`，采用 MIT License；原私有仓库 `DTB201/sb-user-manager` 保持私有和原名，继续保存完整历史及私有版、分享版双版本。
- 公开版管理脚本固定通过公开仓库的 GitHub Release 匿名更新；旧配置中的 `GITHUB_TOKEN` 只做兼容解析后丢弃，不再保存、提示或发送令牌。
- 新版本先在公开仓库通过完整本地门禁和公开 CI，再把同版本变更同步提交到私有仓库；公开 Release 只包含脚本和 SHA-256，私有 Release 继续保留双版本及两份摘要。
- 本版本不改变菜单结构、状态 schema、迁移加密格式、事务顺序或已部署服务器业务行为；不可变 v4.23.0 Release 继续作为代码回退版本。

## 4.23.0 - 2026-08-09

- 建立休眠态 v5 入口控制器与多落地安全基础：独立控制器状态、每落地秘密、固定身份传输、受限 SSH 通道、严格 apply 协议、落地 agent、持久事务恢复、启动恢复门禁、root 引导、注册、凭据初始化和可恢复 onboarding 编排。
- 增加角色识别与路由、入口依赖修复、入口角色初始化，以及由入口发起的落地依赖与 sing-box 安全准备；统一 readiness 门禁会在任何每落地秘密或受管状态落盘前完成平台、指纹、依赖和运行时核对。
- 新增能力继续保持 dormant：不开放入口数据面或多落地业务菜单，不改变现有 standalone 菜单、状态 schema、迁移加密格式、事务顺序或正式服务器可见行为；v4.22.9 作为不可变代码回退版本，本次仅采用本地门禁与 GitHub CI 验收，不登录或修改任何服务器。
- 发布准备由 [Issue #242](https://github.com/DTB201/sb-user-manager/issues/242) 跟踪；正式 Release 必须通过版本、CHANGELOG、确定性构建、Debian 12、jq 1.6、标签 CI 和四附件摘要门禁。
- v4.23.0 已由 [PR #243](https://github.com/DTB201/sb-user-manager/pull/243) 准备并正式发布：本地门禁、PR、合并后 main 与标签 CI、Debian 12、jq 1.6、不可变 Release 和四附件独立复核全部通过。私有版 SHA-256 为 `af61bebb864b7d119af04a3eec2af8d1f46ec4e8308de22a0225cff47002d9bd`，分享版为 `15b3efb5cab6a380705fee6f91c350f35e1929d2e75aecfd058351cdf5af2735`，均与标签源码确定性构建逐字节一致；本次未登录或修改任何服务器。

## 4.22.9 - 2026-08-05

- 修复状态恢复可能覆盖共享系统目录权限的问题，并保证旧格式快照在清理现有数据前已完整准备和校验；相关修复由 [PR #185](https://github.com/DTB201/sb-user-manager/pull/185) 与 [PR #187](https://github.com/DTB201/sb-user-manager/pull/187) 合并。
- 旧状态和迁移包缺少用量偏移时会安全归一化，显式无效值仍被拒绝，新迁移包则继续要求完整字段；修复由 [PR #189](https://github.com/DTB201/sb-user-manager/pull/189) 合并。
- Nfuse SQLite 快照改用数据库备份接口生成一致的独立主库，纳入 WAL 中已提交数据并执行完整性检查，同时继续兼容旧快照；修复由 [PR #191](https://github.com/DTB201/sb-user-manager/pull/191) 合并。
- sing-box、Nfuse、管理脚本及其启动副本的可执行文件安装统一为同目录临时文件加原子替换，失败时保留旧文件并拒绝符号链接目标；修复由 [PR #193](https://github.com/DTB201/sb-user-manager/pull/193) 合并。
- 本版本不改变菜单结构、状态 schema、迁移加密格式或服务器可见业务行为；v4.22.8 作为代码回退版本，仅采用本地门禁与 GitHub CI 验收，不登录或修改任何服务器，发布准备由 [Issue #194](https://github.com/DTB201/sb-user-manager/issues/194) 跟踪。
- v4.22.9 已由 [PR #195](https://github.com/DTB201/sb-user-manager/pull/195) 准备并正式发布：Pull Request、合并后主线与标签 CI、ShellCheck、Debian 12 / jq 1.6 兼容检查和不可变 Release 工作流全部通过。四个附件经独立下载复核，私有版 SHA-256 为 `2b353336a571bcf22f0338aac885a78a243417a089198f9a48e8fbc407c98bb1`，分享版为 `af98a1fb7e23a64c1710442eea9870fd1e42f9001ca905a49dd7e8f13a879fc0`，均与标签源码确定性构建逐字节一致；Release 已启用不可变保护，本次未登录或修改任何服务器。

## 4.22.8 - 2026-08-05

- 修复迁移状态备份失败时错误未能可靠传播的问题，并将恢复替换改为临时文件加原子移动。
- 移除秘密进入子进程参数的路径，限制迁移规则集为可解析的公网 HTTPS 地址，并加固备份字段校验、环境快照权限和归档成员检查。
- 修复格式化失败时可能输出完整状态文件的回退逻辑；下载、依赖安装和迁移加密路径补充失败即止、超时、协议和摘要约束。
- 本版本不改变菜单结构、状态 schema、迁移备份格式或服务器可见行为；v4.22.7 作为代码回退版本。

## 4.22.7 - 2026-08-04

- 完整环境快照不再保存 SQLite 可重建的 `nfuse.db-shm`，避免只读诊断或其他数据库连接改写该运行期文件后导致整份快照的 SHA-256 清单失效；修复由 [PR #179](https://github.com/DTB201/sb-user-manager/pull/179) 合并。
- 快照继续完整保留 `nfuse.db` 与 `nfuse.db-wal`，避免丢失尚未 checkpoint 到主数据库的已提交数据；恢复不含 SHM 的 Nfuse 快照前会清除目标机遗留的旧 SHM，由 SQLite 在服务启动时重新建立。
- 单元测试覆盖主库与 WAL 保留、SHM 排除、清单不引用 SHM，以及恢复时清除旧 SHM；本版本不改变菜单、状态 schema、迁移备份格式、事务顺序或服务可见行为，不可变 v4.22.6 Release 继续作为代码回退版本。
- 本版本按项目所有者决定仅采用本地门禁与 GitHub CI 验收，不登录或修改任何服务器；发布准备由 [Issue #180](https://github.com/DTB201/sb-user-manager/issues/180) 跟踪。
- v4.22.7 已由 [PR #181](https://github.com/DTB201/sb-user-manager/pull/181) 准备并正式发布：PR CI、合并后的 `main` CI、标签 CI、ShellCheck、Debian 12 / jq 1.6 兼容检查与不可变 Release 工作流全部通过。四个附件经独立下载复核，私有版 SHA-256 为 `a83927b8b7c9c0758f7421b859b8a6498dc7f712579059035008acf66aaf799c`，分享版为 `422b86073a6ed1e99d09ea35e84dcd7c7aa4b142be31d40c57aff585ce6cb93f`，均与标签源码确定性构建逐字节一致；Release 已启用不可变保护，本次未登录或修改任何服务器。

## 4.22.6 - 2026-07-31

- 干净服务器尚未部署时，进入用户管理或分流管理不再因缺少 `/etc/sb-user-manager.conf` 而直接退出；脚本会明确提示进入“系统管理 → 部署与卸载 → 安装或修复环境”，按回车后返回主菜单。修复由 [PR #174](https://github.com/DTB201/sb-user-manager/pull/174) 合并。
- 仅把配置路径完全不存在识别为正常未部署状态；目录、符号链接、不安全权限和损坏配置继续沿用原有严格校验，避免掩盖异常环境。
- 真实伪终端回归同时覆盖用户与分流入口、提示内容、返回主菜单、进程继续运行以及已部署菜单路径。
- 本版本不改变菜单结构、部署流程、配置格式、状态 schema、迁移包格式或已部署服务器行为；不可变 v4.22.5 Release 继续作为代码回退版本。
- v4.22.6 已由 [PR #176](https://github.com/DTB201/sb-user-manager/pull/176) 准备并正式发布：标签 CI、不可变 Release 四个附件、独立下载摘要和标签源码确定性构建比对全部通过。专用发布测试服务器 misaka 已通过 `sbm` 真实菜单从 v4.22.5 更新到 v4.22.6，sing-box 同步更新到 1.13.15；使用隔离的缺失配置路径真实验证用户与分流入口均提示尚未部署、按回车返回主菜单且进程继续运行。发布后 `release` 只读验收失败项为 0，管理状态、sing-box 配置和 Nfuse 逻辑摘要保持一致，三项服务 active/enabled、事务为空，更新前新建快照与更新自动快照均通过完整性和权限复检。上次诊断曾以 SQLite 连接打开旧快照并改写其中的 `nfuse.db-shm`，该快照未修改清单或删除，已原样移至 700 权限隔离目录；正式业务服务器未修改，继续运行 v4.22.1。

## 4.22.5 - 2026-07-29

- “添加外部备份文件”会自动发现 `/root` 顶层符合 `sb-user-data-*.sbm` 命名的普通文件，按真实修改时间从新到旧显示大小和完整状态，用户输入数字即可导入；没有候选时直接沿用原有路径输入，也可随时选择“手动输入其他路径”。实现由 [PR #169](https://github.com/DTB201/sb-user-manager/pull/169) 合并。
- 自动发现不递归扫描 `/root`、不跟随符号链接，也不会自动删除上传原文件；选中后继续复用既有结构与摘要校验、同名覆盖确认、600 权限临时文件和原子导入流程。
- 本版本不改变状态 schema、迁移包格式、HMAC 值、加密格式、恢复规则、正式备份目录或正式业务环境数据；不可变 v4.22.4 Release 继续作为代码回退版本。
- v4.22.5 已由 [PR #171](https://github.com/DTB201/sb-user-manager/pull/171) 准备并正式发布：标签 CI、不可变 Release 四个附件、独立下载摘要和标签源码确定性构建比对全部通过。专用发布测试服务器 misaka 已通过 `sbm` 真实菜单从 v4.22.4 更新到 v4.22.5，并在 `/root` 顶层真实验证自动发现、编号选择和导入有效 `.sbm`；导入前后结构、字节和 600 权限检查通过，测试原件与导入副本随后精确清理。发布后 `release` 只读验收失败项为 0，管理状态和 sing-box 配置的文件摘要保持一致；Nfuse SQLite 文件因更新过程的正常持久化和服务重启发生物理重写，但更新前快照与当前数据库的只读逻辑导出摘要完全一致。正式业务服务器未修改，继续运行 v4.22.1。

## 4.22.4 - 2026-07-26

- 为经 `run_step_or_rollback` / `run_managed_step` 执行的受管步骤建立显式失败处理不变量和静态检查，防止 Bash 在条件调用上下文中静默关闭 `set -e` 后继续执行危险操作；现有安装、状态写入和迁移恢复路径已补齐失败即止，由 [PR #160](https://github.com/DTB201/sb-user-manager/pull/160) 合并。
- 仅在线更新的旧环境在进入迁移备份创建、查看、预览或恢复前，会预检 `jq`、OpenSSL、Python 3 和 SHA-256 工具；依赖缺失时提供明确的中文修复入口，不在非交互任务中自动安装，由 [PR #162](https://github.com/DTB201/sb-user-manager/pull/162) 合并。
- 迁移备份菜单增加本地全部 `.sbm` 的批量只读体检：一次输入密码后逐份检查外层结构、密文摘要、认证、解密和 JSON 结构，并区分健康、结构异常、校验失败、密码不匹配或认证失败、载荷异常；检查不修改备份、状态、配置或服务，由 [PR #164](https://github.com/DTB201/sb-user-manager/pull/164) 合并。
- 本版本不改变状态 schema、迁移包格式、HMAC 值、加密格式、用户配置或正式业务环境数据；不可变 v4.22.3 Release 继续作为代码回退版本。
- v4.22.4 已由 [PR #166](https://github.com/DTB201/sb-user-manager/pull/166) 准备并正式发布：标签解析到通过最终 `main` CI 的提交，私有版、分享版及两份 SHA-256 文件均通过独立下载校验，且分别与标签源码和本地确定性构建一致；GitHub Release 已启用不可变保护。专用发布测试服务器 misaka 已通过 `sbm` 真实菜单从 v4.22.3 更新到 v4.22.4，更新前完整环境快照有效，已安装脚本与 Release 一致，三项服务、配置、事务、schema v4、管理锁和数据一致性的发布后 `release` 验收失败项为 0；管理状态、sing-box 配置和 Nfuse 数据的更新前后摘要一致。正式业务服务器未修改，继续运行 v4.22.1。

## 4.22.3 - 2026-07-26

- 迁移备份的 HMAC-SHA256 改用 Python 标准库计算，密钥只通过 `SB_MIGRATION_HMAC_KEY` 环境变量传入并由 `os.environ` 读取，不再进入外部进程命令行；固定测试向量确认校验结果和既有格式保持不变。Debian 安装依赖显式补充 Python 3，修复由 [PR #155](https://github.com/DTB201/sb-user-manager/pull/155) 合并。
- 分享版构建会同时剥离 `download_manager` 的定义和调用，并反向禁止任何相关字符串残留，避免引用不存在的私有函数。诊断报告对 1–3 字符用户名改用完整 token 边界脱敏，既隐藏独立用户名，也不再误改普通单词、连字符或下划线标识；修复由 [PR #156](https://github.com/DTB201/sb-user-manager/pull/156) 合并。
- 本版本不改变状态 schema、迁移包格式、HMAC 值、用户配置格式或正式业务环境数据；v4.22.2 不可变 Release 继续作为回退版本。
- v4.22.3 已由 [PR #157](https://github.com/DTB201/sb-user-manager/pull/157) 准备并正式发布：标签解析到通过最终 `main` CI 的提交，私有版、分享版及两份 SHA-256 文件均通过独立下载校验，且分别与标签源码和本地确定性构建一致；GitHub Release 已启用不可变保护。专用发布测试服务器 misaka 已通过 `sbm` 真实菜单从 v4.22.2 更新到 v4.22.3，更新前完整环境快照有效，已安装脚本与 Release 一致，三项服务、配置、事务、schema v4、管理锁和数据一致性的发布后 `release` 验收失败项为 0。正式业务服务器未修改，继续运行 v4.22.1。

## 4.22.2 - 2026-07-26

- 加固 sing-box、Nfuse 和管理脚本的下载更新链：下载、摘要校验、解包、语法检查和安装任一步失败都会立即停止；版本号、归档成员和归档属主受到限制，错误 SHA-256 不再可能继续进入安装阶段。
- 迁移备份和恢复报告改为按文件真实修改时间排序，避免名称异常时误删最新记录；状态迁移失败恢复改用同目录临时文件和原子替换，降低恢复过程中留下半写入文件的风险。
- 修复嵌套事务提前清除错误与信号回滚处理的问题；续期自动启用复用无事务内部步骤，避免嵌套启用事务，并在 SSH 连接不安全时明确报告用户没有启用。
- jq 与迁移 HMAC 使用的秘密不再放入命令行参数；解密输出使用严格权限，HMAC 校验使用常量时间比较。诊断脱敏、短用户名、菜单输入和 HUP/QUIT 中断处理同步收紧。
- 远程规则集只接受可解析到公网地址的最终 HTTPS 直链，拒绝本机、内网、凭据 URL 和任何重定向；GitHub 等跳转链接需要改用最终 `raw.githubusercontent.com` 地址。
- 分享版构建通过成对哨兵真正移除私有仓库、令牌和更新实现，并增加 `--check`、安全临时目录、符号链接保护及私有特征反向断言；CI 的 Debian 12 镜像固定到不可变摘要。
- [Issue #149](https://github.com/DTB201/sb-user-manager/issues/149) 与 [PR #150](https://github.com/DTB201/sb-user-manager/pull/150) 已完成实现。本地完整门禁、GitHub ShellCheck、Debian 12 / jq 1.6 CI 和专用发布测试服务器候选验收均已通过；实机发现的验收工具旧参数调用已修复并复测，最终生命周期与只读验收失败项均为 0，临时对象全部清理且原环境基线保持不变。
- 本版本不改变状态 schema、迁移包格式、用户配置格式或正式环境数据；已从不可变 v4.22.1 标签保存并核对本地回退脚本。发布准备由 [Issue #151](https://github.com/DTB201/sb-user-manager/issues/151) 跟踪。
- v4.22.2 已正式发布：标签解析到通过最终 `main` CI 的提交，私有版、分享版及两份 SHA-256 文件均与标签源码和本地确定性构建一致，GitHub 附件 digest 全部匹配，Release 已启用不可变保护。专用发布测试服务器 misaka 已通过 `sbm` 真实菜单从 v4.22.1 更新，操作前完整环境快照校验通过；安装脚本与 Release 一致，root 启动副本、三项服务、sing-box 配置、Nfuse、事务、schema v4、管理锁和数据一致性的发布后 `release` 验收失败项为 0。正式业务服务器未修改，继续运行 v4.22.1。

## 4.22.1 - 2026-07-26

- 修复通过 `sbm` 或 `/usr/local/sbin/sb-user-manager` 更新时，已经存在的 `/root/sb-user-manager.sh` 启动副本仍停留在旧版本的问题。
- 更新完成后会同时检查当前启动文件和已有的 root 启动副本；新版从正式安装入口首次进入菜单时还会自动补做一次检查，因此从旧版本升级也能立即修复。普通文件会使用临时文件原子同步，未创建过的 root 副本不会凭空生成。
- 如果 root 路径是指向正式安装入口的正确链接则保持不变；如果链接指向其他位置则不覆盖，并提示用户改用正式安装入口。
- misaka 从 v4.20.0 更新到 v4.22.0 后复现该问题：业务数据、服务和配置均正常，但首次 `release` 只读验收因 root 副本仍为 v4.20.0 出现 1 项失败；同步后重新验收失败项为 0。
- 自动化测试新增从正式安装入口更新时同步既有 root 副本，以及不覆盖指向其他位置链接的回归场景。
- v4.22.1 已正式发布：标签提交与 `main` 一致，私有版、分享版及两份 SHA-256 文件均通过自动核对，Release 已启用不可变保护。nube2 通过 `sbm` 真实菜单从 v4.22.0 更新，并使用临时 v4.22.0 root 副本精确复现原故障；新版首次启动自动将其同步到 v4.22.1，发布后 `release` 验收失败项为 0。测试结束后已恢复 root 副本原本不存在的状态，用户数据、配置、三项服务和事务状态均保持正常。
- 专用发布测试服务器 misaka 已在既有加密迁移备份和自动完整环境快照保护下，通过 `sbm` 真实菜单从 v4.22.0 更新到 v4.22.1。root 启动副本自动同步成功，更新前后的用户状态、Nfuse 映射和 sing-box 配置摘要一致；三项服务保持运行并已启用开机启动，发布后 `release` 验收失败项为 0，没有未完成事务、新增服务错误或临时验收文件。

## 4.22.0 - 2026-07-25

- 系统管理中的“安装或修复环境”调整为“部署与卸载”入口，在同一子菜单中提供安装、修复和完整卸载，避免增加同类一级菜单。
- 增加完整卸载：卸载前明确说明数据影响，默认引导创建新的加密迁移备份，也允许用户明确选择不创建；两条路径都可按 `0` 返回，不会因误触修改服务器。
- 完整卸载会停止并移除 sing-box、Nfuse、到期检查、用户与分流数据、运行配置、证书、管理脚本、`sbm` 快捷入口和内部回滚材料；用户主动创建或导入的 `.sbm` 迁移包继续保留，Debian 共用工具和无法确认归属的 root 文件不删除。
- 卸载复用本机代理回连 SSH 保护，并在删除前建立经过校验的临时环境快照和持久化事务。步骤失败或受控中断会恢复文件、服务和开机启动状态；全部完成后才清除临时回滚点并退出脚本。
- 部署不完整或损坏时先要求修复，避免在无法生成可信迁移备份的状态下删除残余数据。历史接管环境不会被伪装成“恢复到接管前”，确认页面会明确说明这一限制。
- nube2 已完成候选版真实菜单验收：创建加密迁移包后完整卸载，三个服务、程序、配置、状态、快捷入口和内部回滚材料均被移除，仅保留迁移包；随后从空白状态重新部署并通过真实菜单完整恢复，用户、Nfuse 账户映射和实际用量与卸载前一致，候选版只读检查失败项为 0。验收结束后已精确恢复测试前的 v4.21.1、五份完整快照和全部历史备份，三个服务及配置检查正常，候选文件和独立测试回滚点均已清理。
- 已在本地保留 v4.21.1 私有版脚本和迭代记录；开发任务见 [Issue #140](https://github.com/DTB201/sb-user-manager/issues/140)，实现由 [PR #141](https://github.com/DTB201/sb-user-manager/pull/141) 合并。
- v4.22.0 已正式发布：私有版、分享版及两份 SHA-256 文件均与标签源码和本地确定性构建一致，Release 已启用不可变保护。nube2 已通过真实菜单从 v4.21.1 更新到 v4.22.0，安装脚本与 Release 完全一致，三个服务、sing-box 配置、Nfuse、事务、数据一致性和最新完整快照的发布后 `release` 验收全部通过，失败项为 0；正式环境未修改。

## 4.21.1 - 2026-07-25

- 修复从 v4.20.4 等旧版本首次更新后，旧完整快照不会立即按新上限整理的问题。新版本安装成功、环境事务清除并切换到正式安装入口后，会执行一次安全整理并记录完成状态，无需等待下一次安装、更新或版本切换。
- 整理只在正式安装入口首次交互启动时运行；root 下的候选文件、旧启动副本、到期定时任务和后续普通启动不会重复触发。历史快照较多时先显示明确提示，避免用户误以为脚本卡住。
- 活动环境事务会阻止整理；损坏、残缺、名称异常和符号链接继续保持不动。整理或完成状态写入失败只显示警告，不改变已经成功的脚本更新，不修改用户、分流、配额、Nfuse 或 sing-box 配置，也不重启服务。
- nube2 已通过 Debian 12 候选版真实安装入口验证：首次启动把包含一份最旧测试副本在内的 6 份有效快照立即整理为 5 份，第二次启动不重复扫描；整理前后业务配置、管理状态、Nfuse 数据和服务重启次数保持一致。测试完成后已恢复不可变正式版 v4.21.0，并清除候选文件、回滚副本、测试快照和完成标记。
- 已在本地保留 v4.21.0 私有版脚本和迭代记录；开发任务见 [Issue #135](https://github.com/DTB201/sb-user-manager/issues/135)，实现由 [PR #137](https://github.com/DTB201/sb-user-manager/pull/137) 合并。
- v4.21.1 已正式发布：私有版、分享版及两份 SHA-256 文件均与标签源码和本地确定性构建一致，Release 已启用不可变保护。nube2 已通过真实菜单从 v4.21.0 更新，更新后有效完整快照保持最近 5 份，首次整理完成状态正确记录，第二次启动直接进入菜单；业务配置、管理状态、全局配置、Nfuse 数据和服务重启次数均未变化，发布后 `release` 只读验收失败项为 0。

## 4.21.0 - 2026-07-24

- 增加备份生命周期管理：用户主动创建的 `.sbm` 迁移包继续只由用户确认删除；内部操作备份按完整事务组自动保留最近 10 组，操作前完整环境快照自动保留最近 5 份，迁移恢复记录自动保留最近 20 份。
- 自动整理只在新备份、业务操作或恢复已经成功后执行。当前未完成事务引用的内部备份和完整快照始终额外保留；残缺、名称异常、结构损坏或符号链接不会被后台删除，整理失败也不改变已经成功的用户、分流或恢复结果。
- 完整环境快照不再重复复制 `/etc/sing-box/backups/` 内已经由独立策略管理的历史事务组，正式配置、用户状态、Nfuse 数据、程序、服务和自动回滚范围保持不变。
- “查看已有备份”增加迁移备份、完整回滚备份、内部操作备份和恢复记录的数量、磁盘占用及保留方式概览；创建迁移包后提醒另存到其他设备，不增加一级菜单。
- nube2 已通过 Debian 12 隔离保留测试、真实菜单验收和受控历史整理：旧版与新版事务组均可识别，活动恢复点和异常文件保持不动，真实历史备份概览约 1 秒完成；47 份有效完整快照安全保留最近 5 份，245 组内部操作备份安全保留最近 10 组。
- 真实整理前后 sing-box 配置、管理状态、全局配置和 Nfuse 账户逐项摘要一致，三项服务保持运行并继续开机启动，sing-box 与 Nfuse 没有发生额外重启，事务记录为空；独立复核确认保留内容全部有效且没有残缺文件。历史快照共享了大量相同磁盘数据，因此逐份相加的表面体积明显大于实际释放空间。
- 修正候选版 CI 对三个保留函数可选参数的 ShellCheck 警告：正式调用明确传入 10、5、20 三项既定数量，保留规则和服务器行为不变。
- 不改变迁移包格式、状态 schema、用户、分流、配额、sing-box 或 Nfuse 行为。已保留 v4.20.4 本地版本备份；开发任务见 [Issue #133](https://github.com/DTB201/sb-user-manager/issues/133)。
- v4.21.0 已正式发布：私有版、分享版及两份 SHA-256 文件均与标签源码和本地确定性构建一致，Release 已启用不可变保护；nube2 通过真实菜单从 v4.20.4 更新成功，业务数据摘要保持一致，备份保留规则最终收敛为 5 份完整快照和 10 组内部操作备份，发布后 `release` 只读验收失败项为 0。
- 从 v4.20.4 首次更新时，更新前快照由旧版进程创建，因此旧历史快照要到 v4.21.0 下一次创建完整快照时才会按新上限整理；不影响服务、数据或回滚能力。立即整理这一升级边界由 [Issue #135](https://github.com/DTB201/sb-user-manager/issues/135) 跟踪。

## 4.20.4 - 2026-07-24

- 修复从 v4.20.2 等旧版本通过真实菜单更新到 v4.20.3 后，没有自动获得 `sbm` 快捷入口的问题。根因是更新动作由旧版进程完成，旧版代码不知道新版本增加的安装步骤。
- 新版在普通交互启动时会检查已安装环境并补齐缺失的 `sbm`；已存在正确链接时不改写，同名普通文件或其他链接存在时不覆盖，只提示继续使用完整命令。
- 到期检查等内部任务不会执行快捷入口补齐；菜单、用户数据、sing-box、Nfuse 和服务运行方式保持不变。
- air 上已确认安装脚本与 v4.20.3 Release 完全一致但快捷入口缺失，并已用单个可回退链接修复，全程没有重启服务。已按迭代规范保留 v4.20.3 本地脚本备份；开发任务见 [Issue #131](https://github.com/DTB201/sb-user-manager/issues/131)。
- nube2 已按原故障条件完成候选版实机验证：从已安装 v4.20.3 环境暂时移除 `sbm` 后，普通启动 v4.20.4 会自动补齐；用户状态、sing-box 配置、服务重启次数和事务状态均保持不变。

## 4.20.3 - 2026-07-24

- 增加简短启动命令 `sbm`，固定指向正式安装入口 `/usr/local/sbin/sb-user-manager`；全新部署、环境接管和脚本更新都会自动创建或补齐快捷入口。
- 如果 `/usr/local/bin/sbm` 已被其他文件或链接占用，会在下载、备份或重启服务前停止并明确提示，不覆盖服务器原有内容。
- 环境快照、失败回滚和中断恢复已纳入快捷入口；旧版环境暂时没有 `sbm` 时仍视为正常，不会因此误报安装损坏。
- 已按迭代规范保留 v4.20.2 本地脚本备份；开发任务见 [Issue #129](https://github.com/DTB201/sb-user-manager/issues/129)。
- 本地完整门禁和 GitHub CI 全部通过；nube2 上的候选脚本与本地摘要一致，并已通过真实环境的快捷入口确认。

## 发布安全治理 - 2026-07-24

- [Issue #126](https://github.com/DTB201/sb-user-manager/issues/126) 与 [PR #127](https://github.com/DTB201/sb-user-manager/pull/127) 已完成：GitHub Release 改为先创建草稿、上传并核对私有版、分享版及两份 SHA-256 文件，全部一致后才公开；相同标签或 Release 已存在时直接失败，不再允许覆盖同版本附件。
- 草稿准备失败时只清理本次未发布内容；发布工作流测试同时通过常规 CI 和 Debian 12 / jq 1.6 兼容检查，合并后的 `main` 分支检查全部成功。
- 仓库不可变 Release 已启用并通过官方接口读回确认；保护只应用于未来发布，现有 v4.20.2 未被修改。下一正式版本继续执行完整发布验收。本项不改变管理脚本版本、菜单、配置、数据或服务器行为。

## 4.20.2 - 2026-07-22

- 将仓库源码按职责机械拆分为九个 `src/` 模块，并通过固定模块清单和 `tools/build-manager.sh` 确定性生成最终私有版单脚本；部署仍只需要一个脚本文件，菜单、配置和服务器行为不变。
- 统一两种协议新增用户时的 Nfuse 登记流程和前置冲突检查，保持计量、自用、端口、标签、证书及错误提示原有语义不变。
- 统一用户状态变化后的专属分流重建判断，以及分流事务的配置重建、检查重启和提交收尾顺序，降低以后漏改某条入口的风险。
- 统一迁移预览与真实恢复的数据准备链，确保解密、旧格式升级、完整校验和恢复计划生成使用同一实现；迁移包格式、取消路径和失败回滚不变。
- 增加以上共用流程的确定性单元测试和入口约束；模块化与维护工作分别由 [Issue #110](https://github.com/DTB201/sb-user-manager/issues/110) 至 [Issue #121](https://github.com/DTB201/sb-user-manager/issues/121) 记录，发布任务见 [Issue #123](https://github.com/DTB201/sb-user-manager/issues/123)。
- 本版本只交付已经完成的内部维护改造，不增加功能，不改变用户可见文案、状态结构、sing-box 配置、Nfuse 行为或部署文件；上一正式版 v4.20.1 已在本地保留备份。
- [PR #124](https://github.com/DTB201/sb-user-manager/pull/124) 已合并，v4.20.2 已正式发布；标签 CI、ShellCheck、Debian 12 / jq 1.6 兼容检查和 Release 作业全部通过。私有版、分享版及两份 SHA-256 文件均与标签源码和本地候选一致；nube2 通过真实菜单从 v4.19.0 更新到 v4.20.2，自动环境快照、服务、sing-box 配置、Nfuse、定时器、事务、数据一致性和发布后 `release` 只读验收全部通过，失败项为 0；正式环境未修改。

## 4.20.1 - 2026-07-20

- 修复历史手工分流与新版预置使用相同远程规则集地址时，旧规则因排序靠前而抢先命中、导致“预期出口”和“实际出口”不一致的问题。
- 脚本部署后的分流由脚本统一管理：升级检查会按规则集地址识别与当前分流重复但不在状态文件中的旧规则，事务备份后删除旧路由和旧规则集，再重建当前受管配置。
- 旧出口仅在清理后不再被其他路由、最终出口或转接引用时删除；与当前分流规则地址无关的 sing-box 基础配置保持不变。
- 服务与配置检查会把重复旧规则标记为可自动修复；如果 SSH 断联保护阻止了启动迁移，分流诊断会先说明原因并停止，避免继续显示容易误解的结果。
- 使用新的迁移指纹标记：配置本来正确的服务器只记录新标记且不重启；存在旧残留时继续使用原有事务和自动回滚机制完成一次整理。
- 已创建 [Issue #107](https://github.com/DTB201/sb-user-manager/issues/107)，保留 v4.20.0 本地脚本备份，并增加旧规则抢先、无引用旧出口清理、仍被引用出口保留和无关规则保持的回归测试。
- 本地完整门禁已通过：语法、静态、单元、交互伪终端、验收框架和分享版检查全部成功。克隆测试机已通过旧规则抢先场景的一次性整理：首次启动只重启 sing-box 一次，旧路由、旧规则集和无引用旧出口均被移除，管理状态保持不变；第二次启动不改写配置且不重启服务。测试后已用完整环境快照精确恢复原状态和原配置。
- [PR #108](https://github.com/DTB201/sb-user-manager/pull/108) 已合并，v4.20.1 已正式发布；私有版、分享版及两份 SHA-256 文件均与标签源码和本地候选一致。克隆测试机通过真实菜单从 v4.20.0 更新成功，安装入口与 root 启动副本一致，发布后 `release` 只读验收失败项为 0；正式环境未修改。

## 4.20.0 - 2026-07-20

- 多个用户选择同一条预置规则和同一个预置出口时，sing-box 运行配置不再按分流逐份复制：远程规则集和出口各只生成一次，相同“规则＋出口”的用户范围合并到一条路由中。
- 同一条预置规则可以让互不重叠的用户使用不同出口；同一用户重复添加相同路径会提示无需重复，同一用户对同一规则选择不同出口会在修改服务器前被拒绝，“全部用户”与指定用户的重叠同样会被拦截。
- 共享运行标签只在脚本内部生成，分流列表、详情和诊断继续显示用户填写的分流与预置名称；增加和编辑分流不再要求填写已经失去实际意义的每条分流出口名称。
- 停用、启用或删除某个用户或分流时会按剩余状态统一重建共享配置，不影响仍在引用同一预置的其他用户；删除预置仍保留最后一次可用快照和运行标识，不改变既有“不重启、不断开”的产品语义。
- 首次运行 v4.20.0 会在事务与 SSH 断联保护下把 v4.19.x 的重复对象整理为共享配置；成功后记录配置指纹，未变化时不重复扫描或重启。失败会恢复状态、配置和服务。
- 迁移合并会在预置自动改名后同步更新内部共享标识；故障报告继续隐藏内部规则、出口和连接标签。新增多协议用户共享、规则复用、出口复用、冲突拒绝、预置删除快照及旧配置升级回归测试。
- 修复真实菜单中重复或冲突分流被拒绝后直接退出脚本的问题；现在会说明现有配置没有改变，等待用户按回车后返回分流管理。
- 克隆测试机已通过真实菜单候选升级、不同协议用户共享同一规则和出口、重复与不同出口冲突拒绝、停用、启用、删除用户及测试数据清理；运行配置始终只保留一个规则集、一个出口和一条合并路由，最终只读验收失败项为 0。相同配置同时通过最新正式版、最新测试版及测试版整理后回到正式版的往返检查。
- 已按迭代规范保留 v4.19.1 和本次菜单修复前的本地脚本备份。
- v4.20.0 已正式发布：私有版、分享版及两份 SHA-256 文件均与标签源码和 GitHub 摘要一致；克隆测试机通过真实菜单从 v4.19.1 更新成功，首次启动完成旧重复配置共享整理，发布后 `release` 只读验收失败项为 0。共享规则、出口和路由仍各只有一份，重复启动未触发服务重启；正式环境未修改。

## 4.19.1 - 2026-07-20

- 修复“查看预置出口”和“查看预置规则”在存在预置时出现 jq 错误并直接退出脚本的问题：列表排序后改为始终从完整状态对象统计关联分流和其中启用数量。
- 列表数据读取或终端排版异常时不再泄露 jq 技术错误或退出脚本，而是显示普通用户可理解的提示并返回当前菜单。
- 增加空预置、有预置及关联分流统计、损坏数据友好提示回归测试；本修复不修改 schema、用户、分流、Nfuse 或 sing-box 运行配置，并已保留 v4.19.0 本地脚本备份。
- 本地完整门禁和克隆测试机真实菜单验收通过：两类预置列表均正确显示统计并可返回菜单，测试前后用户状态与 sing-box 配置摘要一致，已安装的 v4.19.0 未被覆盖。

## 4.19.0 - 2026-07-19

- 分流管理新增“预置出口管理”和“预置规则管理”：先保存常用出口节点和远程规则集，再在增加分流时直接选择，减少重复填写和误填。
- 预置内容仅保存时不会写入 sing-box，也不会影响现有连接；只有被分流选用后才会生效。新增分流不再提供临时手动填写入口。
- 新建分流与所选预置保持关联。修改预置会同步全部关联分流：启用中的分流通过一次受保护事务统一检查、重建和重启，停用分流只更新保存内容并保持停用；失败会恢复预置、分流和 sing-box 配置。
- 删除预置不会删除或停用分流，而是解除关联并保留分流最后一次可用的规则和出口快照，不重启服务。
- 状态结构升级到 schema 4，旧数据自动增加空预置库，旧分流保持独立配置。迁移备份包含预置及关联关系；合并时同名同内容自动复用，同名不同内容自动安全改名并修正关联，不覆盖目标服务器内容。
- 分流列表和详情会标明预置来源或“独立配置”；故障报告增加脱敏后的预置数量，并隐藏预置名称、规则地址、服务器、域名和密码。
- v4.19.0 已正式发布，关联 [Issue #98](https://github.com/DTB201/sb-user-manager/issues/98) 与 [PR #99](https://github.com/DTB201/sb-user-manager/pull/99)。专用测试机已通过 schema 3→4 升级、完整用户与分流生命周期、预置修改同步、删除解关联、运行配置保持、逐级返回、加密单文件完整恢复、智能合并恢复、预置关联和 Nfuse 用量恢复以及最终基线清理验收，失败项为 0；私有版、分享版及两份 SHA-256 附件已逐项校验，两台专用测试机的正式版安装脚本、服务、配置、Nfuse、数据一致性和最近完整快照 `release` 验收失败项均为 0。正式环境更新仍需单独授权。

## 4.18.1 - 2026-07-18

- 修复通过服务器自身代理节点连接该服务器 SSH 时，添加用户等操作重启 sing-box 并中断当前管理会话的问题。
- 脚本会用当前 SSH 源端口核对本机 TCP 套接字的实际所属进程；只有确认连接由本机 sing-box 建立时才停止操作，普通直连、非 SSH 和无法可靠判断的连接不受影响。
- 进入“添加用户”时会立即检查；其他风险连接会在事务创建和数据修改前被拒绝，并提示将该服务器的 SSH 地址设为直连后重试。重启函数保留第二道检查，防止后续新增操作遗漏前置保护。
- 不改变用户、分流、Nfuse 或备份数据格式；按迭代规则保留 v4.18.0 本地脚本备份，并补充 IPv4、IPv6、普通直连、异常会话及事务前拒绝测试。air 已使用与原故障相同的本机代理回连路径完成实机验证，脚本在进入添加用户时正确停止，SSH 未断开且服务器数据未修改。
- v4.18.1 已正式发布：私有版、分享版及两个 SHA-256 文件均已逐项校验；nube2 通过真实菜单从 v4.18.0 更新成功，安装脚本与 Release 完全一致，服务、配置、Nfuse、事务、数据一致性和最近完整快照的 `release` 验收失败项为 0。

## 4.18.0 - 2026-07-17

- Shadowrocket 导出由文本配置行改为客户端可直接导入的 URL，并在交互终端显示内容完全一致的二维码；覆盖 AnyTLS 与 SS2022 + ShadowTLS v3。
- AnyTLS URL 按 Shadowrocket 当前正式版样本包含 `peer`、`insecure=1` 和 `udp=1`；SS2022 + ShadowTLS 使用整段 Base64 凭据及 `shadow-tls` v3 JSON 参数，名称和特殊字符执行 URL 安全编码。
- 二维码只在服务器本地通过 Debian `qrencode` 生成，不把节点地址或密码发送给第三方；新部署自动安装，旧服务器首次使用时由用户确认安装，取消或失败仍保留完整 URL。
- 二维码保持标准终端 UTF-8 渲染，不加入依赖特定 SSH 客户端的图片协议、紧凑字符方案或缩放提示，以优先保证跨终端显示一致性。
- 补充 URL 结构、Base64 内容、ShadowTLS JSON、特殊字符编码、端口覆盖、二维码内容一致性及旧文本格式移除测试；AnyTLS 与 SS2022 + ShadowTLS 已在当前正式版 Shadowrocket 完成真实扫码、TCP 和 UDP 验收，按迭代规则保留 4.17.5 及本次修正前的本地脚本备份。

## 4.17.5 - 2026-07-17

- 修复历史完整环境快照较多时进入脚本变慢：成功检查后记录备份根目录状态，目录没有变化时不再重复遍历全部历史快照。
- 新增或删除快照、标记缺失或损坏时会自动重新执行权限检查；标记文件采用原子写入、最小权限并拒绝符号链接，不改变新快照权限和历史快照修复规则。
- 补充首次检查、重复启动、目录变化、损坏标记、符号链接和无备份目录回归测试；按迭代规则保留 4.17.4 本地脚本备份。

## 待发布

- 仓库源码按职责机械拆分到 `src/`，由固定模块清单和 `tools/build-manager.sh` 确定性生成最终私有版单脚本；CI 会拒绝源码与生成物不一致。本项不修改 v4.20.1 的任何字节，不改变版本、功能、菜单、配置、部署文件或服务器行为；服务器运行范围明确为 Debian 12 x86_64。[Issue #110](https://github.com/DTB201/sb-user-manager/issues/110) 与 [PR #111](https://github.com/DTB201/sb-user-manager/pull/111) 已完成，完整本地门禁、GitHub CI 和 Debian 实机构建及只读验收均通过。
- 记录首次正式上线完成（Issue #96）：v4.18.1 通过固定 Release 全新部署、真实 AnyTLS 与 SS2022 + ShadowTLS TCP/UDP 和用量统计验收，以及上线后 30 分钟与 24 小时观察；最终 `release` 只读验收失败项为 0，项目进入上线后维护阶段。本项只同步长期项目状态，不改变脚本版本或服务器行为。
- 完成阶段 1 退出评估（Issue #94）：适用的基线、生命周期、迁移恢复、菜单更新、发布和真实客户端验收均已有证据，当前没有未解决的 P0/P1 问题；项目进入阶段 2 准备状态，但首次正式上线仍需具体服务器准备和项目所有者独立授权。
- 建立产品方向、架构、开发、发布和安全设计文档，明确项目所有者、AI 产品与技术负责人和 Codex 执行代理的职责边界。
- 新增产品需求、问题报告和 Pull Request 模板，让没有编程经验的项目所有者可以用普通语言提出需求并完成验收。
- 新增安全报告与参与开发说明；本次仅完善项目治理，不改变脚本版本、运行行为、数据格式或部署文件。
- 新增项目状态、路线图、待办和部署回退文档，把当前阶段、上线条件和后续优先级从聊天记录迁移到仓库。
- 新增决策记录 0002，由 AI 承担日常产品与技术领导，项目所有者保留最终业务验收、正式上线和高风险授权；历史决定 0001 保留并标记为已被取代。
- 同步阶段 1 的实际验收进度：v4.17.0 `audit` 基线和用户生命周期已经通过，当前唯一任务更新为分流生命周期验收 Issue #52；本次不改变脚本或服务器行为。
- 记录 v4.17.0 分流生命周期实机验收通过：两条临时分流的新增、编辑、排序、停启用、删除、残留清理和最终 `audit` 全部成功，原有数据恢复验收前基线；下一项转为准备迁移备份与恢复验收。
- 验收工具生成的 `audit`、`lifecycle` 和 `full` JSON 报告不再写入系统原始主机名；保留兼容字段并固定为 `[redacted]`，不改变正式管理脚本版本或服务器运行行为。
- 记录 v4.17.0 迁移备份与恢复实机验收通过：加密单文件服务器外保存与重新导入、错误密码和损坏副本拒绝、完全恢复、合并恢复、流量记录、外层快照字节级回退及最终 `audit` 全部成功，临时对象和文件已清理。
- 记录 v4.16.0 通过真实菜单更新到 v4.17.0 的实机验收通过：取消更新不修改数据，正式更新保持用户、分流、配置与 Nfuse 用量，Release 校验、启动副本同步、新进程接管、服务状态、菜单提示和最终 `audit` 全部成功；测试结束后恢复空环境并清理临时凭据与文件。
- 新增面向非技术项目所有者的首次正式上线检查单：以可勾选方式覆盖 Release 与服务器确认、备份和回退、停止条件、部署过程、真实客户端验证、上线后 30 分钟与 24 小时观察及脱敏记录；检查单完成不等于正式上线授权。
- 记录 v4.17.1 正式发布完成：私有版、分享版及校验文件均已独立复核；克隆测试机通过真实菜单从 v4.17.0 更新成功，版本、服务、配置、Nfuse、事务和数据一致性只读验收失败项为 0。
- 记录 v4.17.2 正式发布完成：私有版、分享版及校验文件均已独立复核；克隆测试机通过真实菜单从 v4.17.1 更新成功，返回文案、提示词、服务与数据一致性验收通过，失败项为 0；更新前快照目录和清单权限符合 4.17.1 的安全要求。
- 验收工具新增只读 `release` 模式：复用 `audit` 并自动核对精确 GitHub Release、对应脚本附件与 SHA-256、服务器已安装脚本，以及最近完整环境快照的校验清单和权限；统一写入脱敏 JSON 报告。克隆测试机对 v4.17.2 的真实验收失败项为 0，业务状态与 sing-box 配置前后未变化；本项不改变管理脚本版本或服务器业务行为。
- 记录 v4.17.3 正式发布完成：私有版、分享版及两个 SHA-256 文件均已逐项下载校验；nube2 通过真实菜单从 v4.17.2 更新成功，管理脚本、服务、配置、Nfuse、数据一致性和最新完整快照的 `release` 验收失败项为 0。
- 记录 v4.17.4 正式发布完成：私有版、分享版及两个 SHA-256 文件的 GitHub 摘要与本地构建逐项一致；nube2 通过真实菜单从 v4.17.3 更新成功，管理脚本、服务、配置、Nfuse、数据一致性和最新完整快照的 `release` 验收失败项为 0，Surge 与 Shadowrocket 的 UDP 实机验证均通过。
- 记录 v4.18.0 正式发布完成：nube2 通过真实菜单从 v4.17.4 更新成功；私有版和分享版 Release 附件均与 GitHub SHA-256 摘要及本地构建一致，脚本版本、服务、配置、Nfuse、数据一致性和最新完整快照验收失败项均为 0。

## 4.17.4 - 2026-07-17

- 修复 SS2022 + ShadowTLS 节点在 Surge 中无法使用 UDP：服务端在原公网端口增加独立的 SS2022 UDP 入站，TCP 仍由 ShadowTLS v3 承载，不增加额外端口或用户输入项。
- Surge 导出配置新增 `udp-relay=true`；同一数字端口分别承载 TCP 与 UDP，因此不生成多余的 `udp-port`。
- 新 UDP 入站纳入用户新增、编辑、停启用、删除、默认域名重建、分流、诊断、迁移恢复和一致性修复；旧版启用中的 SS2022 用户首次运行新版时会在事务保护下自动补齐，失败自动回滚。
- 补充三入站结构、旧配置升级、同端口导出、标签生命周期和真实 UDP 监听验收；按迭代规则保留 4.17.3 本地脚本备份。

## 4.17.3 - 2026-07-16

- 脚本启动时自动收紧历史完整环境快照权限：备份根目录、快照目录和数据目录统一为 `700`，格式与校验清单文件统一为 `600`。
- 权限迁移只识别结构完整且版本受支持的真实目录，不跟随符号链接、不修改无关目录，也不读取、重写或删除快照数据；重复运行不会反复修改已经正确的权限。
- 权限迁移失败时保留管理菜单可用并给出普通用户可理解的检查入口；补充正常迁移、重复运行、无关目录、符号链接和异常路径回归测试。
- 按迭代规则保留 4.17.2 本地脚本备份。

## 4.17.2 - 2026-07-16

- 修正系统管理下各级子菜单的返回文案：“检查与故障报告”“数据备份与恢复”“默认连接域名”和“sing-box 版本管理”统一显示“返回上一级”，与按 `0` 后实际返回系统管理的行为一致。
- 安装或修复环境中的取消选项同步改为“返回上一级”；主菜单直属的用户、分流和系统管理仍显示“返回主菜单”，不改变菜单编号、返回层级或业务功能。
- 增加真实伪终端和菜单函数回归测试，验证诊断菜单文案、返回系统管理及返回后的“请选择：”提示；按迭代规则保留 4.17.1 本地脚本备份。

## 4.17.1 - 2026-07-16

- 修复完整环境快照最外层目录可能继承为 `755` 的问题：备份根目录、每次快照目录和数据目录现在都明确使用 `700`，格式与校验清单文件使用 `600`。
- 快照创建完成后会立即复核关键权限；权限不符合要求时删除不完整快照并停止后续部署，避免把可被其他本机用户查看的备份当作有效回滚点。
- 旧版快照继续按原格式恢复，不因历史最外层权限为 `755` 而失去兼容性；补充新快照权限、错误权限拒绝和旧快照恢复兼容回归测试。
- 按迭代规则保留 4.17.0 本地脚本备份。

## 4.17.0 - 2026-07-16

- 建立统一终端界面组件：标题、上下文、副标题、分隔线、双列/单列菜单和返回区使用一致的层级与间距。
- 主菜单、用户、分流、备份和默认连接域名菜单按任务分组；危险操作独立呈现，减少连续编号清单的扫描负担。
- 双列菜单改为按终端实际显示列宽对齐中文，统一左右边界，并收紧分组、菜单项和返回区间距；72 列以下自动使用单列布局。
- 主菜单按职责分为“用户管理”“分流管理”“系统管理”三个板块；用户与分流操作保留在对应板块，运行状态、故障报告、备份、基础配置和版本维护统一归入系统管理。
- 交互终端使用克制的语义色，非交互输出、哑终端及 `NO_COLOR` 环境自动保持纯文本；窄终端自动切换单列显示。
- 每个板块内部都从 `1` 按实际显示顺序自动编号，统一显示为 `1`、`2`、`3`……且不补零，返回统一显示为 `0`；不改变业务流程、配置格式或服务行为。

## 4.16.0 - 2026-07-15

- 统一主菜单和全部二级菜单的输入处理：输入文字、空值或超出范围的编号时，会在当前位置说明可选范围并等待重新输入，不再清屏、延迟一秒或退出脚本。
- 用户添加、编辑、续期、流量调整和配置导出改为原地校验；用户名、端口、协议、加密方式、SNI、账单日、有效期或列表编号填写错误时可直接修正，并在各层保留清晰的 `0` 返回入口。
- 分流新增、编辑、排序、启停和验证流程统一使用可重试的编号与字段校验；规则集地址、适用用户、出口名称、服务器、端口、密码和协议参数填写错误时不会中断整个脚本。
- 迁移包导入、选择、解密、恢复方式、记录选择及清理数量支持错误重试；密码错误会停留在解密流程，用户可重新输入或按 `0` 安全取消。
- 输入流关闭时会正常退出当前菜单，Nfuse 预检查在隔离或未就绪环境中不再输出无关的程序路径错误；补充公共选择器和真实伪终端的无效输入回归测试，按迭代规则保留 4.15.4 本地备份。
- 校验错误捕获兼容 `bash -x` 调试模式，调试轨迹不会混入面向用户的错误提示；Debian 12/jq 1.6 发布门禁会同时覆盖这一行为。

## 4.15.4 - 2026-07-15

- 管理配置改为先验证普通文件、禁止符号链接、检查所有者与访问权限，再按允许的配置项逐行解析；不再把配置文件作为 root shell 脚本执行。
- 配置中出现未知项目、重复项目、命令式写法或不完整引号时会在执行任何内容前安全拒绝，并显示具体行号，避免损坏或被篡改的配置影响服务器。
- 保留旧版配置迁移所需字段及直接编辑常用配置项的兼容性；补充配置权限、符号链接、恶意内容、未知与重复配置项回归测试。
- 固定 GitHub Actions、构建系统和 Debian 12 验证环境版本，减少同一版本因外部构建环境变化产生不同结果；按迭代规则保留 4.15.3 本地备份。

## 4.15.3 - 2026-07-15

- 主界面在版本号旁明确显示“私有版”或“分享版”，方便小范围分享后识别当前运行版本；故障诊断报告保持原有内容，不增加版本类型字段。
- 分享版仍然只移除管理脚本自更新能力，sing-box、Nfuse、用户、分流、备份恢复及其他功能不变。
- 补充双版本标识自动生成与回归检查；按迭代规则保留 4.15.2 本地备份。

## 4.15.2 - 2026-07-15

- 新增与私有版同步生成的公开版脚本；公开版仅在“检测更新”中移除管理脚本检查、GitHub Token 提示和管理脚本在线更新，sing-box、Nfuse 及其他管理功能保持一致。
- GitHub 自动发布现在同时测试并上传私有版、公开版及各自 SHA-256 校验文件，公开版由私有主脚本自动生成，避免后续迭代时两份脚本内容不同步。
- 按迭代规则保留 4.15.1 本地备份。

## 4.15.1 - 2026-07-15

- 修复诊断报告新增代码触发的 ShellCheck 数组下标、条件表达式和未使用变量告警，使 GitHub CI 能继续执行完整测试和自动发布附件。
- 不改变诊断报告内容及服务器运行行为；按迭代规则保留 4.15.0 本地备份。

## 4.15.0 - 2026-07-15

- 新增“检查与故障报告”菜单，可一键生成适合交给维护者查看的纯文本诊断报告；生成过程只读取服务器状态，不修改配置、不重启服务。
- 报告集中显示管理脚本、sing-box、Nfuse 与当前 sing-box 通道版本，三项服务状态，配置和流量统计检查结果，未完成操作、用户与分流数量，以及服务与配置一致性结论。
- 收集 sing-box、Nfuse 和到期检查最近 24 小时内最多 30 条警告；自动隐藏用户、分流、出口、上游服务器、规则集地址、密码、令牌、主机名、IP 地址和日志中的域名。
- 历史警告会保留 `sing-box.service` 等 systemd 单元名称，并明确说明“总体结果正常”时不代表历史问题仍在发生，避免过度脱敏造成误解。
- 诊断报告使用 root 专用目录和 `600` 权限保存；菜单支持列出、查看、删除及按保留数量清理旧报告，异常或伪造文件不会作为有效报告直接展示。
- 补充报告生成、故障状态、脱敏、文件权限、列表管理、清理和真实伪终端菜单回归测试；按迭代规则保留 4.14.0 本地备份。

## 4.14.0 - 2026-07-15

- 分流管理新增“验证分流是否生效”：选择启用中的分流和测试用户后，脚本使用 systemd 日志游标只读取本次操作期间新增的 sing-box 连接记录，不修改配置、不重启服务。
- 诊断结果按目标地址整理为“命中当前分流、走直连、命中其他分流、使用其他出口或未看到出口记录”，并显示实际出口名称；最多展示最近 20 条连接，避免直接输出杂乱的完整技术日志。
- 兼容 sing-box 写入 systemd journal 的彩色日志控制符，解析前会清理颜色信息，确保连接编号、入站用户和实际出口能够在真实服务器日志中正确关联。
- 兼容 AnyTLS 等协议在一个长期会话编号中连续承载多个目标连接的情况，按每次入站事件与随后出口事件排队匹配，不会因会话复用而遗漏后续域名。
- 全部用户分流可从启用用户列表选择测试对象，指定用户分流自动使用关联用户；停用用户、日志级别不足、没有新连接或日志服务异常时给出面向普通用户的处理提示。
- 补充分流日志连接匹配、结果分类和交互菜单回归测试；按迭代规则保留 4.13.1 本地备份。
- 修复发布检查发现的未使用日志字段告警，不改变分流诊断的日志格式与实际判断结果。

## 4.13.1 - 2026-07-15

- 修复云服务器位于 NAT 网络时，导出的用户配置错误使用网卡内网 IPv4 的严重问题；只有默认路由源地址本身是公网地址时才直接使用。
- 默认路由为私网地址时，改为通过多个 HTTPS IPv4 查询服务识别公网地址，并严格拒绝私网、共享地址、链路本地、文档示例及其他保留地址；无法可靠识别时停止导出并给出明确提示，不再生成不可用配置。
- 新增可选的 `PUBLIC_SERVER_OVERRIDE` 公网 IPv4 覆盖项，供多重 NAT、独立映射地址或特殊出口网络手动指定；补充公网、私网、查询回退和手动覆盖的回归测试，并保留 4.13.0 本地备份。

## 4.13.0 - 2026-07-15

- 自用用户现在也会接入 Nfuse，使用原生不限额统计类型：不设置月配额、不设有效期、不会因用量增加而阻断，只记录累计流量；SS2022 + ShadowTLS 与 AnyTLS 行为一致。
- 新增自用用户时自动创建不限额流量记录并绑定端口；4.12.x 及更早版本创建的自用用户首次运行新版时，在没有同名记录或端口冲突的情况下自动补齐，遇到冲突只提示检查而不覆盖原数据。
- “查看用户”中自用用户的月配额改为“不限”，同时显示累计已用流量；停用、重新启用和编辑端口均保留累计用量，且自用用户仍不参与续期、到期停用和流量配额调整。
- 编辑用户端口改用 Nfuse 原地编辑端口记录，避免删除后重建导致自用用户累计值丢失；启用用户和服务一致性检查会同时验证计费用户与自用用户的统计类型及端口绑定。
- 迁移包继续使用数据格式 1 和状态格式 3，新增可选的累计用量衔接字段，保持旧单文件迁移包可恢复；合并恢复、完全恢复及未完成操作自动回滚后，自用用户的历史用量会与新计数连续相加。
- 补充自用用户创建、启用不清零、旧用户自动接入、累计显示、迁移恢复、断电回滚及一致性检查测试；按迭代规则保留 4.12.0 本地备份。
- 修正静态测试对受管操作数量的旧预期，并改为显式输出错误，确保 macOS 旧版 Bash 与 GitHub Actions 新版 Bash 的发布门禁行为一致。

## 4.12.0 - 2026-07-15

- 启动 sing-box 正式版与测试版安全切换功能的第一阶段；主菜单新增“sing-box 版本管理”，当前仅查询和检查，不会替换正在运行的程序。
- 直接读取 sing-box 官方 GitHub Release：分别显示当前版本、最新正式版和最新测试版；测试版只接受 `draft=false`、`prerelease=true` 的最新发行。
- 正式版和测试版均只接受官方 `linux-amd64` 压缩包，并要求 GitHub Release 提供有效的 SHA-256 digest；下载后先验证校验值和压缩包内版本。
- 新增目标版本兼容检查：使用下载到临时目录的 sing-box 检查现有完整配置，并逐一编译或反编译现有及已停用分流引用的远程规则集。
- 针对 sing-box 测试版经常调整配置规范的情况，测试版检查会在临时目录模拟目标版本的配置格式化结果，再交给最新正式版反向检查；测试版虽能读取当前配置、但处理后无法回到正式版时，明确判定为不满足安全往返要求。
- 检查通过或失败都不会修改 `/usr/local/bin/sing-box`、用户、分流、Nfuse 数据或服务状态；失败时用普通用户可理解的方式说明配置或规则集问题。
- 抽取可指定 sing-box 二进制的规则集检查函数，由新增/编辑分流和版本兼容检查共用，避免维护两套判断逻辑。
- 补充 Release 元数据、通道识别、digest 拒绝、规则集去重及主菜单入口测试；按迭代规则在本地保存 4.11.1 脚本备份。
- 第二阶段开放正式版与测试版真实切换，以及“更新当前通道”；进入测试版前需先通过双向兼容检查并输入 `TEST`，回到正式版需通过目标配置检查并输入 `SWITCH`。
- 每次切换独立保存当前通道、目标通道和切换前二进制，通道状态记录当前版本、已缓存版本和上一版本；版本文件同步更新实际运行版本。
- 切换复用完整环境快照和持久化环境事务：替换前再次检查配置，替换后重启并检查 sing-box、Nfuse 和到期定时器；下载、写入、配置、服务或状态保存失败时恢复原二进制及数据，异常中断后下次启动自动恢复。
- 测试通道中的普通“检测更新”不再把 sing-box 静默替换为正式版；该入口继续更新 Nfuse 和管理脚本，sing-box 则明确引导到“版本管理 → 更新当前通道”。
- 补充真实切换状态、三份二进制留存和版本文件同步测试；按阶段迭代要求额外保存 4.12.0 第一阶段候选备份。

## 4.11.1 - 2026-07-15

- 修复创建迁移备份时，密码为空、少于 8 位或两次输入不一致会直接退出整个脚本的问题。
- 密码输入改为原地循环：输入不符合要求时给出简短原因并允许立即重输；在第一次或确认密码时输入 `0` 均可取消并直接返回“数据备份与恢复”。
- 取消流程会清理本次生成的临时迁移数据，不创建不完整备份，也不显示多余的“按回车返回菜单”；按迭代规则在本地保存 4.11.0 脚本备份。
- 将 Bash 文案中的中文弯引号调整为直观的中文书名式括号，并清理文案重构后不再使用的变量，保持界面可读性的同时通过 ShellCheck 严格检查。

## 4.11.0 - 2026-07-15

- 迁移恢复新增“智能合并”模式并设为默认选项；无冲突的用户和分流直接导入，目标服务器已有且未被替换的数据全部保留，原“完全替换”能力继续作为“覆盖恢复”提供。
- 同名用户和分流采用面向普通用户的逐项处理界面，默认保留本机项目；也可选择使用备份覆盖，或作为新项目导入并自行填写名称、端口和分流内部标识，任意冲突阶段均可输入 `0` 取消且不修改服务器。
- 用户改名导入时同步改写备份分流的关联用户名并迁移对应 Nfuse 已用流量；跳过不存在的关联用户时安全跳过对应分流，避免生成失效规则。
- 合并计划完全在临时文件中生成，确认前展示新增、覆盖、改名、跳过数量以及最终差异；智能合并使用 `MERGE` 二次确认，覆盖恢复继续使用 `RESTORE`。
- 合并和覆盖恢复共用完整环境快照、持久化事务、sing-box 校验、Nfuse 用量恢复、一致性审计与失败整体回滚链；恢复报告新增恢复方式和合并处理摘要，旧报告继续兼容。
- 新迁移包仅保存脚本状态中实际存在的用户对应 Nfuse 账户，恢复时过滤无关外部账户；补充 ShadowTLS 分流传输标签的外部冲突检查。
- 自动化测试新增智能合并、默认保留、覆盖、用户/分流改名、关联映射、取消无副作用、完整恢复链和 Nfuse 用量覆盖；`full` 实机验收扩展为依次验证覆盖恢复与智能合并恢复。
- README 新增面向非专业用户的恢复方式选择、冲突处理、确认和回滚说明；按迭代规则在本地保存 4.10.1 脚本备份。
- 全面审查主菜单、用户、分流、安装、状态检查、迁移恢复及 README 文案：以“操作结果和下一步”为主，将入站、出站、受管、事务、快照、Socket 等内部术语改为普通用户可理解的连接配置、出口、自动还原、完整备份和服务通信；协议名、SNI、端口及规则集格式等必要概念继续保留。
- 菜单和表格保持短句与对齐布局，详细解释只在首次输入、危险确认和错误场景出现；新增静态检查，禁止 Shell 变量后直接紧跟非 ASCII 文本，避免中文标点在不同 Bash 环境中被误解析。

## 4.10.1 - 2026-07-15

- “增加分流”入口现在明确提示可输入 `0` 返回分流管理；在第一个分流名称提示处输入 `0` 会立即返回，不再要求继续填写规则集和上游信息。
- 返回后直接恢复分流管理菜单，不显示多余的“按回车返回菜单”暂停提示。
- 新增输入流程单元测试及真实伪终端菜单回归测试，覆盖误触“增加分流”后的无副作用返回和菜单提示恢复。
- 按迭代规则在本地保存 4.10.0 脚本备份。

## 4.10.0 - 2026-07-15

- 分流管理新增“查看分流详情”，显示优先级、完整规则集地址、作用范围、上游参数及创建/修改时间，但不显示任何协议密码。
- 新增事务化编辑分流，可修改远程规则集地址、作用范围、上游协议与连接参数及出站名称；保持同一协议时，密码留空可沿用原值，分流名称和规则集标签保持稳定。
- 新增分流优先级调整，以状态文件数组顺序作为受管路由的唯一优先级来源；分流列表显示实际优先级，启停、编辑、增加和重排都会按该顺序重建全部启用分流。
- 统一重建过程只移除脚本明确管理的规则集、路由和出站，继续保留外部 sing-box 配置；停用分流保留其顺序，重新启用后回到原优先级位置。
- 新增保存前远程规则集验证：HTTPS `.json` 由当前 sing-box 编译校验，`.srs` 由当前 sing-box 反编译校验；下载失败、格式不兼容、配置检查或服务重启失败均拒绝生效或由持久化事务整体回滚。
- 抽取新增与编辑共用的分流上游输入流程，详情和预览均脱敏；补充分流格式验证、顺序重建、外部配置保留、编辑、优先级、脱敏和失败回滚测试。
- 按迭代规则在本地保存 4.9.1 脚本与迭代记录备份。

## 4.9.1 - 2026-07-15

- 添加用户的“使用方式”现在默认选择计量节点，直接回车等同于选择 `1`；仍可输入 `2` 选择自用节点或输入 `0` 返回用户管理。
- 交互菜单明确标注计量节点为默认选项，并将提示调整为“请选择使用方式 [1]”。
- 新增直接回车进入计量用户流程的单元测试，并保留显式选择自用节点的既有覆盖。
- 按迭代规则在本地保存 4.9.0 脚本与迭代记录备份。

## 4.9.0 - 2026-07-15

- 管理配置新增两项全局 SNI：SS2022 + ShadowTLS 默认 `publicassets.cdn-apple.com`，AnyTLS 默认 `weKbP9SVYU.download.windowsupdate.com`；全新部署自动写入，不在安装过程中询问。
- 新增用户时仍可自行填写协议 SNI，直接回车则使用对应全局默认值；每个用户继续保存实际 SNI，保证迁移包和单用户导出自包含。
- 主菜单新增“SNI 管理”，可查看两项全局值及既有用户不一致数量，并事务化修改全局值、批量同步对应协议的全部用户。
- 修改 SS2022 + ShadowTLS 全局 SNI 时重建启用用户入站并校验、重启 sing-box；修改 AnyTLS 全局 SNI 后立即影响既有用户导出，协议密码均保持不变。
- 4.8.x 及更早配置首次运行时自动补入两项默认值并保留原配置备份，不自动覆盖既有用户 SNI；只有使用者确认全局修改后才批量同步。
- 持久化运行事务新增管理配置快照、同步落盘和自动恢复，确保全局 SNI、用户状态及 sing-box 配置在失败或断电后一起回滚。
- 新增全局配置迁移、新增用户默认值、批量同步、配置重建、交互返回和管理配置断电恢复测试。
- 按迭代规则在本地保存 4.8.0 脚本与迭代记录备份。

## 4.8.0 - 2026-07-15

- 用户管理新增“编辑用户”，按排序后的编号选择用户，可修改端口、协议对应的 SNI；SS2022 用户还可切换 AES-128/AES-256 加密方式，计量用户可修改账单日并按月重新设置有效期。
- 编辑前集中展示字段差异，任意输入阶段均可取消；停用用户编辑后保持停用，不会意外恢复入站。
- 端口修改继续限制在 `20001–30000`，支持随机选择空闲端口；仅保留历史迁移端口而不修改时仍兼容 `1–65535`。
- 编辑操作统一纳入持久化事务，同时更新管理状态、sing-box 入站和 Nfuse 端口/账单日；失败、中断或重启后按编辑前快照整体恢复。
- SS2022 加密方式变化时重新生成符合密钥长度要求的 SS2022 密钥，保留 ShadowTLS 密钥，并明确提示旧客户端配置失效；普通端口、SNI、账单日和有效期修改不更换凭据。
- 新增编辑用户的配置替换、Nfuse 联动、停用用户、冲突拒绝和失败回滚测试。
- 按迭代规则在本地保存 4.7.2 脚本与迭代记录备份。

## 4.7.2 - 2026-07-15

- 抽取安装部署、在线更新与现有环境接管共用的默认网卡识别、AnyTLS 证书准备、管理脚本安装、版本记录写入及 systemd 服务激活流程，删除三条路径中的重复实现。
- 统一部署与接管失败后的环境快照恢复、恢复日志清理和临时目录收尾；各流程仍保留自身的新建路径清理以及全新部署覆盖策略。
- 修复在线更新通过条件表达式调用部署函数时，Bash 可能抑制函数内部 `ERR` 陷阱并在步骤失败后继续执行的问题；部署与接管的所有关键步骤现在都显式调用公共失败回滚执行器。
- 统一成功提交环境事务的清理顺序，集中清除恢复日志、错误与信号陷阱及临时下载目录；菜单、安装结果、外部配置接管策略和在线更新行为保持不变。
- 新增公共环境流程单元测试，覆盖本地/在线管理脚本安装、证书复用与生成、版本文件、服务启动顺序、失败恢复及成功收尾。
- 按迭代规则在本地保存 4.7.1 脚本与迭代记录备份。

## 4.7.1 - 2026-07-15

- 新增统一的事务步骤执行器，集中处理命令执行、原始错误码捕获和失败回滚，替代用户、流量、到期、分流及一致性修复流程中散落的条件回滚样板。
- 用户与分流事务中的配置、状态、Nfuse 及服务校验步骤全部显式经过公共执行器，不再混用隐式 `ERR` 陷阱和各自实现的步骤包装函数；菜单、协议、状态结构和部署格式保持不变。
- 新增公共执行器回归测试，验证成功时不回滚、失败时只回滚一次并保留原始错误码；静态检查要求事务操作持续使用公共入口。
- 按迭代规则在本地保存 4.7.0 脚本与迭代记录备份。

## 4.7.0 - 2026-07-15

- 新增持久化运行事务日志：用户增删、停用/启用、续期、临时/永久流量调整、到期自动停用、分流增删与状态切换、一致性修复和迁移恢复在修改前统一保存 sing-box 配置、管理状态和 Nfuse 实时账户快照。
- 事务日志与三份快照完成落盘后才开始修改；成功提交前强制持久化 Nfuse 并同步配置、状态和数据库。若进程被 `SIGKILL`、SSH 异常断开或主机重启，下一次启动会在显示菜单或执行到期任务前自动恢复。
- Nfuse 恢复按事务前快照重建受管账户的层级、端口范围、配额和已用流量，同时删除中断事务新建的受管账户；不属于管理状态的外部 Nfuse 账户保持不变。
- 安装部署、在线更新和现有环境接管新增独立的环境恢复日志，记录完整环境快照及本次新建路径；异常重启后会先清理本次新增对象并恢复快照，再允许继续使用脚本。
- 运行事务支持嵌套调用，续期自动启用和迁移内一致性重建只提交最外层事务；日志损坏、快照不完整或恢复失败时会保留证据并拒绝继续修改。
- 实机验收新增未完成事务日志检查；单元测试新增运行事务断电模拟、Nfuse 精确恢复、外部账户隔离、嵌套提交和环境事务异常恢复覆盖。
- 按迭代规则在本地保存 4.6.11 脚本与迭代记录备份。

## 4.6.11 - 2026-07-15

- 用户停用与启用改为仅列出符合当前状态的用户，按用户名排序并使用编号选择；选择 `0` 可直接返回用户管理，不再要求手工输入用户名。
- 修复检测更新调用部署失败时，在部分受控调用上下文中可能继续执行并掩盖失败状态的问题；现在会显式向上返回失败。
- 安装修复流程改用统一的系统根路径解析，并新增隔离流程回归，覆盖全新部署确认、部分/损坏部署修复与覆盖、外部配置接管、危险状态缺失拒绝和更新失败传播。
- 伪终端测试新增用户停用、启用直接返回路径；README 明确正式实机支持范围为 Debian 12 x86_64，并说明 Debian/Ubuntu、ARM64 与 IPv6-only 的边界。
- 按迭代规则在本地保存 4.6.10 脚本与迭代记录备份。

## 4.6.10 - 2026-07-15

- 修复 Debian 12 自带 jq 1.6 将迁移差异预览中的 `$label` 识别为关键字、导致单文件恢复在正式事务开始前失败的问题；改用兼容变量名，并让新增、删除、替换行分别稳定输出 TSV。
- 部署、现有环境接管和环境快照恢复在启动 Nfuse 后会等待 Socket 与 JSON 接口实际就绪，避免 systemd 已显示 active、但 Nfuse 尚未完成监听时的瞬时误判。
- `full` 验收若在恢复单文件迁移备份步骤失败，会在回滚前显示该步骤日志末尾 80 行，便于定位且不回显迁移包密码。
- 补充迁移差异预览、Nfuse 就绪等待和失败日志回显回归测试；按迭代规则在本地保存 4.6.9 脚本与迭代记录备份。

## 4.6.9 - 2026-07-15

- 修复全新安装只写入基础 sing-box 配置、却未初始化 `/etc/sing-box/managed-users.json` 的问题；部署事务现在会载入刚生成的运行配置并创建、校验 schema 3 空状态，覆盖式全新部署会明确清除旧管理状态。
- 初始化失败现在会回到外层部署事务执行环境快照回滚；修复部署模式还会记录原环境中不存在的受管路径，后续阶段失败时不再遗留本次新建的状态、证书、服务或二进制文件。
- 新部署完成后无需先进入其他管理菜单，即可被识别为完整环境，并可直接通过只读 audit 与空机 `full` 迁移恢复验收。
- 补充新部署状态初始化回归测试，并在验收工具帮助与 README 中列出版本记录和 root 启动副本路径覆盖变量。
- 按迭代规则在本地保存 4.6.8 脚本与迭代记录备份。

## 4.6.8 - 2026-07-15

- 抽取统一的 sing-box 配置原子改写函数，合并用户入站追加与删除、分流应用与删除中重复的格式化、临时文件、失败清理、权限继承和原子替换逻辑。
- 抽取“根据状态数据生成用户入站”函数，用户启用和一致性修复统一通过同一入口重建 SS2022 + ShadowTLS 或 AnyTLS 入站。
- 保持现有菜单、命令行为、状态文件结构、迁移包格式和协议配置不变；缺少协议字段的既有用户仍按 SS2022 + ShadowTLS 处理。
- 新增两种协议的状态入站构建、用户入站增删、分流配置增删、配置权限继承，以及 sing-box 格式化或 jq 失败时原配置不变且不遗留临时文件的回归测试；实机生命周期验收补充自用 SS2022 + ShadowTLS 用户停用和重新启用检查。
- 修复配置改写函数在续期自动启用或迁移重建等条件调用链中失败后，Bash 可能抑制 `errexit/ERR` 并继续修改状态的问题；配置写入、用户删除、续期备份和迁移恢复各步骤现在显式传播失败，启用与一致性修复会明确执行回滚。
- 强化事务备份、回滚和完整环境快照的逐步错误检查：双文件恢复先阶段化并校验，避免半恢复或不完整备份；一致性修复失败时恢复包含 Nfuse 的完整环境快照；计量用户启用前验证 Nfuse 账户与端口绑定，并在失败时恢复原用量。
- 修复迁移清理、状态写入、Nfuse 用量恢复、sing-box 校验或一致性审计失败后仍可能继续并生成成功报告的问题；任一步骤失败都会恢复操作前环境，并记录实际失败阶段。
- 修复写入型验收把整个生命周期置于 `if` 条件中、可能掩盖中间命令失败的问题；生命周期、清理和 full 子流程改为非条件执行，失败时统一恢复验收前环境快照、复检服务与管理数据并生成失败报告；library 方式载入验收工具不再创建临时目录，帮助、非法参数和其他早退路径也不会遗留目录。
- 按迭代规则在本地保存 4.6.7 脚本与迭代记录备份。

## 4.6.7 - 2026-07-15

- 新增分级实机验收工具：默认 `audit` 只读检查版本、脚本副本、服务、sing-box 配置、Nfuse、到期定时器和管理数据一致性。
- 新增显式授权的 `lifecycle` 验收，使用随机测试名称和空闲端口验证 SS2022 + ShadowTLS 自用用户、AnyTLS 计量用户、导出、停用、启用、配额耗尽、到期停用、续期及 Nfuse 联动，并在结束时自动清理。
- 设置远程规则集地址后，生命周期验收会同时验证用户专属分流的增加、停用、启用和删除。
- 新增仅允许在受管用户和分流均为空时执行的 `full` 验收，覆盖 `.sbm` 单文件迁移包创建、结构验证，以及用户、分流和 Nfuse 已用流量恢复。
- 每次验收生成不包含用户、协议或上游凭据的 JSON 报告；写入型验收前自动创建完整环境快照，清理失败时使用该快照兜底恢复。
- CI 新增验收工具语法、ShellCheck 和隔离审计夹具测试，确保报告结构、默认只读行为、模式保护和退出状态稳定。
- 修复 Debian Bash 在 `set -u` 下首次登记临时路径时，空数组长度被视为未绑定并导致环境快照中断的问题；临时路径改用独立计数器管理，并新增首次登记与清理回归测试。
- 按迭代规则在本地保存 4.6.6 脚本与迭代记录备份。

## 4.6.6 - 2026-07-15

- 修复管理脚本在线更新只替换 `/usr/local/sbin/sb-user-manager`，但用户最初下载到 `/root` 等位置的启动副本仍保持旧版本的问题。
- 更新成功并完成服务验证后，会把新脚本同步回当前实际启动文件；同步失败时明确提示以后应使用的安装路径。
- 管理脚本更新完成后不再要求用户手动退出，而是立即 `exec` 到新版已安装脚本，避免当前交互进程继续执行旧代码。
- 从旧副本启动时，如果检测到 `/usr/local/sbin/sb-user-manager` 版本更高，会在进入菜单前自动转交给新版脚本；内部到期任务参数也会原样转交。
- 在临时服务器确认复现根因：版本记录和已安装脚本为 4.6.5，但 `/root/sb-user-manager.sh` 及运行进程仍为 4.6.3，旧进程 FD 2 已被重定向到 `/dev/null`。
- 自动化测试新增启动副本同步和旧副本向新版已安装脚本转交验证；临时服务器实测“查看服务状态”和“检测更新”返回主菜单后提示词均正常显示。
- 三台测试服务器均已完成旧版备份、双入口脚本哈希核对、服务状态、sing-box 配置及管理数据一致性验证。
- 按迭代规则在本地保存 4.6.5 脚本与迭代记录备份。

## 4.6.5 - 2026-07-15

- 完成交互状态泄漏专项审查，覆盖文件描述符、管理锁、错误陷阱、信号、工作目录、`IFS`、终端回显及菜单返回路径。
- 修复用户管理和分流管理中选择“0 返回”并跳过暂停提示时，FD 9 管理锁可能持续占用到下一次操作的问题；所有菜单现在会在绘制前统一恢复空闲锁状态。
- 新增真实伪终端菜单回归测试，连续覆盖隐藏输入、检测更新、查看服务状态、用户管理直接返回、分流管理直接返回和一致性检测，逐次验证提示可见、终端回显恢复且管理锁已经释放。
- 静态测试禁止再次引入会永久修改标准错误的无子命令 `exec ... 2>/dev/null` 写法。
- 按迭代规则在本地保存 4.6.4 脚本与迭代记录备份。

## 4.6.4 - 2026-07-15

- 修复执行检测更新、查看服务状态或其他会释放管理锁的操作后，返回菜单时“请选择：”及后续 `read -p` 交互提示消失的问题。
- 根因是无子命令的 `exec 9>&- 2>/dev/null` 永久修改了脚本标准错误；现将错误重定向限制在关闭锁文件描述符的局部命令组内。
- 自动化测试新增释放操作锁后标准错误仍可写入的回归验证。
- 按迭代规则在本地保存 4.6.3 脚本与迭代记录备份。

## 4.6.3 - 2026-07-14

- 新增统一的运行期临时文件登记与清理机制；脚本正常退出、报错退出、收到 `INT` 或 `TERM` 时都会清理由本次运行创建的受管临时文件和目录。
- 安装部署、现有环境接管和迁移恢复三类高风险事务新增信号中断回滚；中断发生在修改阶段时，会先恢复操作前环境快照，再以标准中断状态退出。
- 迁移状态临时文件及单文件迁移包的固定名称 `.tmp` 文件纳入安全清理，避免异常中断后在配置目录或备份目录留下含敏感数据的中间文件。
- 清理两台测试服务器上此前异常中断遗留的 `/tmp/sb-*` 临时数据，并终止本机失去父任务的旧 SSH/传输进程。
- 自动化测试新增受管路径边界、正常退出清理、`TERM` 中断清理、回滚回调和退出码验证。
- 按迭代规则在本地及两台测试服务器保存 4.6.2 脚本备份。

## 4.6.2 - 2026-07-14

- 修复内容显示完毕后“按回车返回菜单…”提示可能不显示的问题；提示改为显式输出后再读取回车，不再依赖 `read -p` 的终端判断。
- 检测更新时优先从实际安装的 `/usr/local/sbin/sb-user-manager` 读取管理脚本版本，版本记录文件仅作后备，避免记录滞后时误报旧版本。
- 自动化测试新增暂停提示回显和实际安装版本优先级验证。
- 按迭代规则在本地及两台测试服务器保存 4.6.1 脚本备份。

## 4.6.1 - 2026-07-14

- “数据备份与恢复”新增“恢复报告管理”子菜单，可直接查看历史恢复报告列表和详细内容。
- 报告列表以中文显示成功、失败后已回滚及回滚异常状态，并汇总来源主机、用户、分流、Nfuse 账户和迁移包名称。
- 报告详情显示执行时间、失败阶段、源端版本、迁移包 SHA256 与恢复前环境快照路径，不展示用户或上游凭据；旧报告仍可正常查看。
- 新增按编号删除恢复报告和按保留数量清理旧报告；清理前必须输入 `CLEANUP`，且至少保留 1 份。
- 损坏或格式异常的报告会在列表中明确标记，查看详情时拒绝解析。
- 一致性检测改为一次规范化读取用户和分流字段，修复部分既有服务器在逐字段解析首条用户记录时检测提前结束的问题，并减少循环内重复调用。
- 自动化测试覆盖报告校验、列表、详情、异常识别、单份删除和保留策略清理。
- 按迭代规则在本地及两台测试服务器保存 4.6.0 脚本备份。

## 4.6.0 - 2026-07-14

- 迁移备份升级为单个 `.sbm` 文件，内部封装 AES-256-CBC 密文、密文 SHA256、PBKDF2 参数和 HMAC-SHA256 认证信息。
- 按使用者决定移除旧三文件迁移包兼容；4.6.0 菜单只识别、导入、验证、恢复和删除 `.sbm`。
- 新增“验证并预览备份”，在不修改服务器的情况下验证密码、认证、解密数据和状态结构。
- 恢复预览列出将新增、替换、删除的用户和分流，以及 Nfuse 已用流量变化。
- Nfuse 预览按流量大小自动使用 B、MiB 或 GiB，避免极小用量被显示为 `0 GiB`。
- 恢复前检查改为一次列出全部已发现的端口、入站、出站和规则集标签冲突。
- 恢复成功或失败回滚后生成不含凭据的 JSON 结果报告，记录迁移包 SHA256、来源、数量、结果及环境快照位置。
- 新增“清理旧备份”，分别保留最近 1–100 份单文件迁移备份和环境快照，必须输入 `CLEANUP` 才会删除旧文件。
- 更新 README 中的单文件迁移、验证预览、恢复报告和保留策略操作说明。
- 按迭代规则在本地及两台测试服务器保存 4.5.2 脚本备份。

## 4.5.2 - 2026-07-14

- 在 README 中新增完整的备份与恢复操作手册，区分跨服务器迁移包和自动环境快照的用途。
- 详细说明源服务器创建、三文件传输、目标服务器路径导入、恢复确认、恢复后验证及冲突处理步骤。
- 补充迁移数据包含/排除范围、4.5.0 旧包兼容、密码与文件保管、自动回滚和故障处理说明。
- 按迭代规则在本地及两台测试服务器保存 4.5.1 脚本备份。

## 4.5.1 - 2026-07-14

- 环境快照改用独立 `root/` 数据目录、版本标记、符号链接清单和 SHA256 清单；恢复前必须完整校验，损坏快照会被拒绝。
- 环境回滚会先停止到期定时器、sing-box 与 Nfuse，再恢复文件并重新启动服务，修复覆盖运行中二进制时的 `Text file busy` 问题。
- 迁移包新增基于 200000 次 PBKDF2 派生密钥的 HMAC-SHA256 认证文件，可检测迁移密文或认证信息被篡改。
- 继续兼容 4.5.0 迁移包；旧包会明确标记为“旧版未认证”，通过原 SHA256 校验后仍可恢复。
- “数据备份与恢复”新增“导入迁移包”，可直接输入任意位置的 `.enc` 文件路径，自动校验并复制迁移包及相邻认证文件。
- 自动化测试新增错误密码、密文篡改、环境快照篡改和快照文件恢复覆盖。
- GitHub Actions 的 `actions/checkout` 更新到 v7，适配当前 Node.js 运行时。
- 按迭代规则在本地及两台测试服务器保存 4.5.0 脚本备份。

## 4.5.0 - 2026-07-14

- 新增“数据备份与恢复”菜单，用于在全新部署的服务器之间迁移受管用户、凭据、配额、有效期、启停状态、分流和 Nfuse 已用流量。
- 迁移包使用 AES-256-CBC、PBKDF2 与 200000 次迭代加密，密码至少 8 位，并生成独立 SHA256 完整性校验文件。
- 支持查看迁移包列表与加密详情、删除迁移包，以及恢复前展示来源、时间、用户和分流数量。
- 恢复前检查状态结构、重复用户/端口、外部入站端口、出站标签和规则集标签冲突。
- 恢复仅替换脚本受管数据，不覆盖目标服务器的基础 sing-box 配置和外部入站、出站或路由。
- 恢复时重建 sing-box 入站、分流、Nfuse 账户与端口绑定，并恢复已用流量；失败时自动回滚恢复前完整环境快照。
- 迁移包不包含服务器地址、域名、系统组件、二进制或 systemd unit，订阅与接入地址继续由外部系统管理。
- 修复交互菜单完成管理操作后仍持续持有并发锁的问题；返回菜单前立即释放操作锁。
- 兼容迁移旧版本创建的历史端口；恢复时接受合法的 `1–65535` 端口并继续执行占用冲突检查，新建用户仍固定限制在 `20001–30000`。
- 按迭代规则在本地 `backups/` 保存 4.4.4 脚本与迭代记录备份。

## 4.4.4 - 2026-07-14

- 从新部署管理配置中删除无效的 `PORT_MIN`、`PORT_MAX`，避免使用者误以为可以通过配置覆盖端口范围。
- 现有环境首次加载时自动备份并清理这两个旧字段。
- 用户端口校验和随机生成仍由脚本内部固定限制在 `20001–30000`。
- 按迭代规则在本地 `backups/` 保存 4.4.3 脚本与迭代记录备份。

## 4.4.3 - 2026-07-14

- AnyTLS SNI 改为添加用户时必填并按用户保存，导出配置读取用户自己的 `tls_sni`。
- 状态结构升级到 schema 3；现有 AnyTLS 用户从旧 `TLS_SERVER_NAME` 自动迁移，失败时恢复状态备份并拒绝继续。
- 迁移校验成功后备份并从管理配置删除 `TLS_SERVER_NAME`；新部署不再生成该全局字段。
- 保留全局 `HANDSHAKE_PORT=443` 与 `SHADOWTLS_STRICT_MODE=true`，继续统一用于 ShadowTLS。
- 按迭代规则在本地 `backups/` 保存 4.4.2 脚本与迭代记录备份。

## 4.4.2 - 2026-07-14

- 状态结构升级到 schema 2；旧 SS2022 用户自动从现有 sing-box 入站迁移加密方式与 ShadowTLS SNI。
- 已停用、当前没有入站的旧用户可使用迁移前管理配置中的旧值完成一次性迁移；缺少可靠来源时拒绝迁移并恢复状态备份。
- 全部用户迁移校验成功后，自动备份管理配置并删除 `SS_METHOD` 与 `HANDSHAKE_SERVER`。
- 新部署配置及用户启用、修复、导出流程不再包含旧全局 SS2022 配置或回退逻辑。
- `TLS_SERVER_NAME` 仅继续用于 AnyTLS 客户端配置，不再参与 SS2022 + ShadowTLS。
- 按迭代规则在本地 `backups/` 保存 4.4.1 脚本与迭代记录备份。

## 4.4.1 - 2026-07-14

- 添加 SS2022 + ShadowTLS v3 用户时可选择 `2022-blake3-aes-128-gcm` 或 `2022-blake3-aes-256-gcm`，直接回车默认使用 AES-128。
- ShadowTLS SNI 改为添加用户时必填，并按用户写入状态文件；启用、修复及导出配置均使用该用户自己的 SNI。
- 旧用户未保存独立 SNI 或方法时继续回退到原全局配置，保持向后兼容。
- 按迭代规则在本地 `backups/` 保存 4.4.0 脚本与迭代记录备份。

## 4.4.0 - 2026-07-14

- 状态文件新增 `schema_version`，当前结构版本为 1。
- 旧版无版本状态文件首次加载时自动备份并迁移，迁移后校验用户及分流数组，失败则恢复原文件。
- 检测到高于当前脚本支持的状态版本时拒绝运行，避免旧脚本破坏新数据。
- 新建状态文件直接使用最新结构。
- “安装部署”新增全新、本项目完整、本项目损坏、本项目部分及外部部署五类环境诊断。
- 外部部署可在完整备份后保留现有 sing-box 配置并接管；原有入站、出站及路由继续保留为外部配置，不自动纳入管理状态。
- 安全接管仅接受标准 Nfuse 数据库与 Socket 路径，配置无效或路径不兼容时拒绝执行。
- 接管可识别系统 PATH 中由发行版软件包或其他安装方式提供的 sing-box，不强制要求原可执行文件位于 `/usr/local/bin`。
- 接管时补齐后续分流所需的 `direct` 出站与 `local` DNS 标签，并在任一步骤失败时恢复原环境及清理新建文件。
- 本项目部分或损坏部署支持保留配置修复；状态文件缺失但存在疑似受管用户入站时拒绝自动修复，防止用户数据失配。
- 单元测试覆盖旧状态迁移、未来版本拒绝及五类环境中的基础分类路径。
- 按迭代规则在本地 `backups/` 保存 4.3.3 脚本与迭代记录备份。

## 4.3.3 - 2026-07-14

- 查看用户时结合 Nfuse 实时用量判断状态；已用量达到或超过配额的 active 计量用户显示为“配额耗尽”。
- 保持 sing-box 入站和状态文件的 active 状态不变，实际网络阻断继续由 Nfuse/nftables 执行。
- 抽取用户表格渲染函数，并增加“恰好耗尽”和“尚有剩余”两种边界单元测试。
- 按迭代规则在本地 `backups/` 保存 4.3.2 脚本与迭代记录备份。

## 4.3.2 - 2026-07-14

- 修复自用用户停用后错误提示“Nfuse 账户与用量保留”的问题；仅实际存在 Nfuse 账户时显示该说明。
- 修复自用用户启用后错误提示“清零已用配额”的问题；仅实际执行 Nfuse 重置时显示该说明。
- 不改变计量用户的停用、启用及清零行为。
- 按迭代规则在本地 `backups/` 保存 4.3.1 脚本与迭代记录备份。

## 4.3.1 - 2026-07-14

- 重构状态文件写入逻辑，引入统一的原子更新函数，集中处理临时文件、权限、所有者继承和失败清理。
- 合并用户删除、状态修改、续期、配额调整及分流状态修改中的重复 `jq + chmod + mv` 代码。
- 抽取统一的用户列表加载、展示和编号校验函数，删除“删除用户”和“导出配置”之间的重复菜单实现。
- 增加库模式和状态/列表单元测试，在不进入交互菜单的情况下验证重构后的公共函数。
- 为传入统一状态更新函数的 jq 过滤器标注 ShellCheck `SC2016` 例外，避免把 jq 变量误判为 Bash 展开错误。
- 重构只改变内部实现，不改变菜单、状态文件结构和现有功能行为。
- 按迭代规则在本地 `backups/` 保存 4.3.0 脚本与迭代记录备份。

## 4.3.0 - 2026-07-14

- “检测更新”接入本仓库 GitHub Release，可同时检查并更新 sing-box、Nfuse 和管理脚本。
- 私有仓库在线更新支持从 root 专用配置读取细粒度只读 `GITHUB_TOKEN`，Release 附件必须具有可信 SHA-256 digest。
- 下载管理脚本后校验 SHA-256、Bash 语法、Release 标签与脚本内版本号，确认无误后才替换已部署脚本。
- 新增“一致性检测与修复”，核对状态文件、sing-box 入站/分流与 Nfuse 账户/端口，并可按状态文件修复明确归属的配置。
- 自动修复不会删除无法确认归属的孤立配置；关联用户不存在等情况保留为人工处理项。
- 新增 GitHub Actions：Bash 语法、ShellCheck、静态发布检查，以及打标签后的 Release 附件和 SHA-256 自动发布。
- 修复首轮 CI 发现的未使用常量和 Unicode 引号 ShellCheck 警告。
- 按迭代规则在本地 `backups/` 保存了 4.2.1 脚本与迭代记录备份。

## 4.2.1 - 2026-07-14

- 新增分流时，用户填写的分流名称直接作为 sing-box `rule_set` 标签，不再自动添加 `managed-split-` 前缀。
- 用户填写的出站名称直接作为 sing-box outbound 标签，不再为新分流自动生成 `managed-out-` 前缀。
- 分流名称和出站名称格式不合法、重名或使用保留名称时，在当前输入步骤提示并允许重新输入。
- 保留对旧版自动标签的兼容，已有分流可继续正常启用、停用和删除。
- 将第一台测试服务器现有 `AI` 分流迁移为精确标签 `AI`，出站标签保持用户填写的 `Hinet`。
