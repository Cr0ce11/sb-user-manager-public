# 交接文档

本文件是**跨会话交接的当前快照**：接手的人先读它，就能知道现在做到哪、下一步该干什么、
哪些决定已经定了不必重问、哪些路已经走不通不必再试。

写作日期：**2026-08-21**，对应 `main` 提交 `36cdb3f`、正式版 **v4.25.24**。

**它不是正式来源。** 每类长期信息的主要正式来源见 [`PROJECT.md`](PROJECT.md) 的
「正式来源与文档职责」；本文件只做摘要与指路，不重复维护路线图、门禁清单或操作步骤。
两者冲突时以那些文件与 GitHub Issue 为准。

**接手前必读**：[公开 Issue #63](https://github.com/Cr0ce11/sb-user-manager-public/issues/63)
的最后几条评论。那里是逐轮的进展续记，比本文件更细，也更新得更勤。

---

## 一、当前任务和进度

### 当前没有已排期的开发任务

上一轮（v4.25.24）把手上排到的活全部做完并发布了。**接手时不需要抢着开工**——
剩下的开放项各有归属，多数需要项目所有者先拍板。

### 仓库与版本状态

| 项 | 取值 |
|---|---|
| `main` | `36cdb3f`（同步 v4.25.24 发布完成状态 (#223)） |
| 最新正式版 | **v4.25.24**（不可变 Release） |
| 直接代码回退点 | **v4.25.23** |
| 开放 Pull Request | 无 |
| 分支 | 只有 `main` |

### 正式业务环境

正在从 sing-box 搬到 mihomo，**由项目所有者自己执行**，仓库侧不登录正式服务器。

- `turin` 已经搬完，切换成功、客户端可用；sing-box 残留还没清，处于观察期。
- 其余服务器仍停在 **v4.25.16**。
- 每搬完一台，结果记在
  [公开 Issue #217](https://github.com/Cr0ce11/sb-user-manager-public/issues/217)。

### 测试环境

开发机上的五台 OrbStack 机器（能力与未覆盖项见
[公开 Issue #139](https://github.com/Cr0ce11/sb-user-manager-public/issues/139)）。

| 机器 | 版本 | 内核 | 管理器数据目录 | 说明 |
|---|---|---|---|---|
| `sbm-deb12` | 4.25.24 | sing-box | `/etc/sing-box` | 1 个探路用户 `probe1` |
| `sbm-deb12b` | 4.25.23 | sing-box | `/etc/sing-box` | 换内核演练机，已用快照还原；本轮没有随其他三台升到 4.25.24 |
| `sbm-deb13` | 4.25.24 | mihomo | `/etc/sb-user-manager` | 2f 全新安装出来的机器，零用户 |
| `sbm-mihomo` | 4.25.24 | mihomo | `/etc/sing-box` | 零用户零分流（2f 之前装的，目录仍是老名字） |
| `sbm-gate` | 未部署 | — | — | 只跑本地门禁 |

**四台部署机上都有一条绕开宿主机代理 fake-IP DNS 的 `/etc/hosts` 条目**
（`raw.githubusercontent.com` 指向 GitHub 的真实地址）。含远程规则集的验收依赖它，
详见下面第五节。

---

## 二、已完成事项

按时间倒序，只列结构性的几件；逐版本的用户可感知变化见
[`CHANGELOG.md`](CHANGELOG.md)，逐条事项与状态见 [`TODO.md`](TODO.md)。

### v4.25.24 —— 分流的两种新规则来源

- **网址来源的规则集**（[#218](https://github.com/Cr0ce11/sb-user-manager-public/issues/218)）：
  配置里写 `type:http` 加一天的更新间隔，由 mihomo 自己更新；保存那一刻由管理器
  自己下载一次并挡住五件事，另加「立即更新规则集」与审计里的来源漂移比对。
- **GeoSite／GeoIP 类别**（[#219](https://github.com/Cr0ce11/sb-user-manager-public/issues/219)）：
  第三种来源，状态里只有类别清单；类别名保存时由 `mihomo -t` 当场校验；
  新增「geo 数据源」设置项。

### v4.25.16 – v4.25.23 —— 接入第二内核（mihomo），三步计划走完

- 第一步：一台部署声明自己用哪个内核（v4.25.16）。
- 第二步 2a–2f：安装启停、用户入口、分流生成、审计、全新安装只装 mihomo
  （[#157](https://github.com/Cr0ce11/sb-user-manager-public/issues/157) 已关闭）。
- 第三步：把已有 sing-box 部署整机切换到 mihomo（v4.25.22，
  [#203](https://github.com/Cr0ce11/sb-user-manager-public/issues/203)）。
- 管理器自身数据搬出 `/etc/sing-box`：新装机器用中立目录 `/etc/sb-user-manager`，
  **存量机器不搬家**（[#172](https://github.com/Cr0ce11/sb-user-manager-public/issues/172)
  三步走的第二步）。

### 更早

公开仓库迁移与匿名更新、不可变 Release、完整卸载、备份保留与恢复演练、
内核适配层（`src/05-kernel.sh`）的抽出与收尾。详见 `CHANGELOG.md`。

---

## 三、下一步计划

**没有已排期的开发任务。** 下列各项都需要项目所有者先拍板或亲自执行：

| 事项 | 归属 | 说明 |
|---|---|---|
| 正式服务器搬到 mihomo | 项目所有者执行 | [#217](https://github.com/Cr0ce11/sb-user-manager-public/issues/217)；机器要先升到 v4.25.22 或更新 |
| 生产线是否单独走一条只修 bug 的 sing-box 稳定线 | **暂缓中** | [#202](https://github.com/Cr0ce11/sb-user-manager-public/issues/202)；项目所有者已叫停，**不要照着它开工** |
| 完整环境快照没有手工恢复入口 | P2 待办 | [#207](https://github.com/Cr0ce11/sb-user-manager-public/issues/207) |
| 静态门禁里的裸 `[[ ]]` 断言在开发机上空转 | P2 待办 | [#167](https://github.com/Cr0ce11/sb-user-manager-public/issues/167) |
| 剥离仅供测试的 `SB_DEPLOY_PROXY_KERNEL` | P3 待办 | `TODO.md` 的 MAINT-012 |
| 「验证分流是否生效」在 mihomo 上不可用 | 人肉兜底、不排期 | [#187](https://github.com/Cr0ce11/sb-user-manager-public/issues/187) |
| Mihomo 格式的用户配置导出 | 候选 | [#16](https://github.com/Cr0ce11/sb-user-manager-public/issues/16) |

长期方向是**全面转向 mihomo**（[#172](https://github.com/Cr0ce11/sb-user-manager-public/issues/172)）；
项目所有者提过 mihomo 版完全稳定之后可以把 sing-box 版归档。

---

## 四、已确定的技术决策及原因

长期有效的产品与技术决定以 [`docs/DECISIONS/`](docs/DECISIONS/) 为准。
下面这些是近期在 Issue 里定下、还没有单独 ADR、但接手后马上会用到的：

### 方向与范围

- **新功能、新护栏默认只做 mihomo 侧。** 项目所有者原话：「这条不做，我都要放弃
  singbox 了」。sing-box 只维持现状——既有行为不许回归，但不加新东西。
- **代理协议只用 SS2022（可加 ShadowTLS）与 AnyTLS。** 不引入 Hysteria2、TUIC
  这类 UDP 系协议。
- **不再支持接管别人手工装的 sing-box。** 相关实现整段删除而不是把入口藏起来：
  留着一段谁都调不到的部署代码，迟早有人以为它还能用。

### 分流的三种规则来源

- **三种来源并存，不是替换**：网址、本机文件、GeoSite／GeoIP 类别。
  项目所有者自己一律用网址。
- **远程规则集的更新间隔固定一天**，不给使用者选——理由是保证规则的时效性。
- **下载不走代理**，服务器默认都能访问 GitHub。
- **下载下来的文件保持地址里的原有命名**，便于自己识别。因此名字不合法时明确拒绝
  而不是悄悄改名；两个不同地址算出同一个文件名时拒绝，并指出被哪一条分流占用了。
- **只支持 yaml 格式的远程规则集**，`.list`／`.txt`／`.mrs` 一律拒绝。多一种格式
  就多一套校验与一种说不清的失败。
- **保存那一刻由管理器自己下载一次并校验。** 这条非做不可：mihomo 取不到远程规则集
  且本地没有缓存时只留一行警告、服务照常起来，**那条分流静静地完全不生效**。
  内核不报，就只能管理器来报。
- **geo 类别的类别名交给 mihomo 当场校验**（保存时跑一次 `mihomo -t`）。与上面相反，
  geo 不会静默失效：类别名写错、数据库缺了，mihomo 直接拒绝加载配置。
  代价是数据库丢了且当时下不到时服务起不来——这个取舍是明知并接受的。
- **`geodata-mode`／`geo-auto-update`／`geo-update-interval` 只在配置里真的有 geo 规则
  时才写**，最后一条 geo 规则没了之后由管理器收回。理由见第五节的实测。
- **「立即更新规则集」接受几秒断线**：更新后重启内核让新规则立刻生效，并明确告知。
  不重启的话新内容要等下一个更新周期，可能是一天以后。

### 护栏与告警的设计原则

- **真正的敌人是「狼来了」，不是漏报。** 会自愈的事只标记、不计为问题（例如
  ShadowTLS 握手目标这次探不通，多半是临时网络问题）。
- **凡是验证「某个限制生效」的断言，必须配一个对照**，证明限制之外的东西仍然正常。
  否则分不清「机制在起作用」和「整个东西都坏了」。
- **新增护栏之后，先看它在「什么都正常」的夹具上说不说话**，再看它该报的时候报不报。
- **可靠性链路上的单点不许记进欠账清单**，当场闭合。

### 发布与授权

- **发布正式版前停下来等项目所有者确认**，即使前面的开发工作已经批准。
- **创建 Release 不等于授权部署到正式环境**，正式环境上线要单独授权。
- **仓库侧不登录任何正式服务器。** 正式机器的操作由项目所有者执行，我读输出。
- **任何凭据都不写进报告**（密码、PSK、token、密钥）；需要时给一条他自己去取的命令。

---

## 五、踩过的坑和已否决的方案及原因

### 已否决的方案

| 方案 | 为什么否决 |
|---|---|
| 骨架里**无条件**写那三个 geo 键 | 实测：写上去而一条 geo 规则都没有时，**不会**下载任何数据库，但每次启动都留一行 error 级日志说 `GeoSite.dat` 找不到；对照——不写这几个键时同一份配置连这行都没有。一台从来不用 geo 的机器不该在日志里天天报一条假错。改成按需写 |
| 按「管理器有没有 geo 分流」决定写不写那几个键 | 使用者自己写在顶层 `rules` 里的 `GEOSITE` 规则同样需要它们，按这个判断会把他的设置删掉。改成按「**配置里**有没有 geo 规则」判断 |
| 在 jq 里用 `contains` 认一条 mihomo 规则行属于哪条分流 | 一条 `OR,((GEOSITE,a),(GEOIP,us))` 的行会被条件 `GEOSITE,a` 认领，而那是别人的行。后果是删一条分流顺手删掉别人的（重建能补回来），以及审计把「已停用的分流仍在生效」报成恒真——那一条自动修复还修不掉，只会一直刷红。改成**连分隔符一起比** |
| 按「规则集标签」认行 | geo 分流根本没有规则集标签。改成按**匹配条件**认（规则集来源是 `RULE-SET,<标签>`，geo 来源就是类别本身），生成、删除、审计三处共用同一份定义 |
| 把「把握手目标指向不说 TLS 的服务」当作 ShadowTLS 严格模式的兜底 | 实测不区分，那条兜底与严格模式无关。真正的行为是：握手目标不支持 TLS 1.3 时，开着严格模式的监听器拒绝承载代理 |
| 用 `openssl s_client` 的**退出码**判断对方说不说 TLS 1.3 | 证书验证失败时它也非零退出。要看 `-brief` 输出里协商出来的 `Protocol version` |
| 把「没有管理配置」一律当成干净机器 | 「安装或修复环境」的修复分支会因此把一台 sing-box 机器修成 mihomo。改默认值时先想清楚「配置丢了的机器」算哪一类 |

### 踩过的坑（会再遇到的）

1. **`if ! cmd_xxx …; then` 会让整个函数体里的 errexit 失效。** Bash 的规则是：
   跟在 `if`／`!`／`&&`／`||` 后面的命令，其**整个动态调用范围**内 `set -e` 都不生效。
   从菜单进去的那些 `cmd_*`，内部任何一句 `return 1` 都不会中断流程，
   必须每个调用点自己 `|| return 1`。写新的 `cmd_*` 时先看一眼它是从哪儿被调的。

2. **只要 mihomo 的配置里有任何一条规则集条目（本机文件来源也一样），
   它启动时就会自己去下一个 12 MB 的 `ASN.mmdb`。** 不是哪个版本引入的，
   只是此前没有一台测试机在 mihomo 上带着分流跑过。因此
   [#165](https://github.com/Cr0ce11/sb-user-manager-public/issues/165)
   那条「不下载任何 geo 数据库」的断言要读作
   「**没有任何分流**的 mihomo 机器一个都不下」。

3. **宿主机的代理客户端工作在 fake-IP 模式**，会把公网域名解析成 `198.18.x.x`，
   于是「地址必须解析到公网」这条校验判它是内网而拒绝——**而手动 `curl` 却是通的**
   （代理替它接管了）。症状很容易看岔。做法是给规则集域名写 `/etc/hosts` 条目。
   同一个原因还会让「探一个公网域名的关闭端口」得到「TCP 居然通了」的结果；
   要验「连不上」这条路径，用本机闭端口（例如 `127.0.0.1:9`）。

4. **升级不会刷新 systemd 单元。** 改了单元内容的版本升上来之后，审计一定会
   报一次单元不一致——更新过程中写单元的还是旧脚本。这是设计如此，不是缺陷，
   跑一次自动修复即可。

5. **未定义变量会让整个进程当场退出，回滚都来不及跑。** 换内核演练撞到过
   （漏了 `fetch_latest_releases`），兜底是启动时的环境事务恢复。
   新写改环境的流程时，先确认要用的全局变量都由谁填。

6. **测试夹具会随默认值改变而失去意义。** 有一批用例模拟「已部署的 sing-box 机器」
   却不给管理配置，2f 之后它们变成「干净机器」，其中一个停在提问上把整份单元测试
   挂住十分钟。改默认值时把「不给配置文件」的夹具全找一遍。

7. **手工往状态文件里造存量形状时，用户对象上的镜像字段也要一起改。**
   `validate_state_user_endpoints` 要求 `.transport`／`.shadowtls_password`／
   `.shadowtls_sni` 与 `endpoints[0]` 一致，只改 `endpoints[0]` 会让管理器一启动就拒绝。

8. **`sb-user-expiry.timer` 每 15 分钟触发一次，会拿走管理锁。**
   它可能恰好在验收收尾清理那几秒里触发，报「另一个管理操作正在进行」并留下
   没删干净的验收用户。跑 `lifecycle` 前先看一眼
   `systemctl list-timers sb-user-expiry.timer`，避开触发时刻。

### 环境与流程上的几条

- **本地门禁必须在 Linux bash 5 上跑**（`sbm-gate`）。开发机是 macOS／arm64，
  整份单元测试在上面必定失败（`switch_kernel_preflight` 的 `uname -m` 那一条没有桩）。
  见 [#167](https://github.com/Cr0ce11/sb-user-manager-public/issues/167)。
- **本地门禁 12 条不含 ShellCheck**（门禁机没装），跑完要在开发机补跑 CI 里那两条。
- **每次往测试机装脚本前后都比对摘要，并带重试**：OrbStack 的共享文件系统偶尔会让
  客机读到半份或旧的文件。
- **还原临时破坏先 `cp` 一份**，不要用 `git checkout <文件>`——那会把工作树里别人的
  改动一起抹掉。
- **提交前跑 `git status --short` 和 `git diff --stat` 逐处确认**改动都是自己这轮做的；
  提交说明用 `-F 文件` 传，不要用 `-m` 带反引号（shell 会把它当命令替换执行）。
- **迭代测试时把新增那一节单独抽出来跑**，全套单元测试留到最后一次。
- **需要 mihomo 的字段名时，从二进制里的结构体标签一次挖出来**；这招对规则类型无效。
