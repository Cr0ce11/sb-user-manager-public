# 交接文档

本文件是**跨会话交接的当前快照**：接手的人先读它，就能知道现在做到哪、下一步该干什么、
哪些决定已经定了不必重问、哪些路已经走不通不必再试。

写作日期：**2026-08-23**，对应 `main` 提交 `52d7a36`、最新正式版 **v4.25.30**。

**它不是正式来源。** 每类长期信息的主要正式来源见 [`PROJECT.md`](PROJECT.md) 的
「正式来源与文档职责」；本文件只做摘要与指路。两者冲突时以那些文件与 GitHub Issue 为准。

**接手前必读**：[公开 Issue #63](https://github.com/Cr0ce11/sb-user-manager-public/issues/63)
的最后几条评论，以及下面第三节点名的两个方向 Issue。

---

## 一、当前任务和进度

### 仓库与版本状态（本轮实读）

| 项 | 取值 |
|---|---|
| `main` | `52d7a36`，与 `origin/main` 一致，工作区干净 |
| 最新正式版 | **v4.25.30**（不可变 Release，附件摘要 `7fd506bb…`），直接代码回退点 v4.25.29 |
| 开放 Pull Request | **无** |
| 分支 | 远端只剩 `main` |

### 正式业务环境

**五台正式服务器（Air、Pro、Malibu、HKRFC、JPRFC）全部在 mihomo 上，并已全部完成
管理器数据搬迁**——用户资料、内部备份、AnyTLS 证书现在都在中立目录 `/etc/sb-user-manager`。

项目所有者在其中一台上实查过 sing-box 残留：`/etc/sing-box`、`/var/lib/sing-box`、
`/usr/local/bin/sing-box`、`sing-box.service` **全部不存在**，systemd 与 dpkg 也查不到
sing-box。**其余四台尚未逐台确认**，接手后值得补一次。

仓库侧按惯例不登录正式服务器；升级与搬迁都由项目所有者自己执行。

### 没有已知的 P0 或 P1 缺陷

这一轮做完了 [#276](https://github.com/Cr0ce11/sb-user-manager-public/issues/276)
（管理器数据搬到中立目录）并发布了 v4.25.30。接手时是一个干净的起点，
下一步是两件已经排好序的方向性工作（见第三节）。

### 测试环境（本轮逐台实读）

| 机器 | 内核 | 管理器数据目录 | 配置里有 `MANAGER_DATA_DIR` | 用途 |
|---|---|---|---|---|
| `sbm-gate` | 未部署 | — | — | 只跑本地门禁 |
| `sbm-deb13` | mihomo | `/etc/sb-user-manager` | 有 | 2f 全新安装形状 |
| `sbm-mihomo` | mihomo | **`/etc/sing-box`** | **没有** | **存量形状的唯一夹具** |
| `sbm-move-a`～`d` | mihomo | `/etc/sb-user-manager` | 有 | 本轮搬迁演练用的克隆机，已全部搬完 |

- **六台部署机的管理配置里 `SINGBOX_` 开头的键一个都没有**（逐台 `grep -c` 全为 0）。
- **一台 sing-box 机器都不存在了**：六台测试机全是 mihomo，三台 sing-box 测试机已于
  MAINT-012 删除，五台正式机全是 mihomo。这是下一步能删掉 sing-box 支持的前提。
- `sbm-move-a`～`d` 是本轮从 `sbm-mihomo` 克隆出来的一次性机器，证据留在
  [公开 PR #277](https://github.com/Cr0ce11/sb-user-manager-public/pull/277) 里；
  用完了可以删，项目所有者尚未表态。

**机器版本与形状一律以登机实读为准**，不要引用记录里的旧数字。

---

## 二、已完成事项

只列结构性的；逐版本的用户可感知变化见 [`CHANGELOG.md`](CHANGELOG.md)，逐条事项见 [`TODO.md`](TODO.md)。

### v4.25.30：搬迁管理器数据目录（[#276](https://github.com/Cr0ce11/sb-user-manager-public/issues/276)，[#172](https://github.com/Cr0ce11/sb-user-manager-public/issues/172) 三步走第三步）

「系统管理 → 部署与卸载」下新增一次性入口，把存量机器上管理器自己的三样
（用户资料、内部备份、AnyTLS 自签证书）从 `/etc/sing-box` 搬到 `/etc/sb-user-manager`。

- 第一步是**只算不改的演练**，答 `N` 时机器一个字节不动。
- 整段包在环境事务里，先照完整环境快照，任一步失败**逐字节还原**。
- **三样必须一起搬**：只搬一两样会让证书被判成不存在而重新签发，所有 AnyTLS 用户当场连不上。
- 证书还牵动 mihomo 单元里的 `SAFE_PATHS` 与运行配置里的证书路径，两处同一次改掉并重启内核
  （**用户断线数秒后自动重连，客户端配置不用改**）。
- **复制后先逐字节校验再删老位置**；老目录里不属于管理器的文件原样留着，目录空了才顺手删。
- 管理配置**只改与数据目录有关的三行**，不用 `write_manager_config` 整份重写。

同版本按 [ADR 0032](docs/DECISIONS/0032-environment-snapshot-serves-automatic-rollback-only.md)
「后续」一节把 `print_offsite_recovery_status` 写了回来并接上调用点——这是新添的不可逆操作。
静态断言相应从「不得作为死代码存在」改成「必须正好有一个调用点」。

同版本还修掉一处真机演练撞出来的缺陷：恢复记录的路径白名单只按当前 `MANAGER_DATA_DIR`
匹配，搬迁崩在改配置之前时新目录会被判成不受管路径，**开机自检拒绝恢复、机器卡在半迁移
状态要人工介入**。中立目录改为白名单里的字面量。

### 本轮的验收覆盖

真机演练在从 `sbm-mihomo` **克隆**出的四台一次性机器上做完，**没有消耗那个存量夹具**：
取消路径、两种存量形状的成功迁移、带 sing-box 遗留 `config.json` 的正式机形状、
注入失败的整体回滚（56 项逐字节一致）、中断后的开机自检恢复、已搬迁机器的「无需搬迁」对照。
迁移前后客户端配置一字未变、用量不清零、geo 类别分流逐条一致、证书文件逐字节相同。

发布后按 `docs/RELEASE.md` 走完：`sbm-mihomo` 退回真正的 v4.25.29 后走**真实菜单更新**
升到 v4.25.30，**升级前后除版本号外一字不差**——这证实了「升级本身不触发任何搬迁」。

---

## 三、下一步计划

两件方向性工作，**顺序已由项目所有者定死：先清 sing-box，再改名**。
理由是改名要碰的每一处与 sing-box 那 306 处有相当一部分重叠，先删干净，改名要动的面积小一截。

### 第一件：清除 sing-box 支持（[公开 Issue #280](https://github.com/Cr0ce11/sb-user-manager-public/issues/280)）

前提已经成立：一台 sing-box 机器都不存在了，而且没有第三方使用者。规模是
`src/` 306 处、`tests/test-unit.sh` 680 处、其余测试与门禁约 100 处。分片：

| 片 | 内容 | 风险 |
|---|---|---|
| 1 | 撤除搬迁入口（`TODO.md` ARCH-015），并删掉 `LEGACY_MANAGER_DATA_DIR` 与那行 `:=`；`MANAGER_DATA_DIR` 收敛成「配置里必须写着」 | 低 |
| 2 | 内核适配层收敛成单内核，`src/05-kernel.sh` 的 sing-box 分支整段删 | 中 |
| 3 | 部署与卸载路径、`singbox_deployment_present`、`singbox_preview_deployed`、三份路径清单里的 sing-box 条目 | 低 |
| 4 | 运行时：sing-box 入站生成、`.srs` 规则集那一套、配置骨架与规范化程序 | 中 |
| 5 | **测试与门禁**——真正的成本所在 | **最高** |
| 6 | 文案与文档 | 低 |

**第 1 片有两个前置动作，先做再动代码**：

1. **先克隆一台存量形状的机器留作反面样本**（例如 `orb clone sbm-mihomo sbm-legacy`），
   用来证明兜底删掉之后这种机器会**响亮地报错停下**，而不是悄悄落到老目录上。
2. **再把 `sbm-mihomo` 自己搬了**——它现在还是存量形状，删兜底之后它会起不来。

**第 5 片的要求**：`tests/test-unit.sh` 整份夹具是 sing-box 形状的（文件开头写死
`PROXY_KERNEL=singbox`）。改成 mihomo 是最大的一块工作，也是「删着删着覆盖悄悄变少」
的高风险区——改完可能全绿，但测到的东西比以前少，从颜色上看不出来。
**每改一块都要先破坏再确认变红，不接受「改完是绿的」作为证据。**

### 第二件：全面改名（[公开 Issue #281](https://github.com/Cr0ce11/sb-user-manager-public/issues/281)）

脚本改 `node-manager`、命令改 `ndm`、仓库与产品名随之更换（新名字尚未拍板）。
磁盘上带旧名字的路径共 **25 条**，外加自更新的仓库名与资产名。

**必须三步走，否则自更新会断**（唯一一条弄断了就得逐台手工修的链路）：

1. 先发一版，把更新目标指向新的仓库名与资产名；**这一版的 Release 同时带新旧两个附件名**。
2. 等所有机器升上来（六台测试机 + 五台正式机）。
3. 再真正改名：改仓库名，并在每台机器上执行一次磁盘改名迁移。

第 3 步本身又是一次一次性迁移，与 #276 同形状但面积大得多；那套骨架刚验证过，直接复用。
比 #276 多出来的难点见 #281，实施时逐条处理（改的是自己、单元改名要 disable/enable、
锁文件路径变了、恢复记录路径变了）。

### 其余开放项

| 事项 | 状态 |
|---|---|
| [#276](https://github.com/Cr0ce11/sb-user-manager-public/issues/276) | 五台已搬完，可在补齐记录后关闭 |
| [#187](https://github.com/Cr0ce11/sb-user-manager-public/issues/187) 「验证分流是否生效」在 mihomo 上不可用 | 人肉兜底、不排期，**有意保留** |
| [#16](https://github.com/Cr0ce11/sb-user-manager-public/issues/16) Mihomo 格式的用户配置导出 | 候选 |
| `TODO.md` 的 MAINT-013 | 开发机 ShellCheck 比 CI 新，本地报 CI 不报的 SC2329（4 条噪声） |

---

## 四、已确定的技术决策及原因

长期决定以 [`docs/DECISIONS/`](docs/DECISIONS/) 为准。下面是接手后马上会用到的：

### 这个仓库没有第三方使用者

项目所有者 2026-08-22 说明：**仓库公开只是为了不受私有仓库的 CI 额度限制**，
唯一的使用者就是他本人。因此：

- **不要为「公开版其他使用者的老机器」保留兼容兜底。** 判断一段向后兼容代码还该不该留，
  只看他自己的机器有没有那种形状。
- 不要因为「别人可能依赖」而放弃破坏式改动，也不要为此排额外的过渡期版本。
- **服务器外现有的加密迁移备份（`.sbm`）目前没有意义，作废没关系**（2026-08-23 明确）。
  但这不等于不需要备份：不可逆操作之前仍必须现做一份**新的**服务器外备份（ADR 0032）。

### 一次性的东西，跑完即撤

项目所有者要求一次性工具从立项起就排好撤除的那一版，不在项目里留不必要的历史遗留。
搬迁入口就是这样安排的（ARCH-014 做、ARCH-015 撤），撤除时要一并回退的记账
（几个断言数字、受管目标清单、单元测试一节、交互测试的桩与编号、README 那一节）
写在 `TODO.md` 的 ARCH-015 里，不靠人记得。

### 方向与范围

- **sing-box 线已归档，而且现在一台 sing-box 机器都不存在了**，因此整条支持可以删（#280）。
- **代理协议只用 SS2022（可加 ShadowTLS）与 AnyTLS。** 不引入 Hysteria2、TUIC。
- **撤除一个功能就整段删除，不把入口藏起来**，撤完加断言钉住不得复活，并配一条对照证明
  断言不是恒真。
- **GeoSite／GeoIP 类别分流是项目所有者在用、并且明确说重要的功能**，涉及它的验收不许省。
- **内核无关的路径不许写死内核名**，用 `kernel_display_name` 等适配层入口。

### 「像真的默认值」比没有值更危险（#262）

`PROXY_KERNEL` 与 `MANAGER_DATA_DIR` 的文件级取值是**「尚未解析」哨兵**，不是可用取值。
任何在载入管理配置之前读到它们的代码会当场报错，而不是拿到一个看起来正常、实际可能
完全错误的答案。第 1 片删兜底之后，`MANAGER_DATA_DIR` 会进一步收敛成「配置里必须写着」。

### 恢复记录的路径白名单要按字面认得固定目录名（#276）

`is_environment_recovery_path` 里管理器数据目录那一条是按**当前取值**匹配的，
而恢复记录是给**下一次运行的进程**读的。因此 `/etc/sb-user-manager` 必须同时是白名单里的
**字面量**——它是本项目固定的数据目录名，与任何一次性入口无关，**撤入口时不回退这一条**。

### 失败要说得出话（#265）

- `run_step_or_rollback` 会记一行失败的步骤名与退出码。
- **只记函数名，绝不记参数**：参数里有用户密码，记进日志等于把凭据扩散到日志和随后
  被贴出来的终端输出里。

### 目录权限（#246、#252）

- **`install -d -m` 点到一个已经存在的目录会直接 chmod 它。** 管理器自己的目录**故意**
  收紧到 700；系统共享目录（`/var/lib`、`/usr/local/sbin` 等）**一律只在缺失时创建**。
- 修复被改坏的机器时**只认 700 这个指纹**，且**只对本脚本部署过的机器动手**。

### 备份与退路（ADR 0032）

- **操作前的完整环境快照只服务于自动回滚，不做手工恢复入口。**
- **任何不可逆操作之前必须先有一份复制到服务器之外的 `.sbm`。** 这条现在由
  `print_offsite_recovery_status` 承担，它的唯一调用点是搬迁流程的确认提示；
  静态断言钉着「必须正好有一个调用点」。**搬迁入口撤除时它会再次变成孤儿**，
  届时把断言改回「不得存在」，或者接到新的不可逆操作上（改名迁移就是下一个）。

### 门禁

- **权威运行环境是 Linux 上的 Bash 5（`sbm-gate`）**；开发机是 macOS／arm64，
  整份单元测试在上面必定失败。
- **不改写那 502 条裸 `[[ ]]` 断言**；**新写的断言要用会自己报错的写法**。
- **新增护栏必须配反面样本与对照**：反面样本证明它不是恒真，对照证明它不误伤。
- **护栏不许被一句注释糊弄**。

### 发布与授权

- **发布正式版前停下来等项目所有者确认。**
- **创建 Release 不等于授权部署到正式环境。**
- **仓库侧不登录任何正式服务器**，除非项目所有者当面授权，且默认只到只读诊断。
- **任何凭据都不写进报告。**
- **CHANGELOG 由发布准备 PR 统一写，功能 PR 不动它。**
- **CHANGELOG 里关于「升级之后会怎样」的断言，只能由发布后那次真实菜单更新证实。**
- **GitHub 侧的事自己做，不要推给项目所有者。** Secret 在不在、工作流跑没跑、发不发 Release
  都有命令行路径（`gh secret list`、`gh workflow run`、`gh release`）。他自述完全不懂技术，
  只有真的只能在别人家后台点的事（域名商、云厂商控制台）才列步骤给他，而且要写到
  「点哪个按钮、填什么、怎么确认生效」这个粒度。
- **验证一个凭据有没有过期，最好的办法是让用到它的流程真跑一次**，而不是让人去看有效期。

---

## 五、踩过的坑和已否决的方案及原因

### 本轮新增的六条

1. **门禁命令不要接管道。** `bash tests/test-unit.sh | tail -5` 读到的退出码是 `tail` 的，
   永远是 0——失败信息滚过去了，看上去却像通过。本轮因此**连着两次把一个失败的单元门禁
   当成通过**，直到 CI 报出来才发现。写成
   `bash tests/test-unit.sh > /tmp/u.log 2>&1; echo "退出码=$?"` 再看日志。
   这条已经写进 `docs/DEVELOPMENT.md`。

2. **往一行几十个模式的长 `case` 分支里插东西，很容易顺手挤掉旁边一条。**
   本轮给 `is_environment_recovery_path` 加中立目录字面量时，把已有的
   `/etc/sb-user-manager.conf` 与 `/etc/sing-box` 一起挤掉了，后果是「完整卸载」组不起事务，
   而这件事只有单元测试里那个卸载用例在一长串调用之后才报出来。现在核心路径已经逐条钉住。

3. **新增菜单项会挤掉后面那些项的编号。** 「搬迁管理器数据目录」排在「安装或修复环境」
   之后，把「完整卸载」从 2 挤到 3，按编号驱动菜单的 `tests/test-interactive.py` 因此走错入口。
   加菜单项时顺手看一眼那个用例。

4. **受管步骤里的临时文件名必须落在 `is_managed_temp_path` 的白名单里。** 真机演练第一次就
   撞在这上面：用 `"${CONF_FILE}.XXXXXX"` 建出来的名字登记不上，整条流程当场回滚，
   还在 `/etc` 下留了一个没人清的残留文件。正确写法是
   `mktemp "$(dirname "$CONF_FILE")/.sb-user-manager.conf.XXXXXX"`，与几个兄弟调用点一致。

5. **用伪终端驱动菜单做真机演练时，「结束标记」不能是会在正文里出现的词。** 本轮的驱动器
   把「失败」当成结束标记，而确认提示的正文里就有「本次操作失败时」这几个字，于是刚发完
   `y` 就以为跑完了，随即 Ctrl-C——**把一次正在进行的迁移打断在事务中途**。
   结束标记用「按回车返回菜单…」这种只在收尾出现的字样。

6. **一次性入口会把「完整卸载」这类危险项的编号往后挤，而这是安全方向的**：
   有肌肉记忆的人按 2 得到的是带演练的搬迁，不是卸载。加菜单项时想一下挤的是哪个方向。

### 更早的坑（仍然有效）

7. **「操作失败」这四个字曾经是全部线索。** 写新的失败分支时别再退回去。

8. **「已修复」可能指的是「我的机器已经好了」，不是「代码修好了」。** 关 Issue 之前先看代码。

9. **交接文档写的计划不一定安全，动手前先自己核一遍。**

10. **`if ... fi` 没有 `else` 分支时退出码恒为 0。** 失败码必须写在 `else` 里。

11. **修复「服务与配置检查」报出的问题，入口是那个检查本身，不是「安装或修复环境」**——
    后者在完好的机器上第一个分支就返回。

12. **升级那一次执行部署的是旧脚本。** 新脚本里的修复要下一次才生效；
    反过来，单元文本的漂移会被下一次升级顺手修掉。

13. **到期定时器每 15 分钟触发一次，会拿走管理锁。** 跑验收前先看
    `systemctl list-timers`，排在一次触发刚结束之后。
    ⚠️ **不要用「停掉定时器」来避开它**：只读验收有一条要求它 active。

14. **用 `bash -x` 跑测试时，追踪行会把参数打进 stderr。**

15. **反向验证要真的把脚本装上去**，别用 `SB_ACCEPTANCE_MANAGER` 指到别处。

16. **护栏可能把缺陷一起钉住。** 写结构性断言时必须写清它保护的是哪个不变量。

17. **跟在 `if`／`!`／`&&`／`||` 后面的命令，其整个动态调用范围内 `set -e` 都不生效。**

18. **裸 `[[ ]]` 断言失败时一个字都不打印。** 定位手法：
    `bash -x tests/test-unit.sh 2>/tmp/x.log >/dev/null; tail -25 /tmp/x.log`。

19. **OrbStack 的共享文件系统偶尔让构建或客机读到半份文件。**
    **先比对摘要再重试，不要当成代码缺陷去查。**

20. **`$(...)` 会在 `gh pr create --body "..."` 里被 shell 执行**；正文一律用 `--body-file`，
    提交说明一律用 `-F` 文件。

21. **`lifecycle` 等写入型验收需要 `SB_ACCEPTANCE_CONFIRM=YES`。**

22. **用 `cp` 直装脚本到测试机，`audit` 会报两项假失败**（版本记录、root 启动副本）。
    同步 `/var/lib/sb-user-manager/versions` 与 `/root/sb-user-manager.sh` 即可避免。

23. **宿主机的代理客户端工作在 fake-IP 模式**，会把公网域名解析成 `198.18.x.x`；
    部署机上有绕开它的 `/etc/hosts` 条目。

24. **文档里靠人记得的那一行迟早会漂。** 遇到同类「必须一起改」的两处，直接加断言。

### 已否决的方案

| 方案 | 为什么否决 |
|---|---|
| 用 `write_manager_config` 整份重写来改数据目录那三行 | 会把使用者改过的握手端口、SNI、对外地址一并写回默认值；单元测试里有一条用它真做一遍的对照 |
| 撤搬迁入口时保留老目录兜底当「安全网」 | 前提是「公开版其他使用者的老机器」，而那不存在；兜底跟着一起退场，只是晚一步 |
| 命令名用 `nm` | 与 binutils 的 `/usr/bin/nm` 撞名，装在 `/usr/local/bin` 会盖住它；改用 `ndm` |
| 磁盘上的名字不改（#172 原建议） | 项目所有者知情后仍选择全改；便宜的时机（#276 那次迁移）已经过去，代价照付 |
| 把失败步骤的参数一起记进日志 | 参数里有用户密码；只记函数名 |
| 给完整环境快照加「从操作前快照恢复」的菜单入口 | 会静静抹掉快照之后的用户、分流与用量；详见 ADR 0032 |
| 把 502 条裸 `[[ ]]` 断言全部改写 | 它们在 Bash 5 上有效，问题出在运行环境 |
| 让脚本去清理 apt 装的 sing-box 残包 | dpkg 装的东西必须由 dpkg 卸载 |
| 停掉到期定时器来避开验收时的锁冲突 | 只读验收要求它是 active |
| 用 `openssl s_client` 的退出码判断对方说不说 TLS 1.3 | 证书验证失败时它也非零退出 |
| 用消耗 `sbm-mihomo` 的方式做搬迁真机演练 | 那是存量形状的唯一夹具；改为克隆专用机器，项目所有者 2026-08-22 采纳 |

### 环境与流程

- **门禁在门禁机上跑**：`orb -m sbm-gate bash tests/test-static.sh`；其余同理。
  **门禁 12 条不含 ShellCheck**，跑完在开发机补跑 CI 里那两条。
- **`tests/test-public-snapshot.sh` 要求工作区干净**，必须提交之后才能跑。
- **还原临时破坏先 `cp` 一份**，不要用 `git checkout <文件>`；还原后比对生成物摘要。
- **提交前跑 `git status --short` 和 `git diff --stat` 逐处确认。**
- **改 `src/` 之后要跑 `bash tools/build-manager.sh`** 重新生成单脚本。
- **服务器连接方式**：别名直接可用（`ssh Air`），资产清单在个人文件 `~/.claude/servers.md`，
  不在本仓库内。
- **克隆测试机**：`orbctl clone <源> <新名>`（源会被短暂暂停，新机器处于停止状态，
  用 `orbctl start` 起来）。本轮的 `sbm-move-a`～`d` 就是这么来的。

### 怎么跑「真实菜单更新路径」

用伪终端驱动编号式菜单：`pty.fork()` 起脚本，读屏幕、用正则从 `  N  标签` 里取编号发过去。
本轮实际走通的几条路径：

- 更新：`系统管理` → `检测更新` → `y`
- 搬迁：`系统管理` → `部署与卸载` → `搬迁管理器数据目录` → `y`／`n`
- 看菜单：`系统管理` → `部署与卸载`

**驱动器要注意**：按键发完不能因为没输出就退出（下载内核和 Nfuse 时会长时间静默），
要一直等到完成标记命中或总超时；**完成标记不能用会在正文里出现的词**（见第五节第 5 条）。
跑更新之前先把机器退回上一版的**已安装脚本、版本记录和 root 启动副本**三处，
否则脚本会认为已是最新而不走下载。

**证明「升级之后会怎样」时，要从一个真的会出问题的版本升上来。**
