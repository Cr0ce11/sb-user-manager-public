# 交接文档

本文件是**跨会话交接的当前快照**：接手的人先读它，就能知道现在做到哪、下一步该干什么、
哪些决定已经定了不必重问、哪些路已经走不通不必再试。

写作日期：**2026-08-22**，对应 `main` 提交 `08ee6fd`、最新正式版 **v4.25.26**。

**它不是正式来源。** 每类长期信息的主要正式来源见 [`PROJECT.md`](PROJECT.md) 的
「正式来源与文档职责」；本文件只做摘要与指路。两者冲突时以那些文件与 GitHub Issue 为准。

**接手前必读**：[公开 Issue #63](https://github.com/Cr0ce11/sb-user-manager-public/issues/63)
的最后几条评论。那里是逐轮的进展续记，比本文件更细，也更新得更勤。

---

## 一、当前任务和进度

### ⚠️ `main` 上有已合并、**尚未发布**的改动，其中一条是 P1 修复

v4.25.26 发布之后又合入了这些，服务器上**还拿不到**：

| 提交 | 内容 | 影响运行脚本 |
|---|---|---|
| `08ee6fd` (#244) | **修复「安装或修复环境」的自动修复分支在 mihomo 上必定失败**（[#242](https://github.com/Cr0ce11/sb-user-manager-public/issues/242)） | **是（P1）** |
| `b9518b9` (#243) | 内核无关路径不再写死 sing-box 的文案与文档 | 是（仅文案） |
| `0b95166` (#241) | sing-box 线归档：删测试机、同步记录 | 否 |
| `a626885` (#240) | 剥离仅供测试的 `SB_DEPLOY_PROXY_KERNEL`（[#227](https://github.com/Cr0ce11/sb-user-manager-public/issues/227)） | 是（无菜单入口，零用户影响） |
| `13daef0`／`40e1cb4`／`2ca3bf0` | 状态同步与 OPS-007 | 否 |

**项目所有者 2026-08-22 明确说「先不发」。** 下次发版时这些会一起带上。要点是：
`main` 与最新 Release 之间存在差异，**别把 v4.25.26 当成当前代码**。

### 正在做：sing-box 残留审查，三步走，做完第一步

正式环境全部搬到 mihomo 之后，对残留的 sing-box 代码／菜单／文案做了一次审查，
按「先说清事实、再动行为」分三步：

| 步骤 | 内容 | 状态 |
|---|---|---|
| 第一步 | 修文案与文档：内核无关路径不再写死 sing-box | **已完成**（#243） |
| 插队 | [#242](https://github.com/Cr0ce11/sb-user-manager-public/issues/242) 自动修复分支在 mihomo 上必定失败 | **已完成**（#244） |
| 第二步 | **撤掉两个永远点不动的菜单项**，连同底下无人可达的实现 | **待做** |
| 第三步 | 文件级默认值仍然是 sing-box | **待做** |

第二步与第三步的具体清单见下面第三节。

### 仓库与版本状态（本轮实读）

| 项 | 取值 |
|---|---|
| `main` | `08ee6fd` |
| 最新正式版 | **v4.25.26**（不可变 Release），直接代码回退点 v4.25.25 |
| 开放 Pull Request | 无 |
| 分支 | 只有 `main` |

### 正式业务环境

**五台正式服务器已全部升到 v4.25.26 并切换到 mihomo**（2026-08-22 完成，
[公开 Issue #217](https://github.com/Cr0ce11/sb-user-manager-public/issues/217) 已关闭）。
搬迁由项目所有者自己执行，仓库侧不登录正式服务器。

- 切换后「服务与配置检查」0 个问题，客户端无需改配置即可连接，sing-box 残留已清理。
- **正式环境从此没有 sing-box 在跑。**
- 服务器上的 v4.25.26 **不含** #242 的修复——那个入口在生产机上目前仍然不可用。

### 测试环境（本轮逐台实读）

| 机器 | 版本 | 内核 | 管理器数据目录 | 用户／分流 |
|---|---|---|---|---|
| `sbm-deb13` | 4.25.24 | mihomo | `/etc/sb-user-manager` | 0／0 |
| `sbm-mihomo` | 4.25.26 | mihomo | `/etc/sing-box`（2f 之前装的，目录仍是老名字） | 0／0 |
| `sbm-gate` | 未部署 | — | — | 只跑门禁，bash 5.2.15 |

两台部署机都有绕开宿主机代理 fake-IP DNS 的 `/etc/hosts` 条目
（`raw.githubusercontent.com` 指向 GitHub 真实地址）。

> ⚠️ **没有任何办法在真机上验证 sing-box 的行为。** 三台 sing-box 测试机已随
> sing-box 线归档删除；全新安装只装 mihomo，唯一的逃生口 `SB_DEPLOY_PROXY_KERNEL`
> 已剥离。真需要一台只能用 **v4.25.22 或更早的不可变 Release** 去装，装出来的是
> 旧版管理器，**不能用来验当前版本的行为**。sing-box 代码路径现在只剩
> `tests/test-unit.sh` 的夹具覆盖。

**机器版本一律以登机实读为准**，不要引用记录里的旧数字。曾经吃过一次：#63 的评论
把一台机器的版本记错两个小版本，登机才发现。

---

## 二、已完成事项

只列结构性的；逐版本的用户可感知变化见 [`CHANGELOG.md`](CHANGELOG.md)，
逐条事项见 [`TODO.md`](TODO.md)。

### 2026-08-22 这一天（未发布部分见第一节）

- **正式环境全部搬到 mihomo**，[#122](https://github.com/Cr0ce11/sb-user-manager-public/issues/122)
  的三步计划在生产也走完（#217）。
- **sing-box 线归档**：剥离 `SB_DEPLOY_PROXY_KERNEL`（#227），删除三台 sing-box 测试机。
  [#202](https://github.com/Cr0ce11/sb-user-manager-public/issues/202)（单开 sing-box 稳定线）
  的前提随之消失，其中定下的号段方案全部作废。
- **OPS-007**：实测「v4.25.16 一步直升 v4.25.26」，#217 的步骤单更新到实测过的版本号。
- **残留审查第一步 + #242**（见第一节）。

### v4.25.25 / v4.25.26

- v4.25.25：换内核与「清理 sing-box 残留」两处确认提示此前承诺「退路是从快照恢复」，
  而菜单里没有这个入口——改为指向服务器外的 `.sbm`，并在确认前当场清点
  （[ADR 0032](docs/DECISIONS/0032-environment-snapshot-serves-automatic-rollback-only.md)）。
  同版本新增门禁运行环境自检（[#167](https://github.com/Cr0ce11/sb-user-manager-public/issues/167)）。
- v4.25.26：geo 类别名的字符集漏了 `!`，`category-ai-!cn` 这类排除写法被拒绝
  （[#233](https://github.com/Cr0ce11/sb-user-manager-public/issues/233)）。

### 更早

接入第二内核 mihomo 的三步计划（v4.25.16–v4.25.23）、分流的三种规则来源（v4.25.24）、
公开仓库迁移、不可变 Release、完整卸载、备份保留与恢复演练。详见 `CHANGELOG.md`。

---

## 三、下一步计划

### 第二步：撤掉两个永远点不动的菜单项（待做）

「部署与卸载」里这两项**无条件显示**，而所有机器都是 mihomo，因此永远点不动：

| 菜单项 | 现在的结果 |
|---|---|
| **切换到 mihomo 内核** | 前置检查第一句是 `PROXY_KERNEL == singbox`，必定报「本机的代理内核不是 sing-box，无需切换」 |
| **清理 sing-box 残留** | 必定报「没有找到 sing-box 的残留文件，无需清理」 |

这与项目自己定的规矩冲突——「留一个永远点不动的菜单项比少一项更让人困惑」，当初撤
接管功能时正是按这条**整段删除**的。

底下无人可达的实现（`src/50-install-update.sh`）：

| 功能 | 规模 |
|---|---|
| sing-box 版本管理（通道切换，10 个函数） | 约 228 行 |
| 换内核到 mihomo（3 个函数） | 约 155 行 |
| 清理 sing-box 残留（3 个函数） | 约 78 行 |

合计约 460 行。「sing-box 版本管理」的菜单项已按内核隐藏，因此**永远不显示**，
但实现还在。`singbox_deployment_present`（7 行）是例外：它是「管理配置丢了怎么判」
的判据，逻辑本身仍该留着。

**动手前先想清楚**：删掉换内核之后，万一将来又要支持别的内核，这条路要重写；而
sing-box 侧已经没有真机可验，删除的回归证据只能来自单元测试夹具与 mihomo 侧对照。

### 第三步：文件级默认值仍然是 sing-box（待做）

`src/00-bootstrap.sh` 第 20 行 `PROXY_KERNEL="singbox"`、第 35 行
`MANAGER_DATA_DIR="/etc/sing-box"`（第 194、218 行还各有一次兜底赋值）。

这个默认值现在和现实完全相反，而且**已经出过一次事**：`resolve_deployment_kernel`
的注释里记着，一台 mihomo 机器执行「自动修复缺失内容」曾经去下载并部署了 sing-box，
所以那里必须显式定内核、不能依赖文件级默认。**改它要配回归测试**，
单元测试里已有一条「干净机器必须装 mihomo，且不受调用者残留的 `PROXY_KERNEL` 影响」
可以作为起点。

### 发版

`main` 上积了几笔未发布改动，其中 #242 是 P1。项目所有者说先不发；
下次发版时把第二、三步一起带上，或者单独为 #242 发一版——由项目所有者定。

### 其余开放项

| 事项 | 状态 |
|---|---|
| [#187](https://github.com/Cr0ce11/sb-user-manager-public/issues/187) 「验证分流是否生效」在 mihomo 上不可用 | 人肉兜底、不排期。该菜单项显示但必定拒绝，属**有意保留** |
| [#16](https://github.com/Cr0ce11/sb-user-manager-public/issues/16) Mihomo 格式的用户配置导出 | 候选 |
| `TODO.md` 的 MAINT-013 | 开发机 ShellCheck 比 CI 新，本地报 CI 不报的 SC2329 |
| README 其余约 30 处 sing-box 语境的说明 | 随第二步一起改，避免改两遍 |

---

## 四、已确定的技术决策及原因

长期决定以 [`docs/DECISIONS/`](docs/DECISIONS/) 为准。下面是接手后马上会用到的：

### 方向与范围

- **sing-box 线已归档**（2026-08-22 项目所有者决定）。新功能、新护栏只做 mihomo 侧。
- **代理协议只用 SS2022（可加 ShadowTLS）与 AnyTLS。** 不引入 Hysteria2、TUIC。
- **不再支持接管别人手工装的 sing-box。** 相关实现整段删除而不是把入口藏起来——
  留着一段谁都调不到的部署代码，迟早有人以为它还能用。**第二步要撤的两个菜单项
  正是按这条规矩处理。**
- **内核无关的路径不许写死内核名**，用 `kernel_display_name` 等适配层入口。
  值本来就走适配层时，写死的只会是标签，而标签错了同样是错的。

### 备份与退路（ADR 0032）

- **操作前的完整环境快照只服务于自动回滚，不做手工恢复入口。** 恢复整机快照会静静
  抹掉快照之后新增的用户、分流与累计用量。
- **任何不可逆操作之前必须先有一份复制到服务器之外的 `.sbm`。** 换内核与清理残留
  两处会当场清点并在没有备份时明确告知。

### 门禁

- **权威运行环境是 Linux 上的 Bash 5（`sbm-gate`）**，由 `tests/require-strict-errexit.sh`
  强制，开发机上会被直接拒绝。
- **不改写那 502 条裸 `[[ ]]` 断言**：它们在 Bash 5 上有效，问题出在运行环境。
- **新写的断言要用会自己报错的写法**，不要用裸 `[[ ]]`——后者失败时一个字都不打印。

### 分流的三种规则来源

网址、本机文件、GeoSite／GeoIP 类别，三种并存不是替换；项目所有者自己一律用网址。
远程规则集更新间隔固定一天、下载不走代理、文件名沿用地址里的原名、只支持 yaml、
保存那一刻由管理器自己下载校验一次（mihomo 取不到时只留一行警告、分流静静失效）。
geo 类别名交给 `mihomo -t` 当场校验；类别名允许 `!`（`category-ai-!cn` 这类排除写法）。

### 护栏设计

- **真正的敌人是「狼来了」，不是漏报。** 会自愈的事只标记、不计为问题。
- **凡是验证「某个限制生效」的断言，必须配一个对照**，证明限制之外的东西仍然正常。
- **可靠性链路上的单点不许记进欠账清单**，当场闭合。

### 发布与授权

- **发布正式版前停下来等项目所有者确认。**
- **创建 Release 不等于授权部署到正式环境。**
- **仓库侧不登录任何正式服务器。**
- **任何凭据都不写进报告。**
- **CHANGELOG 由发布准备 PR 统一写，功能 PR 不动它。**

---

## 五、踩过的坑和已否决的方案及原因

### 已否决的方案

| 方案 | 为什么否决 |
|---|---|
| 给完整环境快照加「从操作前快照恢复」的菜单入口 | 会静静抹掉快照之后的用户、分流与用量；要安全必须先有差异预检，那是这项功能里最大的一块。快照还含凭据。详见 ADR 0032 |
| 把 502 条裸 `[[ ]]` 断言全部改写 | 它们在 Bash 5 上有效，问题出在运行环境。一次覆盖 8 个文件的无差别批量替换，改错了单元测试自己不会告诉你 |
| 给生产线单开一条只修 bug 的 sing-box 稳定线 | 前提（生产全是 sing-box）已消失，[#202](https://github.com/Cr0ce11/sb-user-manager-public/issues/202) 里定下的号段方案全部作废 |
| 把「逃生口」用例改造后保留 | 改造之后与紧邻的第三条完全重复，因此并入而不是留一份一模一样的副本 |
| 在 jq 里用 `contains` 认一条 mihomo 规则行属于哪条分流 | `OR,((GEOSITE,a),(GEOIP,us))` 会被条件 `GEOSITE,a` 认领。改成连分隔符一起比 |
| 骨架里无条件写那三个 geo 键 | 实测：一条 geo 规则都没有时不会下载数据库，但每次启动留一行 error 级日志。改成按需写 |
| 用 `openssl s_client` 的退出码判断对方说不说 TLS 1.3 | 证书验证失败时它也非零退出。要看 `-brief` 输出里的 `Protocol version` |

### 踩过的坑（会再遇到的）

1. **护栏可能把缺陷一起钉住。** #242 那处写死的 sing-box 配置路径，静态门禁里**正好
   有一条断言钉着它**，而且没有注释说明想保护什么。任何人想改对这段代码，门禁都会
   拦下来。**写结构性断言时必须写清它保护的是哪个不变量**，否则它保护的就只是「当前
   长这样」。

2. **跟在 `if`／`!`／`&&`／`||` 后面的命令，其整个动态调用范围内 `set -e` 都不生效。**
   写门禁探针时撞过：`probe="$(set -e; [[ 0 == 1 ]]; printf REACHED)" || true` 里的
   `|| true` 让 errexit 连命令替换那层子进程都不生效，探针在 Bash 5 上也返回 `REACHED`。
   同一条规则的另一个后果：从菜单进去的 `cmd_*` 内部的 `return 1` 不会中断流程。

3. **裸 `[[ ]]` 断言失败时一个字都不打印。** 本轮又撞到一次：单元测试退出码 1、
   没有任何输出，只能 `bash -x` 重跑再看 trace 的最后几行。定位手法记住：
   `bash -x tests/test-unit.sh 2>/tmp/x.log >/dev/null; tail -25 /tmp/x.log`。

4. **OrbStack 的共享文件系统偶尔让客机读到半份或旧的文件。** 本轮撞到两次，症状是
   「生成脚本语法检查失败」而本机文件完好。**先比对摘要再重试，不要当成代码缺陷去查。**

5. **`$(...)` 会在 `gh pr create --body "..."` 里被 shell 执行。** 本轮 PR 正文里的
   `$(kernel_display_name)` 被求值成空，正文里留了个空格子。与「提交说明用 `-F` 文件传」
   是同一类坑——**正文一律用 `--body-file`**。

6. **`cp` 不还原可执行位。** 临时破坏后用 `cp` 还原，提交里混进过一次 mode change。
   提交前用 `git status --short` 看一眼。

7. **`sb-user-expiry.timer` 每 15 分钟触发一次，会拿走管理锁。** 跑 `lifecycle` 前先看
   `systemctl list-timers`。注意**等它「触发」还不够**——要等 `sb-user-expiry.service`
   变回 `inactive`，否则照样撞上「存在其他管理操作」。

8. **升级本身不改运行配置，但升级后第一次重建配置时它的摘要会变一次。** 差异是
   sing-box 自己的规范化（省略 `"format": "binary"` 与 `"action": "route"`，
   `"24h"` → `"24h0m0s"`）。把升级前的配置喂给 `sing-box format`，输出与重建后逐字节相同。

9. **用 `cp` 直装脚本到测试机，`audit` 会报两项假失败**：「版本记录一致」和
   「root 启动副本一致」。真实更新路径会同步 `/var/lib/sb-user-manager/versions` 与
   `/root/sb-user-manager.sh`。**不要当成缺陷去查。**

10. **宿主机的代理客户端工作在 fake-IP 模式**，会把公网域名解析成 `198.18.x.x`，
    于是「地址必须解析到公网」这条校验判它是内网而拒绝——**而手动 `curl` 却是通的**。
    做法是给规则集域名写 `/etc/hosts` 条目。

11. **文档里靠人记得的那一行迟早会漂。** `PROJECT.md` 的「最新正式版」从 v4.25.19
    一路停到 v4.25.25 才被发现。现在由静态检查与 `SCRIPT_VERSION` 绑住。
    **遇到同类「必须一起改」的两处，直接加断言。**

### 环境与流程

- **门禁在门禁机上跑**：`orb -m sbm-gate bash tests/test-static.sh`。
- **门禁 12 条不含 ShellCheck**（门禁机没装），跑完在开发机补跑 CI 里那两条；
  第二条会报 CI 不报的 SC2329（本机版本更新），见 MAINT-013。
- **`tests/test-public-snapshot.sh` 要求工作区干净**，必须提交之后才能跑。
- **还原临时破坏先 `cp` 一份**，不要用 `git checkout <文件>`。
- **提交前跑 `git status --short` 和 `git diff --stat` 逐处确认**；提交说明用 `-F 文件` 传。
- **改 `src/` 之后要跑 `bash tools/build-manager.sh`** 重新生成单脚本，否则门禁会说
  生成物与源码不一致。

### 怎么跑「真实菜单更新路径」

发布后要在测试机上实跑一次。菜单是编号式的，可以用伪终端驱动：起一个 `pty.fork()`，
读屏幕、用正则找出「系统管理」「检测更新」各自的编号发过去，再对 `[y/N]` 发 `y`。
上一轮实际发的按键是 `3` → `2` → `y`。

**驱动器要注意两点**：按键发完之后**不能因为没输出就退出**——安装要下载内核和 Nfuse，
中间会长时间静默；应当一直等到完成标记命中或总超时。跑之前先把机器退回上一版的
**已安装脚本、版本记录和 root 启动副本**三处，否则脚本会认为已是最新而不走下载。
