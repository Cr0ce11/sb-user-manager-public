# 交接文档

本文件是**跨会话交接的当前快照**：接手的人先读它，就能知道现在做到哪、下一步该干什么、
哪些决定已经定了不必重问、哪些路已经走不通不必再试。

写作日期：**2026-08-22**，对应 `main` 提交 `4b8c9c2`、正式版 **v4.25.25**。

**它不是正式来源。** 每类长期信息的主要正式来源见 [`PROJECT.md`](PROJECT.md) 的
「正式来源与文档职责」；本文件只做摘要与指路，不重复维护路线图、门禁清单或操作步骤。
两者冲突时以那些文件与 GitHub Issue 为准。

**接手前必读**：[公开 Issue #63](https://github.com/Cr0ce11/sb-user-manager-public/issues/63)
的最后几条评论。那里是逐轮的进展续记，比本文件更细，也更新得更勤。

---

## 一、当前任务和进度

### 没有已排期的开发任务，但有一件事排在「动正式机」之前

上一轮（v4.25.25）把手上的活全部做完并发布了。**唯一有先后顺序要求的是
[`TODO.md`](TODO.md) 的 OPS-007**：

> 实测「v4.25.16 一步直升到最新正式版」，并把
> [公开 Issue #217](https://github.com/Cr0ce11/sb-user-manager-public/issues/217)
> 的步骤单更新到实测过的版本号。

原因：#217 是项目所有者操作正式机时**照着走的那份步骤单**，它第 3 步写的还是
「升级到 v4.25.23」，实测过的也只是 4.25.16 → 4.25.23。v4.25.24 与 v4.25.25 发布之后
这份单子就落后了。正式机现在停在 v4.25.16，照着一份没验过的版本号操作是不必要的风险。

做法写在 OPS-007 的备注里：在测试机上造一台 4.25.16 的机器，走真实菜单更新路径直升到
最新正式版，核对已安装脚本与 Release 附件逐字节一致、`audit` 失败项 0、单元无漂移，
再跑一次含分流的 `lifecycle`；然后把 #217 的版本号与实测结论一并更新。

### 仓库与版本状态（本轮实读）

| 项 | 取值 |
|---|---|
| `main` | `4b8c9c2`（同步 v4.25.25 发布完成状态 (#231)） |
| 最新正式版 | **v4.25.25**（不可变 Release，`immutable=true`） |
| 直接代码回退点 | **v4.25.24** |
| 开放 Pull Request | 无 |
| 分支 | 只有 `main` |

### 正式业务环境

正在从 sing-box 搬到 mihomo，**由项目所有者自己执行**，仓库侧不登录正式服务器。

- `turin` 已经搬完，切换成功、客户端可用；sing-box 残留还没清，处于观察期。
- 其余服务器仍停在 **v4.25.16**。
- 每搬完一台，结果记在
  [公开 Issue #217](https://github.com/Cr0ce11/sb-user-manager-public/issues/217)。
- **动手之前先做完 OPS-007**（见上）。

### 测试环境（本轮逐台实读）

开发机上的五台 OrbStack 机器（能力与未覆盖项见
[公开 Issue #139](https://github.com/Cr0ce11/sb-user-manager-public/issues/139)）。

| 机器 | 版本 | 内核 | 管理器数据目录 | 用户／分流 | 说明 |
|---|---|---|---|---|---|
| `sbm-deb12` | **4.25.25** | sing-box | `/etc/sing-box` | 1／0 | 上一轮通过真实菜单更新路径升上来的 |
| `sbm-deb12b` | **4.25.23** | sing-box | `/etc/sing-box` | 1／0 | 换内核演练机，已用快照还原；上两轮都没动它 |
| `sbm-deb13` | **4.25.24** | mihomo | `/etc/sb-user-manager` | 0／0 | 2f 全新安装出来的机器；上一轮没升 |
| `sbm-mihomo` | **4.25.25** | mihomo | `/etc/sing-box` | 0／0 | 2f 之前装的，目录仍是老名字 |
| `sbm-gate` | 未部署 | — | — | — | 只跑本地门禁 |

`sbm-deb12` 与 `sbm-deb12b` 的管理配置里**没有** `PROXY_KERNEL` 这一行——它们是内核声明
之前装的存量机器，内核由 `singbox_deployment_present` 判出来。这不是缺陷。

**四台部署机上都有一条绕开宿主机代理 fake-IP DNS 的 `/etc/hosts` 条目**
（`raw.githubusercontent.com` 指向 GitHub 的真实地址）。含远程规则集的验收依赖它，
详见第五节。

**一处历史记录的错误已经查实**：[Issue #63](https://github.com/Cr0ce11/sb-user-manager-public/issues/63)
的两条评论把 `sbm-deb12b` 记成 4.25.21，本轮登机实读确认**是 4.25.23**。
机器版本以登机实读为准，不要引用 Issue 里的旧数字。

---

## 二、已完成事项

按时间倒序，只列结构性的几件；逐版本的用户可感知变化见
[`CHANGELOG.md`](CHANGELOG.md)，逐条事项与状态见 [`TODO.md`](TODO.md)。

### v4.25.25 —— 把两处确认提示指向的退路改准

- **换内核与「清理 sing-box 残留」两处确认提示，此前承诺了一条并不存在的路**
  （[#207](https://github.com/Cr0ce11/sb-user-manager-public/issues/207)）：
  它们说「真出问题时的退路是从切换前的完整环境快照整体恢复」，而菜单里没有手工恢复
  快照的入口。改为指向服务器外的 `.sbm` 加密迁移备份，并在确认之前当场清点本机 `.sbm`
  的份数与最新一份；一份都没有时明确告知没有退路。脚本不阻止操作。
- 同一件事的决定写成
  [ADR 0032](docs/DECISIONS/0032-environment-snapshot-serves-automatic-rollback-only.md)：
  **快照只服务于自动回滚，不提供手工恢复入口**。
- **门禁运行环境自检**（[#167](https://github.com/Cr0ce11/sb-user-manager-public/issues/167)）：
  新增 `tests/require-strict-errexit.sh`，十个门禁脚本开头各 source 一次，
  在不会因求值为假的 `[[ ]]` 而中止的 Bash 上直接拒绝运行。不改写那 502 条裸断言。
- **MAINT-012 的记录收口**：`SB_DEPLOY_PROXY_KERNEL` 的剥离条件与逐处步骤写进
  [#227](https://github.com/Cr0ce11/sb-user-manager-public/issues/227)，代码一行未动。
- 清掉一处文档旧账：`PROJECT.md` 的「最新正式版」一行停在 v4.25.19 连着五个版本，
  本次改正并由静态检查与 `SCRIPT_VERSION` 绑住。

### v4.25.24 —— 分流的两种新规则来源

网址来源的规则集（[#218](https://github.com/Cr0ce11/sb-user-manager-public/issues/218)）
与 GeoSite／GeoIP 类别（[#219](https://github.com/Cr0ce11/sb-user-manager-public/issues/219)），
另加「立即更新规则集」「geo 数据源」两个入口与来源漂移审计。

### v4.25.16 – v4.25.23 —— 接入第二内核（mihomo），三步计划走完

- 第一步：一台部署声明自己用哪个内核（v4.25.16）。
- 第二步 2a–2f：安装启停、用户入口、分流生成、审计、全新安装只装 mihomo
  （[#157](https://github.com/Cr0ce11/sb-user-manager-public/issues/157) 已关闭）。
- 第三步：把已有 sing-box 部署整机切换到 mihomo（v4.25.22，
  [#203](https://github.com/Cr0ce11/sb-user-manager-public/issues/203)）。
- 管理器自身数据搬出 `/etc/sing-box`：新装机器用中立目录 `/etc/sb-user-manager`，
  **存量机器不搬家**（[#172](https://github.com/Cr0ce11/sb-user-manager-public/issues/172)
  三步走的第二步）。

### 更早

公开仓库迁移与匿名更新、不可变 Release、完整卸载、备份保留与恢复演练、
内核适配层（`src/05-kernel.sh`）的抽出与收尾。详见 `CHANGELOG.md`。

---

## 三、下一步计划

| 顺序 | 事项 | 归属 | 说明 |
|---|---|---|---|
| **1** | **OPS-007：实测 v4.25.16 直升最新版，更新 #217 的步骤单** | 仓库侧可做 | **排在正式机动手之前**；理由见第一节 |
| 2 | 正式服务器搬到 mihomo | 项目所有者执行 | [#217](https://github.com/Cr0ce11/sb-user-manager-public/issues/217)；机器要先升到 v4.25.22 或更新 |
| — | MAINT-013：开发机 ShellCheck 比 CI 新，本地报 CI 不报的 SC2329 | P3 待办 | 使「补跑 ShellCheck」永远带噪声，久了会被略过 |
| — | 完整环境快照没有手工恢复入口 | **已明确不做** | [ADR 0032](docs/DECISIONS/0032-environment-snapshot-serves-automatic-rollback-only.md)；不要再当作待补的功能 |
| — | 生产线是否单独走一条只修 bug 的 sing-box 稳定线 | **暂缓中** | [#202](https://github.com/Cr0ce11/sb-user-manager-public/issues/202)；项目所有者已叫停，**不要照着它开工** |
| — | 剥离仅供测试的 `SB_DEPLOY_PROXY_KERNEL` | 等条件 | [#227](https://github.com/Cr0ce11/sb-user-manager-public/issues/227)；条件是存量机器全搬完、sing-box 线归档 |
| — | 「验证分流是否生效」在 mihomo 上不可用 | 人肉兜底、不排期 | [#187](https://github.com/Cr0ce11/sb-user-manager-public/issues/187) |
| — | Mihomo 格式的用户配置导出 | 候选 | [#16](https://github.com/Cr0ce11/sb-user-manager-public/issues/16) |

长期方向是**全面转向 mihomo**（[#172](https://github.com/Cr0ce11/sb-user-manager-public/issues/172)）；
项目所有者提过 mihomo 版完全稳定之后可以把 sing-box 版归档。

---

## 四、已确定的技术决策及原因

长期有效的产品与技术决定以 [`docs/DECISIONS/`](docs/DECISIONS/) 为准。
下面这些是近期定下、接手后马上会用到的：

### 方向与范围

- **新功能、新护栏默认只做 mihomo 侧。** 项目所有者原话：「这条不做，我都要放弃
  singbox 了」。sing-box 只维持现状——既有行为不许回归，但不加新东西。
- **代理协议只用 SS2022（可加 ShadowTLS）与 AnyTLS。** 不引入 Hysteria2、TUIC
  这类 UDP 系协议。
- **不再支持接管别人手工装的 sing-box。** 相关实现整段删除而不是把入口藏起来：
  留着一段谁都调不到的部署代码，迟早有人以为它还能用。

### 备份与退路（v4.25.25 定下，ADR 0032）

- **操作前的完整环境快照只服务于自动回滚，不做手工恢复入口。** 恢复整机快照会静静
  抹掉快照之后新增的用户、分流与累计用量；要安全就得先有一份「这次会丢什么」的差异
  预检，那是这项功能里最大的一块工作。快照里还装着用户资料与 AnyTLS 私钥，做成可
  浏览可选择的界面等于给凭据多开一条出口。
- **因此任何不可逆操作之前必须先有一份复制到服务器之外的 `.sbm`。** 这条不能只写在
  文档里——真需要退路的那一刻，人已经站在确认提示前面了。换内核与清理残留两处会
  当场清点并在没有备份时明确告知。
- **重新评估的条件**写在 ADR 0032 末尾：真发生一次「操作成功、事后反悔、当时没有
  `.sbm`」，或出现第二个「`.sbm` 是唯一退路」的不可逆操作，或审计侧已经能算出两个
  状态之间的差异。

### 门禁

- **门禁的权威运行环境是 Linux 上的 Bash 5（`sbm-gate`），不是开发机。**
  裸 `[[ ]]` 断言只在会因它中止的 Bash 上才有效；开发机的 Bash 3.2 上它们全部空转。
  这一条现在由 `tests/require-strict-errexit.sh` 强制，不再靠人记得。
- **不改写那 502 条裸断言。** 它们在 Bash 5 上是有效的，问题出在运行环境；
  一次覆盖 8 个文件的批量替换风险大于收益。

### 分流的三种规则来源

- **三种来源并存，不是替换**：网址、本机文件、GeoSite／GeoIP 类别。
  项目所有者自己一律用网址。
- **远程规则集的更新间隔固定一天**，不给使用者选——理由是保证规则的时效性。
- **下载不走代理**，服务器默认都能访问 GitHub。
- **下载下来的文件保持地址里的原有命名**，便于自己识别。因此名字不合法时明确拒绝
  而不是悄悄改名；两个不同地址算出同一个文件名时拒绝，并指出被哪一条分流占用了。
- **只支持 yaml 格式的远程规则集**，`.list`／`.txt`／`.mrs` 一律拒绝。
- **保存那一刻由管理器自己下载一次并校验。** 这条非做不可：mihomo 取不到远程规则集
  且本地没有缓存时只留一行警告、服务照常起来，**那条分流静静地完全不生效**。
- **geo 类别的类别名交给 mihomo 当场校验**（保存时跑一次 `mihomo -t`）。geo 不会静默
  失效：类别名写错、数据库缺了，mihomo 直接拒绝加载配置。代价是数据库丢了且当时
  下不到时服务起不来——这个取舍是明知并接受的。
- **`geodata-mode`／`geo-auto-update`／`geo-update-interval` 只在配置里真的有 geo 规则
  时才写**，最后一条 geo 规则没了之后由管理器收回。理由见第五节的实测。
- **「立即更新规则集」接受几秒断线**：更新后重启内核让新规则立刻生效，并明确告知。

### 护栏与告警的设计原则

- **真正的敌人是「狼来了」，不是漏报。** 会自愈的事只标记、不计为问题。
- **凡是验证「某个限制生效」的断言，必须配一个对照**，证明限制之外的东西仍然正常。
- **新增护栏之后，先看它在「什么都正常」的夹具上说不说话**，再看它该报的时候报不报。
- **可靠性链路上的单点不许记进欠账清单**，当场闭合。
- **新写的断言要用会自己报错的写法**，不要用裸 `[[ ]]`——后者失败时一个字都不打印。

### 发布与授权

- **发布正式版前停下来等项目所有者确认**，即使前面的开发工作已经批准。
- **创建 Release 不等于授权部署到正式环境**，正式环境上线要单独授权。
- **仓库侧不登录任何正式服务器。** 正式机器的操作由项目所有者执行，我读输出。
- **任何凭据都不写进报告**（密码、PSK、token、密钥）；需要时给一条他自己去取的命令。
- **CHANGELOG 由发布准备 PR 统一写，功能 PR 不动它。**

---

## 五、踩过的坑和已否决的方案及原因

### 已否决的方案

| 方案 | 为什么否决 |
|---|---|
| 给完整环境快照加一个「从操作前快照恢复」的菜单入口 | 恢复整机快照会静静抹掉快照之后新增的用户、分流与累计用量，而使用者在确认提示上看不到自己会丢什么；要安全必须先有差异预检，那是这项功能里最大的一块。快照还含凭据。替代路径 `.sbm` 是通的。详见 ADR 0032 |
| 把 `tests/` 下那 502 条裸 `[[ ]]` 断言全部改写成会报错的写法 | 它们在 Bash 5 上是有效的，问题出在运行环境。一次覆盖 8 个文件的无差别批量替换，改错了单元测试自己不会告诉你。改为在门禁开头自检运行环境 |
| 骨架里**无条件**写那三个 geo 键 | 实测：写上去而一条 geo 规则都没有时，**不会**下载任何数据库，但每次启动都留一行 error 级日志说 `GeoSite.dat` 找不到；对照——不写这几个键时同一份配置连这行都没有。改成按需写 |
| 按「管理器有没有 geo 分流」决定写不写那几个键 | 使用者自己写在顶层 `rules` 里的 `GEOSITE` 规则同样需要它们，按这个判断会把他的设置删掉。改成按「**配置里**有没有 geo 规则」判断 |
| 在 jq 里用 `contains` 认一条 mihomo 规则行属于哪条分流 | 一条 `OR,((GEOSITE,a),(GEOIP,us))` 的行会被条件 `GEOSITE,a` 认领，而那是别人的行。改成**连分隔符一起比** |
| 按「规则集标签」认行 | geo 分流根本没有规则集标签。改成按**匹配条件**认 |
| 把「把握手目标指向不说 TLS 的服务」当作 ShadowTLS 严格模式的兜底 | 实测不区分。真正的行为是：握手目标不支持 TLS 1.3 时，开着严格模式的监听器拒绝承载代理 |
| 用 `openssl s_client` 的**退出码**判断对方说不说 TLS 1.3 | 证书验证失败时它也非零退出。要看 `-brief` 输出里协商出来的 `Protocol version` |
| 把「没有管理配置」一律当成干净机器 | 「安装或修复环境」的修复分支会因此把一台 sing-box 机器修成 mihomo |

### 踩过的坑（会再遇到的）

1. **跟在 `if`／`!`／`&&`／`||` 后面的命令，其整个动态调用范围内 `set -e` 都不生效。**
   这条坑本项目已经吃过两次，第二次是在写门禁自检的探针时：
   ```bash
   probe="$(set -e; [[ 0 == 1 ]]; printf REACHED)" || true   # 错
   ```
   `|| true` 让整条命令落进条件上下文，errexit **连命令替换那一层子进程都不生效**，
   探针在 Bash 5 上也返回 `REACHED`，自检因此永远不报。正确写法是用 `if` 包住并让
   子进程自己带 `set -Eeuo pipefail`。同一条规则的另一个后果：从菜单进去的那些
   `cmd_*`，内部任何一句 `return 1` 都不会中断流程，必须每个调用点自己 `|| return 1`。

2. **用 `cp` 把分支脚本直装到测试机，`audit` 一定会报两项假失败**：
   「版本记录一致」和「root 启动副本一致」。真实更新路径会同步
   `/var/lib/sb-user-manager/versions` 与 `/root/sb-user-manager.sh`，手工 `cp` 不会。
   要么把这两处一起对齐，要么直接走真实更新路径——**不要把它们当成缺陷去查**。

3. **裸 `[[ ]]` 断言失败时一个字都不打印。** 本轮新加的一条静态断言就是这样写的，
   破坏它之后只看到一个退出码 1，什么原因都没有。新写断言一律用
   `if ...; then echo '说明为什么这是错的' >&2; exit 1; fi`。

4. **只要 mihomo 的配置里有任何一条规则集条目（本机文件来源也一样），
   它启动时就会自己去下一个 12 MB 的 `ASN.mmdb`。** 因此
   [#165](https://github.com/Cr0ce11/sb-user-manager-public/issues/165)
   那条「不下载任何 geo 数据库」的断言要读作
   「**没有任何分流**的 mihomo 机器一个都不下」。

5. **宿主机的代理客户端工作在 fake-IP 模式**，会把公网域名解析成 `198.18.x.x`，
   于是「地址必须解析到公网」这条校验判它是内网而拒绝——**而手动 `curl` 却是通的**。
   做法是给规则集域名写 `/etc/hosts` 条目。同一个原因还会让「探一个公网域名的关闭
   端口」得到「TCP 居然通了」的结果；要验「连不上」，用本机闭端口（`127.0.0.1:9`）。

6. **升级不会刷新 systemd 单元。** 改了单元内容的版本升上来之后，审计一定会
   报一次单元不一致。这是设计如此，跑一次自动修复即可。

7. **未定义变量会让整个进程当场退出，回滚都来不及跑。** 兜底是启动时的环境事务恢复。
   新写改环境的流程时，先确认要用的全局变量都由谁填。

8. **测试夹具会随默认值改变而失去意义。** 改默认值时把「不给配置文件」的夹具全找一遍。

9. **手工往状态文件里造存量形状时，用户对象上的镜像字段也要一起改。**
   `validate_state_user_endpoints` 要求 `.transport`／`.shadowtls_password`／
   `.shadowtls_sni` 与 `endpoints[0]` 一致。

10. **`sb-user-expiry.timer` 每 15 分钟触发一次，会拿走管理锁。** 跑 `lifecycle` 前
    先看一眼 `systemctl list-timers sb-user-expiry.timer`，避开触发时刻。

11. **文档里靠人记得的那一行迟早会漂。** `PROJECT.md` 的「最新正式版」表格行从
    v4.25.19 一路停到 v4.25.25 才被发现，而同一文件正文里那句「现有最新完整正式版
    为 …」五次发布准备都改对了——差别只在于后者每次都被顺手改到。现在这一行由静态
    检查与 `SCRIPT_VERSION` 绑住。**遇到同类「必须一起改」的两处，直接加断言。**

### 环境与流程上的几条

- **本地门禁必须在 Linux bash 5 上跑**（`sbm-gate`）：
  `orb -m sbm-gate bash tests/test-static.sh`。现在门禁脚本自己会拒绝在开发机上跑。
- **本地门禁 12 条不含 ShellCheck**（门禁机没装），跑完要在开发机补跑 CI 里那两条。
  第二条会报 CI 不报的 SC2329（本机 shellcheck 0.11.0 比 CI 新），见 MAINT-013。
- **`tests/test-public-snapshot.sh` 要求工作区干净**，必须在提交之后才能跑。
- **每次往测试机装脚本前后都比对摘要，并带重试**：OrbStack 的共享文件系统偶尔会让
  客机读到半份或旧的文件。本轮撞到一次——静态检查报「生成脚本语法检查失败」，
  而本机文件完好；比对摘要一致后重试即通过。**遇到这种失败先比摘要再重试，
  不要当成代码缺陷去查。**
- **还原临时破坏先 `cp` 一份**，不要用 `git checkout <文件>`——那会把工作树里别人的
  改动一起抹掉。另外 `cp` 不还原可执行位，破坏前后用 `git status` 看一眼有没有
  多出 mode change。
- **提交前跑 `git status --short` 和 `git diff --stat` 逐处确认**改动都是自己这轮做的；
  提交说明用 `-F 文件` 传，不要用 `-m` 带反引号（shell 会把它当命令替换执行）。
- **迭代测试时把新增那一节单独抽出来跑**，全套单元测试留到最后一次。
- **需要 mihomo 的字段名时，从二进制里的结构体标签一次挖出来**；这招对规则类型无效。

### 怎么跑「真实菜单更新路径」

发布后要在测试机上实跑一次真实菜单更新。菜单是编号式的，可以用伪终端驱动：
起一个 `pty.fork()`，读屏幕、用正则找出「系统管理」「检测更新」各自的编号发过去，
再对 `[y/N]` 发 `y`。上一轮两台机器实际发的按键就是 `3` → `2` → `y`。

跑之前先把机器退回上一版的**已安装脚本、版本记录和 root 启动副本**三处，
否则脚本会认为已经是最新而不走下载。跑完核对已安装脚本与 Release 附件
逐字节一致，再跑 `release` 模式复检。
