# 交接文档

本文件是**跨会话交接的当前快照**：接手的人先读它，就能知道现在做到哪、下一步该干什么、
哪些决定已经定了不必重问、哪些路已经走不通不必再试。

写作日期：**2026-08-22**，对应 `main` 提交 `0aa9474`、最新正式版 **v4.25.29**。

**它不是正式来源。** 每类长期信息的主要正式来源见 [`PROJECT.md`](PROJECT.md) 的
「正式来源与文档职责」；本文件只做摘要与指路。两者冲突时以那些文件与 GitHub Issue 为准。

**接手前必读**：[公开 Issue #63](https://github.com/Cr0ce11/sb-user-manager-public/issues/63)
的最后几条评论。

---

## 一、当前任务和进度

### 仓库与版本状态（本轮实读）

| 项 | 取值 |
|---|---|
| `main` | `0aa9474`，与 `origin/main` 一致，工作区干净 |
| 最新正式版 | **v4.25.29**（不可变 Release），直接代码回退点 v4.25.28 |
| 开放 Pull Request | **无** |
| 分支 | 远端只剩 `main`，历史遗留分支已全部清掉 |
| 生成物 | `build-manager.sh --check` 通过，摘要 `22685075…`，与 Release 附件逐字节一致 |

### 正式业务环境

**五台正式服务器（Air、Pro、Malibu、HKRFC、JPRFC）已全部升级到 v4.25.29**
（[公开 Issue #273](https://github.com/Cr0ce11/sb-user-manager-public/issues/273)），
由项目所有者自己执行。

- 全部在 mihomo 上，**正式环境没有 sing-box 在跑**。
- `Air`（主机名 `turin`）是**唯一用了 GeoSite／GeoIP 类别分流**的机器，那条分流叫 `AI`。
- 仓库侧按惯例不登录正式服务器；唯一例外是本轮排查 P1 时经项目所有者当面授权对 `Air`
  做过**只读**诊断与只读复核，未安装任何未发布的脚本。

### 没有已知的 P0 或 P1 缺陷，也没有进行中的开发任务

这一轮把 sing-box 残留审查三步走全部走完，并顺带修掉了一个由正式服务器暴露的 P1。
接手时是一个干净的起点。

### 测试环境（本轮逐台实读）

| 机器 | 版本 | 内核 | 配置里有 `MANAGER_DATA_DIR` | 内容 |
|---|---|---|---|---|
| `sbm-deb13` | 4.25.29 | mihomo | **有**（中立目录 `/etc/sb-user-manager`） | 零用户零分流 |
| `sbm-mihomo` | 4.25.29 | mihomo | **没有**（靠兜底落在 `/etc/sing-box`） | 零用户零分流 |
| `sbm-gate` | 未部署 | — | — | 只跑本地门禁 |

两台部署机的已安装脚本摘要都是 `22685075…`，与 Release 附件一致。

- **`sbm-mihomo` 那个「没有 `MANAGER_DATA_DIR` 行」的形状是宝贵夹具，别顺手补上。**
  它代表存量机器：用户资料与内部备份由配置里的 `STATE_FILE`／`BACKUP_DIR` 指向老目录，
  而 **AnyTLS 证书目录只能由 `MANAGER_DATA_DIR` 派生**（实测在 `/etc/sing-box/cert`）。
  数据目录搬家（[#172](https://github.com/Cr0ce11/sb-user-manager-public/issues/172)）
  的正确性全靠这台机器验。
- **`sbm-deb13` 的 `/var/lib` 700 夹具已经用掉了**（本轮验证 #252 时修成了 755），
  它不再是那个缺陷的现成夹具。
- 仍然**没有任何办法在真机上验证 sing-box 的行为**；该代码路径只剩单元测试夹具覆盖。
- **测试环境里没有一台常备带 geo 类别分流的机器**，但 `lifecycle` 验收现在会自己造一条
  再清理掉（见第二节）。

**机器版本一律以登机实读为准**，不要引用记录里的旧数字。

---

## 二、已完成事项

只列结构性的；逐版本的用户可感知变化见 [`CHANGELOG.md`](CHANGELOG.md)，逐条事项见 [`TODO.md`](TODO.md)。

### sing-box 残留审查三步走：**全部走完**

| 步 | 内容 | 去向 |
|---|---|---|
| 一 | 内核无关的文案与文档不再写死 sing-box | v4.25.27（#243） |
| 二 | 撤除「切换到 mihomo 内核」「清理 sing-box 残留」「sing-box 版本管理」三项 | v4.25.28（[#256](https://github.com/Cr0ce11/sb-user-manager-public/issues/256)／PR #257），净删 2247 行 |
| 三 | 文件级默认值不再假装自己是 sing-box | v4.25.29（[#262](https://github.com/Cr0ce11/sb-user-manager-public/issues/262)／PR #263） |

第二步**入口与实现整段删除**，并由静态断言钉住不得复活（`rollback_switch`／
`rollback_cleanup`／`rollback_channel_switch`／`singbox_channel_menu`／
`print_offsite_recovery_status`），配对照（geo 数据源仍按内核显示）证明断言不是因为
整个菜单都没了才通过。

**保留下来的三样，别再当成孤儿删掉**：

1. `singbox_deployment_present` —— 管理配置丢失时判断这台机器原本是什么的判据。
2. `check_rule_set_with_binary` 与 `kernel_rule_set_compile`／`decompile` ——
   交接文档当初估计它们会变成孤儿，**实际不会**：sing-box 机器保存分流时那道校验仍在调用。
3. **「不把测试版静默换成正式版」这条保护** —— 判据收敛成 `singbox_preview_deployed`
   （sing-box 测试版版本号带 `-`），两条保留现有部署的流程都必须先问它，静态门禁看守。

### v4.25.28

- 第二步的撤除（上表）。
- **[#252](https://github.com/Cr0ce11/sb-user-manager-public/issues/252)**：系统目录权限修复
  挂进 `run_standalone_interactive_startup`。升级完成 `exec` 进新脚本时就自动修回 755，
  不再需要人工点一次「安装或修复环境」。**加了一道门槛**：只在管理配置存在（本脚本部署过
  的机器）时才修——那是「运行脚本就会走到」的位置，不设门槛的话，别人只是下载下来打开菜单
  看看，脚本就动了他的系统目录权限。

### v4.25.29

- 第三步（上表）：文件级默认值改成「尚未解析」哨兵，提前使用当场报错；**存量机器的兼容
  兜底没有消失，只是从文件级默认值挪进了 `load_runtime_config`**（载入前先清空两个变量，
  让本来就写着却从未生效的两行 `:=` 成为真正判据）。
- **[#265](https://github.com/Cr0ce11/sb-user-manager-public/issues/265)（P1，由正式服务器
  `Air` 暴露）**：分流的规则集来源有三种，而「服务与配置检查」自动修复前的那道校验只认了
  网址与本机规则文件两种，漏掉 GeoSite／GeoIP 类别。**任何带 geo 类别分流的机器，自动修复
  必定失败并整体回滚，而且一个字都不说明原因。** 判据改为与迁移包校验、重建分流配置一致的
  三选一；同时补上诊断能力（见第五节第 1 条）。

### 验收工具

**[#270](https://github.com/Cr0ce11/sb-user-manager-public/issues/270)：`lifecycle` 新增一整段
geo 类别分流的生命周期**（已合入 `main`，尚未随版本发布）。重点不是「geo 分流能不能加」，
而是**加了之后那些会改动现有部署的流程还走不走得通**，核心断言是「带 geo 分流时的一致性
自动修复」。跑不了时明确跳过并说清原因（内核不是 mihomo，或 mihomo 认不出类别），不假装通过。

**反向验证证明它抓得住**：把 #265 的修复退掉重跑，正是新加的那一条变红。

---

## 三、下一步计划

**没有已排期的开发任务。** 剩下的开放项，按值得做的顺序：

| 事项 | 状态 |
|---|---|
| [#172](https://github.com/Cr0ce11/sb-user-manager-public/issues/172) 存量机器的数据从 `/etc/sing-box` 真正搬到中立目录 | 方向明确，**没排期**。动手前必读该 Issue 里 2026-08-22 那条评论：存量机器形状不止一种，`sbm-mihomo` 那种的 AnyTLS 证书目录只能由 `MANAGER_DATA_DIR` 派生，搬家必须三样一起搬；完整环境快照的路径清单里也直接放着这个变量 |
| [#187](https://github.com/Cr0ce11/sb-user-manager-public/issues/187) 「验证分流是否生效」在 mihomo 上不可用 | 人肉兜底、不排期，**有意保留** |
| [#16](https://github.com/Cr0ce11/sb-user-manager-public/issues/16) Mihomo 格式的用户配置导出 | 候选 |
| `TODO.md` 的 MAINT-013 | 开发机 ShellCheck 比 CI 新，本地报 CI 不报的 SC2329（4 条噪声） |

下一次发版会自然带上已经合入但尚未发布的 #270（验收工具变更，不影响运行代码）。

---

## 四、已确定的技术决策及原因

长期决定以 [`docs/DECISIONS/`](docs/DECISIONS/) 为准。下面是接手后马上会用到的：

### 方向与范围

- **sing-box 线已归档。** 新功能、新护栏只做 mihomo 侧；sing-box 侧只维持现状，不加新东西。
- **代理协议只用 SS2022（可加 ShadowTLS）与 AnyTLS。** 不引入 Hysteria2、TUIC。
- **撤除一个功能就整段删除，不把入口藏起来**——留着一段谁都调不到的部署代码，迟早有人以为
  它还能用。撤完加断言钉住不得复活，并配一条对照证明断言不是恒真。
- **GeoSite／GeoIP 类别分流是项目所有者在用、并且明确说重要的功能。** 它只有一台正式机器在
  用（`Air`），很容易被当成边角料——不要因此少写验收或少做真机验证。#265 那个 P1 活了五个
  版本没被发现，根本原因就是没人给它建过验收覆盖。
- **内核无关的路径不许写死内核名**，用 `kernel_display_name` 等适配层入口。

### 「像真的默认值」比没有值更危险（#262）

`PROXY_KERNEL` 与 `MANAGER_DATA_DIR` 的文件级取值是**「尚未解析」哨兵**，不是可用取值。
任何在载入管理配置之前读到它们的代码会当场报错，而不是拿到一个看起来正常、实际可能完全
错误的答案（#251 就是那么坏的）。存量机器的兜底在 `load_runtime_config` 里，
配置整个丢失时由 `resolve_deployment_kernel` 显式给出——**两条分支都必须显式设数据目录**。

### 失败要说得出话（#265）

- `run_step_or_rollback` 会记一行失败的步骤名与退出码。
- **只记函数名，绝不记参数**：参数里有用户密码（`append_inbounds` 收到的是整段入站片段），
  记进日志等于把凭据扩散到日志和随后被贴出来的终端输出里。

### 目录权限（#246、#252）

- **`install -d -m` 点到一个已经存在的目录会直接 chmod 它。** 管理器自己的目录**故意**收紧到
  700；系统共享目录（`/var/lib`、`/usr/local/sbin` 等）**一律只在缺失时创建**。
- 修复被改坏的机器时**只认 700 这个指纹**，管理员自己设成别的值不覆盖；且**只对本脚本部署过
  的机器动手**。

### 备份与退路（ADR 0032）

- **操作前的完整环境快照只服务于自动回滚，不做手工恢复入口。**
- **任何不可逆操作之前必须先有一份复制到服务器之外的 `.sbm`。** 该 ADR 的「配套要求」原先
  由 `print_offsite_recovery_status` 承担，它的两个调用点随第二步撤除而消失、函数一并删除；
  **再添一个不可逆操作时必须重新落实这一条**，见 ADR 0032 新增的「后续」一节。

### 门禁

- **权威运行环境是 Linux 上的 Bash 5（`sbm-gate`）**；开发机是 macOS／arm64，
  整份单元测试在上面必定失败。
- **不改写那 502 条裸 `[[ ]]` 断言**；**新写的断言要用会自己报错的写法**。
- **新增护栏必须配反面样本与对照**：反面样本证明它不是恒真，对照证明它不误伤。
- **护栏不许被一句注释糊弄**：本轮发现「保留现有部署必须先判通道」那条门禁，只要函数里有一句
  提到判据函数名的注释就算通过；已改为跳过注释行，并补了反面样本。

### 发布与授权

- **发布正式版前停下来等项目所有者确认。**
- **创建 Release 不等于授权部署到正式环境。**
- **仓库侧不登录任何正式服务器**，除非项目所有者当面授权，且授权范围默认只到只读诊断。
- **任何凭据都不写进报告。**
- **CHANGELOG 由发布准备 PR 统一写，功能 PR 不动它。**
- **CHANGELOG 里关于「升级之后会怎样」的断言，只能由发布后那次真实菜单更新证实**；
  拿不出证据的断言，发布前就不要写（`docs/RELEASE.md` 发布后第 5 条）。

---

## 五、踩过的坑和已否决的方案及原因

### 本轮新增的九条（都会再遇到）

1. **「操作失败」这四个字曾经是全部线索。** `run_step_or_rollback` 不说是哪一步失败，
   `repair_consistency` 里几处校验不过直接静默回滚。一次真实的生产故障（#265）因此只能靠
   逐行读源码加登机比对状态文件定位——**修复本身几行就够，找到它的时间全花在这上面**。
   现在两处都会说话了；写新的失败分支时别再退回去。

2. **「已修复」可能指的是「我的机器已经好了」，不是「代码修好了」。** 项目所有者说 #252
   可以关闭时，我本轮实读代码确认修复根本没进仓库——他跑的是 Issue 里那条临时办法。
   **关 Issue 之前先看代码，别只看报告。**

3. **交接文档写的计划不一定安全，动手前先自己核一遍。** 第三步交接写的是「把两个默认值改成
   中立的」，实际那样做会让 `sbm-mihomo` 那类机器的 AnyTLS 证书被判定为不存在而重新签发，
   所有 AnyTLS 用户当场连不上。正确做法是护栏先立、兜底挪进载入配置那一步，再动值。

4. **`if ... fi` 没有 `else` 分支时退出码恒为 0。** 本轮给 `run_step_or_rollback` 加日志时，
   我把 `else rc=$?` 改成在 `fi` 之后读 `$?`，失败会被当成成功——静态门禁与单元测试当场
   一起抓住。那条门禁正是为这个写法设的。

5. **修复「服务与配置检查」报出的问题，入口是那个检查本身，不是「安装或修复环境」。**
   后者在一台完好的机器上第一个分支就返回「安装完整，无需重复部署」，根本不重写单元。
   我一度把这条说反了。

6. **升级那一次执行部署的是旧脚本。** 由此推出两件事：新脚本里的修复要下一次才生效
   （#252 的成因）；而**单元文本的漂移会被下一次升级顺手修掉**——v4.25.28 → v4.25.29
   这一跳执行部署的是 v4.25.28 的代码，写出的正是新单元文本，所以那两条提示自己就没了，
   不需要手工修复。

7. **到期定时器每 15 分钟触发一次，会拿走管理锁。** 本轮 `sbm-deb13` 的 `audit` 撞上同秒
   触发，报「管理锁：存在其他管理操作」——**那不是缺陷**。跑验收前先看
   `systemctl list-timers`，排在一次触发刚结束之后。
   ⚠️ **不要用「停掉定时器」来避开它**：只读验收有一条要求它 active。

8. **用 `bash -x` 跑测试时，追踪行会把参数打进 stderr。** 断言「输出里不出现参数」的那条用例
   因此会被自己的调试输出弄假，必须在那段前面 `set +x`。

9. **反向验证要真的把脚本装上去。** 想用 `SB_ACCEPTANCE_MANAGER` 指向另一份脚本来做反向
   验证，结果 `audit` 先因为「root 启动副本一致」失败——反向验证于是失败在了错误的地方。
   老老实实装到 `/usr/local/sbin` 与 `/root`，跑完再装回来。

### 更早的坑（仍然有效）

10. **护栏可能把缺陷一起钉住。** 写结构性断言时必须写清它保护的是哪个不变量。

11. **跟在 `if`／`!`／`&&`／`||` 后面的命令，其整个动态调用范围内 `set -e` 都不生效。**

12. **裸 `[[ ]]` 断言失败时一个字都不打印。** 定位手法：
    `bash -x tests/test-unit.sh 2>/tmp/x.log >/dev/null; tail -25 /tmp/x.log`。

13. **OrbStack 的共享文件系统偶尔让构建或客机读到半份文件。** 本轮又遇到一次：门禁机报
    「生成脚本语法检查失败」，而两侧摘要一致、`bash -n` 都通过，重跑即过。
    **先比对摘要再重试，不要当成代码缺陷去查。**

14. **`$(...)` 会在 `gh pr create --body "..."` 里被 shell 执行**；正文一律用 `--body-file`，
    提交说明一律用 `-F` 文件。

15. **`lifecycle` 等写入型验收需要 `SB_ACCEPTANCE_CONFIRM=YES`。**

16. **用 `cp` 直装脚本到测试机，`audit` 会报两项假失败**（版本记录、root 启动副本）。
    同步 `/var/lib/sb-user-manager/versions` 与 `/root/sb-user-manager.sh` 即可避免。

17. **宿主机的代理客户端工作在 fake-IP 模式**，会把公网域名解析成 `198.18.x.x`；
    两台部署机上有绕开它的 `/etc/hosts` 条目。

18. **文档里靠人记得的那一行迟早会漂。** 遇到同类「必须一起改」的两处，直接加断言。

### 已否决的方案

| 方案 | 为什么否决 |
|---|---|
| 第三步只把两个默认值改成中立的 | 解决不了「提前读到像真的错误答案」，还会让没有 `MANAGER_DATA_DIR` 行的机器重新签发 AnyTLS 证书 |
| 用 `environment_is_deployed` 当权限修复的门槛 | 它要看当前内核的核心文件，而那一刻内核变量还是文件级取值，mihomo 机器会被判成没部署（同 #251 那个坑）；改用「管理配置在不在」 |
| 把失败步骤的参数一起记进日志 | 参数里有用户密码；只记函数名 |
| 给完整环境快照加「从操作前快照恢复」的菜单入口 | 会静静抹掉快照之后的用户、分流与用量；详见 ADR 0032 |
| 把 502 条裸 `[[ ]]` 断言全部改写 | 它们在 Bash 5 上有效，问题出在运行环境 |
| 给生产线单开一条只修 bug 的 sing-box 稳定线 | 前提（生产全是 sing-box）已消失，#202 已关闭 |
| 让脚本去清理 apt 装的 sing-box 残包 | dpkg 装的东西必须由 dpkg 卸载；且 sing-box 线已归档（#247） |
| 停掉到期定时器来避开验收时的锁冲突 | 只读验收要求它是 active |
| 用 `openssl s_client` 的退出码判断对方说不说 TLS 1.3 | 证书验证失败时它也非零退出 |

### 环境与流程

- **门禁在门禁机上跑**：`orb -m sbm-gate bash tests/test-static.sh`；其余同理。
  **门禁 12 条不含 ShellCheck**，跑完在开发机补跑 CI 里那两条（第二条会报 4 条 CI 不报的
  SC2329，见 MAINT-013）。
- **`tests/test-public-snapshot.sh` 要求工作区干净**，必须提交之后才能跑。
- **还原临时破坏先 `cp` 一份**，不要用 `git checkout <文件>`；还原后比对生成物摘要。
- **提交前跑 `git status --short` 和 `git diff --stat` 逐处确认。**
- **改 `src/` 之后要跑 `bash tools/build-manager.sh`** 重新生成单脚本。
- **服务器连接方式**：别名直接可用（`ssh Air`），资产清单在个人文件 `~/.claude/servers.md`，
  不在本仓库内。

### 怎么跑「真实菜单更新路径」

用伪终端驱动编号式菜单：`pty.fork()` 起脚本，读屏幕、用正则从 `  N  标签` 里取编号发过去。
本轮实际走通的几条路径：

- 更新：`系统管理` → `检测更新` → `y`
- 修复一致性：`系统管理` → `检查与故障报告` → `服务与配置检查` → `y`
- 看菜单：`系统管理` → `部署与卸载`

**驱动器要注意**：按键发完不能因为没输出就退出（下载内核和 Nfuse 时会长时间静默），
要一直等到完成标记命中或总超时；跑更新之前先把机器退回上一版的**已安装脚本、版本记录和
root 启动副本**三处，否则脚本会认为已是最新而不走下载。

**证明「升级之后会怎样」时，要从一个真的会出问题的版本升上来。** 本轮验证目录权限那条断言
时特意从 v4.25.26 升，而不是 v4.25.27——后者的部署代码本来就不会改坏目录，从它升上来什么
都证明不了。
